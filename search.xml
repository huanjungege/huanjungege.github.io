<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>20-03-09vuex</title>
    <url>/2020/03/09/20-03-09vuex/</url>
    <content><![CDATA[<h2 id="Vuex是什么？"><a href="#Vuex是什么？" class="headerlink" title="Vuex是什么？"></a>Vuex是什么？</h2><p>根据官网解释是一个专门为Vue.js应用程序开发的状态管理模式。（个人理解为数据管理，一个程序在刚开发阶段组件比较少，组件之间的传值解决也很方便，但是当程序越来越大，组件越来越多的时候数据越来越多的时候，组件传值变得复杂难以管理）。Vuex就是将多个组件都会用到的数据进行集中管理，哪个组件需要这些数据就在Vuex的存储库中提取，使数据的调用变得简单直接。</p>
<a id="more"></a>

<h3 id="Vuex的状态管理包含三个部分："><a href="#Vuex的状态管理包含三个部分：" class="headerlink" title="Vuex的状态管理包含三个部分："></a>Vuex的状态管理包含三个部分：</h3><ol>
<li>state，驱动应用的数据源；</li>
<li>view，以声明的方式将state映射到视图；</li>
<li>actions，响应在view上的用户输入导致的状态变化。</li>
</ol>
<img src="/2020/03/09/20-03-09vuex/1.jpg">

<p>这是一个单项数据流。</p>
<p>state中的数据渲染到view视图，用户通过actions交互事件更改state数据，数据再次渲染，形成一个单项数据流。</p>
<h2 id="正常情况下的数据管理"><a href="#正常情况下的数据管理" class="headerlink" title="正常情况下的数据管理"></a>正常情况下的数据管理</h2><p>通过官方文档实现一个计数器</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">&lt;h1&gt;点击数：&#123;&#123; count &#125;&#125;&lt;<span class="regexp">/h1&gt;</span></span><br><span class="line"><span class="regexp">&lt;input type="button" value="点击增加" @click="addEvent" /</span>&gt;</span><br><span class="line">&lt;<span class="regexp">/template&gt;</span></span><br><span class="line"><span class="regexp">export default &#123;</span></span><br><span class="line"><span class="regexp">  name: "Store",</span></span><br><span class="line"><span class="regexp">  data() &#123;</span></span><br><span class="line"><span class="regexp">    return &#123;</span></span><br><span class="line"><span class="regexp">      count: 0</span></span><br><span class="line"><span class="regexp">    &#125;;</span></span><br><span class="line"><span class="regexp">  &#125;,</span></span><br><span class="line"><span class="regexp">  methods: &#123;</span></span><br><span class="line"><span class="regexp">    addEvent:function()&#123;</span></span><br><span class="line"><span class="regexp">        this.count++</span></span><br><span class="line"><span class="regexp">    &#125;</span></span><br></pre></td></tr></table></figure>

<img src="/2020/03/09/20-03-09vuex/2.jpg">

<p>那么Vuex的方式是</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;!-- Vuex方式 --&gt;</span><br><span class="line">    &lt;h1&gt;点击数：&#123;&#123; $store.state.num &#125;&#125;&lt;<span class="regexp">/h1&gt;</span></span><br><span class="line"><span class="regexp">    &lt;input type="button" value="点击增加" @click="emitAction" /</span>&gt;</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  name: <span class="string">"Store"</span>,</span><br><span class="line">  methods: &#123;</span><br><span class="line">  <span class="comment">//vuex中的mutation不能直接调用，我们需要调用store.commit方法</span></span><br><span class="line">    emitAction:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.$store.commit(<span class="string">'addEvent2'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<img src="/2020/03/09/20-03-09vuex/3.jpg">

<p>当勾选了Vuex后，会自动创建一个store实例，在state中存储数据，mutations中 定义方法</p>
<img src="/2020/03/09/20-03-09vuex/4.jpg">

<p>Vuex中的state：</p>
<p>state可以称作Vuex存储数据的仓库，在组件中调用数据时我们发现在组件中的模板语法是这样的，$state.store.num,较为繁琐，我们可以这样写</p>
<img src="/2020/03/09/20-03-09vuex/6.jpg">

<p>这个时候发现下面也很繁琐所以Vuex提供了<code>mapState</code>辅助函数帮助我们生成计算属性</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//第一种方式</span></span><br><span class="line">computed:mapSate([<span class="string">'age'</span>,<span class="string">'gender'</span>,<span class="string">'num'</span>]);</span><br><span class="line"><span class="comment">//第二种</span></span><br><span class="line">computed:mapState(&#123;</span><br><span class="line">    age: <span class="string">'age'</span>,</span><br><span class="line">    <span class="comment">//或者箭头函数方式</span></span><br><span class="line">    gender:<span class="function">(<span class="params">state</span>) =&gt;</span>state.gender,</span><br><span class="line">    num:<span class="function">(<span class="params">state</span>) =&gt;</span> state.num</span><br><span class="line">&#125;)</span><br><span class="line">computed: &#123;</span><br><span class="line">    ...mapState([<span class="string">"username"</span>, <span class="string">"age"</span>, <span class="string">"list"</span>]),</span><br><span class="line">    ...mapGetters([<span class="string">'xuAge'</span>])</span><br><span class="line">  &#125;,</span><br><span class="line">  methods: &#123;</span><br><span class="line">    <span class="comment">// addAge: function() &#123;</span></span><br><span class="line">    <span class="comment">//   this.$store.commit("addAge");</span></span><br><span class="line">    <span class="comment">// &#125;,</span></span><br><span class="line">    ...mapMutations([<span class="string">'addAge'</span>,<span class="string">'getList'</span>]),</span><br><span class="line">    ...mapActions([<span class="string">'getJoke'</span>])</span><br></pre></td></tr></table></figure>

<p>Vuex中的Getter：</p>
<p>刚开始想组件中有计算属性，那么为什么要使用getter，组件中的计算属性只能在该组件中使用而：</p>
<p>Getter相当于全局的computed(计算属性)</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">getters:&#123;</span><br><span class="line">reverseMSg:<span class="function"><span class="keyword">function</span>(<span class="params">state</span>)</span>&#123;</span><br><span class="line"><span class="comment">//返回它的颠倒值</span></span><br><span class="line">  <span class="keyword">return</span> state.msg.split(<span class="string">''</span>).reverse().join(<span class="string">''</span>)</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>VueX中的mutations相当于组件中的methods</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//修改数据/状态的方法--methods</span></span><br><span class="line">  mutations: &#123;</span><br><span class="line">    addAge(state,payload)&#123;</span><br><span class="line">      <span class="comment">// console.log(state)</span></span><br><span class="line">      state.age += payload</span><br><span class="line">    &#125;,</span><br><span class="line">    getList(state,payload)&#123;</span><br><span class="line">      state.list = payload</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br></pre></td></tr></table></figure>

<p>VueX中的actions则是异步的修改数据</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">actions: &#123;</span><br><span class="line">    getJoke(context)&#123;</span><br><span class="line">      <span class="keyword">let</span> httpUrl = <span class="string">'https://api.apiopen.top/getJoke?page=1&amp;count10&amp;type=text'</span></span><br><span class="line">      fetch(httpUrl).then(<span class="function">(<span class="params">res</span>)=&gt;</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> res.json()</span><br><span class="line">      &#125;).then(<span class="function">(<span class="params">res</span>)=&gt;</span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(res)</span><br><span class="line">        context.commit(<span class="string">'getList'</span>,res.result)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">      <span class="comment">//将异步请求到的数据渲染到列表中</span></span><br><span class="line">      &lt;ul&gt;</span><br><span class="line">      &lt;li v-<span class="keyword">for</span>=<span class="string">"(item, i) in $store.state.list"</span> :key=<span class="string">"i"</span>&gt;</span><br><span class="line">        &lt;h3&gt;&#123;&#123; item.name &#125;&#125;&lt;<span class="regexp">/h3&gt;</span></span><br><span class="line"><span class="regexp">        &lt;p&gt;&#123;&#123; item.text &#125;&#125;&lt;/</span>p&gt;</span><br><span class="line">      &lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">    &lt;/u</span>l&gt;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Vuex</tag>
      </tags>
  </entry>
  <entry>
    <title>Nodejs入门介绍</title>
    <url>/2019/10/22/2019-10-22-node-01/</url>
    <content><![CDATA[<h1 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h1><h2 id="Node-js是什么"><a href="#Node-js是什么" class="headerlink" title="Node.js是什么?"></a><code>Node.js</code>是什么?</h2><p>在node官网的介绍中看到</p>
<ol>
<li><strong>Node.js® is a JavaScript runtime built on <a href="https://v8.dev/" target="_blank" rel="noopener">Chrome’s V8 JavaScript engine</a>.</strong></li>
</ol>
<ul>
<li>Nodejs不是一门语言，不是库和框架，是一个<code>Javascript</code>运行环境</li>
<li>以前只有浏览器可以解析执行Javascript代码，由于Node的出现现在js可以脱离浏览器运行</li>
<li>Node中没有BOM和DOM，在Ecmascript的基础上增加了一些服务器级别的操作API</li>
<li>例如：文件读写，网络服务的构建，网络通信，http服务器</li>
</ul>
<a id="more"></a>

<ol start="2">
<li><strong>Node.js uses an <code>event-driven</code>, <code>non-blocking I/O moedl</code>  that makes it <code>lightweight and efficent*</code></strong></li>
</ol>
<ul>
<li>event-driven:事件驱动</li>
<li>non-blocking I/O model： 非阻塞I/O类型（异步）</li>
<li>lightweight and efficent: 轻量和高效的</li>
</ul>
<p>这句话展现了Node.js的特性，在以后的学习中我们会慢慢理解</p>
<ol start="3">
<li><strong>Node.js package ecosytem ,<code>npm</code>,is the largest ecosystem of open source libraries in the world</strong></li>
</ol>
<ul>
<li>npm是世界上最大的开源库生态系统</li>
<li>绝大多数JavaScript相关的包都存放在npm上，这样可以让开发人员方便下载</li>
<li><code>npm install jquery</code></li>
</ul>
<h2 id="学习Node-js可以了解到"><a href="#学习Node-js可以了解到" class="headerlink" title="学习Node.js可以了解到"></a>学习Node.js可以了解到</h2><ul>
<li>B/S编程模型<ul>
<li>Browser-Sever</li>
<li>back-end</li>
</ul>
</li>
<li>模块化编程<ul>
<li>以前的JavaScript只能通过script标签加载，现在可以使用@import(‘文件路径’)</li>
</ul>
</li>
<li>Node常用API</li>
<li>异步编程<ul>
<li>回调函数</li>
<li>Promise</li>
<li>ES6(只是一种新语法)</li>
</ul>
</li>
</ul>
<h2 id="模块的导入与导出"><a href="#模块的导入与导出" class="headerlink" title="模块的导入与导出"></a>模块的导入与导出</h2><p>index1想要使用index2中的内容：index1中需要使用require()导入index2，index2中将需要被引用的内容使用module.exports()导出。</p>
<h2 id="Node中的包"><a href="#Node中的包" class="headerlink" title="Node中的包"></a>Node中的包</h2><p>包其实是一个存档文件。完全复合CommonJS规范的包目录最基本应该包含如下这些文件。</p>
<ol>
<li>package.json：包描述文件</li>
<li>bin：用于存放可执行二进制文件的目录</li>
<li>lib：用于存放JavaScript代码的目录</li>
<li>doc：用于存放文档的目录</li>
<li>test：用于存放单元测试用例的代码</li>
</ol>
]]></content>
      <categories>
        <category>后端</category>
        <category>Node</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>Nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>Node学习入门2</title>
    <url>/2019/10/22/2019-10-22-node-02/</url>
    <content><![CDATA[<h2 id="Node安装检测"><a href="#Node安装检测" class="headerlink" title="Node安装检测"></a>Node安装检测</h2><p>cmd中输入<code>node --version</code>/<code>node -v</code>检测版本号</p>
<img src="/2019/10/22/2019-10-22-node-02/01.png">

<a id="more"></a>

<h2 id="HelloWorld"><a href="#HelloWorld" class="headerlink" title="HelloWorld"></a>HelloWorld</h2><ol>
<li><p>创建编写Javascript脚本</p>
<img src="/2019/10/22/2019-10-22-node-02/02.png">
</li>
<li><p>打开终端，定位到脚本文件所属目录（切换盘符输入d:,然后盘符内相关目录<code>cd 03Nodejs学习记录及代码练习\01</code>）</p>
</li>
<li><p>输入node &lt;”00-helloworld.js”&gt;</p>
<img src="/2019/10/22/2019-10-22-node-02/03.png">

</li>
</ol>
<p><strong>切记文件名字不要起node.js，并且最好不要起中文</strong></p>
<h2 id="Node-js中有没有BOM、DOM"><a href="#Node-js中有没有BOM、DOM" class="headerlink" title="Node.js中有没有BOM、DOM"></a>Node.js中有没有BOM、DOM</h2><ol>
<li><p>新建js文件</p>
<img src="/2019/10/22/2019-10-22-node-02/05.png">
</li>
<li><p>运行结果</p>
<img src="/2019/10/22/2019-10-22-node-02/04.png">

<p>可以看到报了引用错误，window is not defined，可见Node.js中没有BOM和DOM</p>
</li>
</ol>
<h2 id="Node的文件操作"><a href="#Node的文件操作" class="headerlink" title="Node的文件操作"></a>Node的文件操作</h2><p>Node具有文件操作的能力，<code>file-system</code>文件系统，我们简写为<code>fs</code></p>
<p><strong>在Node中进行文件操作必须引入文件系统</strong></p>
<h3 id="读取文件"><a href="#读取文件" class="headerlink" title="读取文件"></a>读取文件</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用require()方法引用fs核心模块</span></span><br><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>)</span><br><span class="line"><span class="comment">//读取文件</span></span><br><span class="line"><span class="comment">//第一个参数：文件地址</span></span><br><span class="line"><span class="comment">//第二个参数：回调函数</span></span><br><span class="line"><span class="comment">//回调函数中有两个参数(data 和 error)</span></span><br><span class="line"><span class="comment">//成功:error为null   data为数据</span></span><br><span class="line"><span class="comment">//失败:error为错误对象  data写入失败</span></span><br><span class="line">fs.readFile(<span class="string">'./data/hello.txt'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">error, data</span>)</span>&#123;</span><br><span class="line">     <span class="built_in">console</span>.log(data);<span class="comment">//自动输入数据</span></span><br><span class="line">    <span class="built_in">console</span>.log(data.toString());<span class="comment">//转换为字符串输出</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<img src="/2019/10/22/2019-10-22-node-02/06.png">

<h3 id="异步写入文件"><a href="#异步写入文件" class="headerlink" title="异步写入文件"></a>异步写入文件</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fs=<span class="built_in">require</span>(<span class="string">'fs'</span>);</span><br><span class="line"><span class="comment">//三个参数 </span></span><br><span class="line"><span class="comment">// 1.文件写入路径文件名</span></span><br><span class="line"><span class="comment">// 2.写入内容</span></span><br><span class="line"><span class="comment">// 3.回调函数（参数 error）</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//写入成功  error为null</span></span><br><span class="line"><span class="comment">//写入失败  error为错误对象</span></span><br><span class="line">fs.writeFile(<span class="string">'./data/你好Node.md'</span>, <span class="string">"你好Nodejs！"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">error</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'写入成功'</span>);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="当文件处理出现错误"><a href="#当文件处理出现错误" class="headerlink" title="当文件处理出现错误"></a>当文件处理出现错误</h3><p>我们在上述回调函数中添加代码<code>console.log(error)</code>,那么在读取成功时返回null，在读取失败时返回错误代码</p>
<p>所以我们可以加入对error的判断代码</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(error)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"读取出错了"</span>);</span><br><span class="line">  &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(data.toString());</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="简单的HTTP服务"><a href="#简单的HTTP服务" class="headerlink" title="简单的HTTP服务"></a>简单的HTTP服务</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//加载http模块</span></span><br><span class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>)</span><br><span class="line"><span class="comment">//创建http实例</span></span><br><span class="line"><span class="keyword">var</span> server = createServer()</span><br><span class="line"><span class="comment">//注册request请求事件，当客户端请求过来，就会自动触发服务器的request事件，然后执行回调函数</span></span><br><span class="line">server.on(<span class="string">'request'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="string">'收到客户端请求了'</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">//绑定端口号，启动服务器</span></span><br><span class="line">server.listen(<span class="number">3000</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'服务器启动成功了，通过127.0.0.1:3000进行访问'</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>这样服务端启动成功并且接收到请求，浏览器却接收不到响应，一直在等待。。</p>
<h3 id="如何进行响应"><a href="#如何进行响应" class="headerlink" title="如何进行响应"></a>如何进行响应</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//加载http模块</span></span><br><span class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>)</span><br><span class="line"><span class="comment">//创建http实例</span></span><br><span class="line"><span class="keyword">var</span> server = createServer()</span><br><span class="line"><span class="comment">//注册request请求事件，当客户端请求过来，就会自动触发服务器的request事件，然后执行回调函数</span></span><br><span class="line"><span class="comment">// reuqest请求事件处理函数，共有两个参数</span></span><br><span class="line"><span class="comment">//    Request:请求对象   用来获取请求对象信息，例如请求路径</span></span><br><span class="line"><span class="comment">//    Response: 响应对象  用来给客户端发送响应信息</span></span><br><span class="line">server.on(<span class="string">'request'</span>,<span class="function"><span class="keyword">function</span>(<span class="params">request,response</span>)</span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="string">'收到客户端请求了,请求地址为'</span> + request.url)</span><br><span class="line">    <span class="comment">//用户请求路径不同响应不同的信息</span></span><br><span class="line">    <span class="keyword">if</span>(request.url == <span class="string">'/index'</span>)&#123;</span><br><span class="line">        response.write(<span class="string">'hello'</span>)</span><br><span class="line">        response.end();<span class="comment">//告诉客户端：我的话说完了，可以呈递给用户了</span></span><br><span class="line">    &#125;<span class="keyword">else</span> <span class="keyword">if</span>(request.url == <span class="string">'/tags'</span>)&#123;</span><br><span class="line">        response.write(<span class="string">'hi'</span>)</span><br><span class="line">        response.end();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">//绑定端口号，启动服务器</span></span><br><span class="line">server.listen(<span class="number">3000</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'服务器启动成功了，通过127.0.0.1:3000进行访问'</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h2 id="Node中的核心模块"><a href="#Node中的核心模块" class="headerlink" title="Node中的核心模块"></a>Node中的核心模块</h2><p>Node为Javascript提供了很多服务器级别的API,这些API绝大多数都包装到了一个具名的核心模块中了</p>
<p>例如：</p>
<ol>
<li>文件操作的<code>fs</code>模块</li>
<li>http构建服务的<code>http</code>模块</li>
<li><code>path</code>路径操作模块</li>
<li><code>os</code>操作系统信息模块</li>
</ol>
<p>所以以后当知道要引用一个核心模块，就千万别忘了’引用‘</p>
<h3 id="require的两个作用"><a href="#require的两个作用" class="headerlink" title="require的两个作用"></a>require的两个作用</h3><p>require方法的第一个作用就是加载模块，具名的核心模块或者自己编写的模块</p>
<p>例：</p>
<img src="/2019/10/22/2019-10-22-node-02/06.png">

<p><strong>note:相对路径必须加./    如不加会被当做核心模块</strong></p>
<p>在Node中没有全局作用域，只有模块作用域，模块完全封闭，外部与内部无法互相访问，那么模块之间如何进行通信，下面介绍require的第二个作用</p>
<p>拿到被加载文件模块导出的接口对象</p>
<p>每个文件模块都提供了一个对象<code>exports</code>（默认是一个空对象）</p>
<img src="/2019/10/22/2019-10-22-node-02/08.png">

<p>可以看到经过exports挂载的foo可以被a.js访问到，并且b中本身自带的foo并不受影响</p>
]]></content>
      <categories>
        <category>后端</category>
        <category>Node</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>Nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>Js中的Array类型总结</title>
    <url>/2019/10/28/2019-10-28-Array/</url>
    <content><![CDATA[<h1 id="Js中的Array类型"><a href="#Js中的Array类型" class="headerlink" title="Js中的Array类型"></a>Js中的Array类型</h1><p>序：除了Object类型之外，Array类型恐怕是ECAMAScript中最常用的类型了，特点：有序列表，并且数组的每一项都可以保存任意数据类型的数据，数组的大小可以动态的调整。</p>
<h2 id="创建数组的方式"><a href="#创建数组的方式" class="headerlink" title="创建数组的方式"></a>创建数组的方式</h2><h3 id="第一种"><a href="#第一种" class="headerlink" title="第一种"></a>第一种</h3><p>使用Array构造函数:</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>();</span><br></pre></td></tr></table></figure>

<ol>
<li>如果预先知道数组保存项的数量，可以给数组传递数量</li>
</ol>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">20</span>);  <span class="comment">//创建数组项数为20的数组</span></span><br></pre></td></tr></table></figure>

<p>2.直接 向数组传递要传入的值</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="string">"1"</span>,<span class="string">"2"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="第二种"><a href="#第二种" class="headerlink" title="第二种"></a>第二种</h3><a id="more"></a>

<p>使用数组字面量法</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = [<span class="string">"green"</span>,<span class="string">"red"</span>,<span class="string">"blue"</span>];</span><br><span class="line"><span class="keyword">var</span> colors = []; <span class="comment">//空数组</span></span><br></pre></td></tr></table></figure>

<p><strong>数组的<code>length</code>很有特点，它不是只读的，我们可以直接控制数组的<code>length</code>值，控制数组的末尾移除项或者添加新项</strong></p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = [<span class="string">"green"</span>,<span class="string">"red"</span>,<span class="string">"blue"</span>];</span><br><span class="line">colors.length = <span class="number">4</span>;</span><br><span class="line"><span class="built_in">console</span>.log(colors[<span class="number">3</span>]); <span class="comment">//undefined</span></span><br></pre></td></tr></table></figure>

<p>由于将colors的length值增加了1，所以当我们访问到3号索引，数组动态的添加了undefined值</p>
<h2 id="数组中的方法"><a href="#数组中的方法" class="headerlink" title="数组中的方法"></a>数组中的方法</h2><h3 id="检测方法"><a href="#检测方法" class="headerlink" title="检测方法"></a>检测方法</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Array</span>.isArray([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]); <span class="comment">//true</span></span><br><span class="line"><span class="built_in">Array</span>.isarray(&#123;<span class="attr">foo</span>:<span class="number">123</span>&#125;)<span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h3 id="转换方法"><a href="#转换方法" class="headerlink" title="转换方法"></a>转换方法</h3><p>转换方法一共有三种<code>tostring()</code>、<code>tolocaleString()</code>、<code>valueOf()</code>作用返回数组中的每个值的字符串形式拼接而成的一个以逗号分隔的字符串，而<code>join()</code>方法则可以使用不同的分隔符来构建字符串</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = [<span class="string">"green"</span>,<span class="string">"red"</span>,<span class="string">"blue"</span>];</span><br><span class="line">alert(colors.join(<span class="string">"||"</span>)); <span class="comment">//green||red||blue</span></span><br></pre></td></tr></table></figure>

<p><em>如果数组中的某一项为null或undefined，那么以上方法返回的结果中以空字符串表示</em></p>
<h3 id="栈方法（后进先出）"><a href="#栈方法（后进先出）" class="headerlink" title="栈方法（后进先出）"></a>栈方法（后进先出）</h3><p>ECMAScript中数组也提供了让数组的行为类似于其它数据结构的方法</p>
<p><code>push()</code>方法：接收任意数量的值，将他们逐个添加到数组末尾</p>
<p><code>pop()</code>方法:删除数组末尾最后一项，减少length值</p>
<h3 id="队列方法（先进先出）"><a href="#队列方法（先进先出）" class="headerlink" title="队列方法（先进先出）"></a>队列方法（先进先出）</h3><p><code>unshift()</code>方法：从数组前端添加任意项</p>
<p><code>shift()</code>方法：从数组前端减少任意项</p>
<h3 id="重排序方法"><a href="#重排序方法" class="headerlink" title="重排序方法"></a>重排序方法</h3><h4 id="reverse-方法：会反转数组的顺序"><a href="#reverse-方法：会反转数组的顺序" class="headerlink" title="reverse()方法：会反转数组的顺序"></a><code>reverse()</code>方法：会反转数组的顺序</h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];</span><br><span class="line">alert(arr.reverse()); <span class="comment">//5,4,3,2,1</span></span><br></pre></td></tr></table></figure>

<h4 id="sort-方法"><a href="#sort-方法" class="headerlink" title="sort()方法"></a><code>sort()</code>方法</h4><p><code>sort()</code>按升序排列数组，sort会调用每个数组的<code>toString()</code>方法，比较得到的字符串</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">0</span>,<span class="number">1</span>,<span class="number">5</span>,<span class="number">10</span>,<span class="number">15</span>];</span><br><span class="line">arr.sort()</span><br><span class="line"><span class="built_in">console</span>.log(arr); <span class="comment">//0,1,10,15,5</span></span><br></pre></td></tr></table></figure>

<p>这种情况是数值5虽然小于10，但是字符串进行比较时却位于10的前面。这时可以使用一个比较函数</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compare</span>(<span class="params">value1,value2</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(value1 &gt; value2) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span> <span class="keyword">if</span>(varlue1 &lt; value2) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">比较两个参数，如果第一个值在第二个值之后则返回<span class="number">-1</span>，如果在第二个值之前返回<span class="number">1</span>；</span><br><span class="line">简化</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compare2</span>(<span class="params">a,b</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a-b;</span><br><span class="line">&#125;</span><br><span class="line">arr.sort(compare2)</span><br></pre></td></tr></table></figure>

<h3 id="操作方法"><a href="#操作方法" class="headerlink" title="操作方法"></a>操作方法</h3><h4 id="concat-方法"><a href="#concat-方法" class="headerlink" title="concat()`方法"></a>concat()`方法</h4><p><code>concat()</code>可以基于当前数组中的所有项来创建一个新数组。具体来说这个方法会创建当前数组的副本，然后将接收到的参数添加到数组末尾，最后返回到新创建的数组。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>];</span><br><span class="line"><span class="keyword">var</span> arr2 = arr.concat(<span class="number">5</span>,[<span class="number">6</span>,<span class="number">7</span>]);</span><br><span class="line">alert(arr2); <span class="comment">//1,2,3,4,5,6,7;</span></span><br></pre></td></tr></table></figure>

<h4 id="slice-方法"><a href="#slice-方法" class="headerlink" title="slice()方法"></a><code>slice()</code>方法</h4><p>它能够基于当前数组中的一项或者多项创建新的数组（对数组进行截取），可以接收一个或两个参数</p>
<h5 id="一个参数时："><a href="#一个参数时：" class="headerlink" title="一个参数时："></a>一个参数时：</h5><p>截取参数位到末尾的所有项</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = [<span class="string">"green"</span>,<span class="string">"red"</span>,<span class="string">"blue"</span>,<span class="string">"orange"</span>];</span><br><span class="line"><span class="keyword">var</span> result = colors.slice(<span class="number">2</span>);</span><br><span class="line">alert(result) <span class="comment">//blue,orange</span></span><br></pre></td></tr></table></figure>

<h5 id="两个参数时："><a href="#两个参数时：" class="headerlink" title="两个参数时："></a>两个参数时：</h5><p>截取第一个参数位到第二个参数位之前一位的所有项</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = [<span class="string">"green"</span>,<span class="string">"red"</span>,<span class="string">"blue"</span>,<span class="string">"orange"</span>];</span><br><span class="line"><span class="keyword">var</span> result = colors.slice(<span class="number">0</span>,<span class="number">2</span>);</span><br><span class="line">alert(result) <span class="comment">//green red</span></span><br></pre></td></tr></table></figure>

<h4 id="splice-方法"><a href="#splice-方法" class="headerlink" title="splice()方法"></a><code>splice()</code>方法</h4><p><code>splice()</code>方法恐怕要算最强大的数组方法了，它有很多中用法</p>
<ul>
<li><p>删除：可以删除任意数组的项，输入两个参数，要删除的第一项位置和删除的个数</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = [<span class="string">"red"</span>,<span class="string">"green"</span>,<span class="string">"blue"</span>];</span><br><span class="line">colors.splice(<span class="number">0</span>,<span class="number">1</span>); <span class="comment">//green,blue   第一个开始删除一个</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>替换：在指定位置插入任意数量的项，且同时删除任意数量的项，三个参数：起始位置、要删除的个数、要插入的项</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = [<span class="string">"red"</span>,<span class="string">"green"</span>,<span class="string">"blue"</span>];</span><br><span class="line">colors.splice(<span class="number">1</span>,<span class="number">1</span>,<span class="string">"red"</span>,<span class="string">"purple"</span>)</span><br><span class="line">alert(colors); <span class="comment">//red,red,purple,green,blue</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>插入：在指定位置插入任意数量的项，三个参数：起始位置、0、插入的项</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//插入可以理解为在替换过程中不删除数组的元素而直接添加元素，那么第二个参数便为0；</span></span><br><span class="line"><span class="keyword">var</span> colors = [<span class="string">"red"</span>,<span class="string">"green"</span>,<span class="string">"blue"</span>];</span><br><span class="line">colors.splice(<span class="number">1</span>,<span class="number">0</span>,<span class="string">"orange"</span>);</span><br><span class="line">alert(colors); <span class="comment">// red,green,orange,blue</span></span><br></pre></td></tr></table></figure>

<h3 id="位置方法"><a href="#位置方法" class="headerlink" title="位置方法"></a>位置方法</h3><p><code>indexOf()</code>和<code>lastIndexOf()</code>方法。一个从数组开头开始查找，一个从数组末尾开始查找</p>
<h4 id="一个参数时-参数为要查找的项"><a href="#一个参数时-参数为要查找的项" class="headerlink" title="一个参数时:参数为要查找的项"></a>一个参数时:参数为要查找的项</h4><figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">var fruits = [&quot;banana&quot;,&quot;apple&quot;,&quot;mango&quot;,&quot;orange&quot;,&quot;apple&quot;];</span><br><span class="line">fruits.indexOf(apple);//0</span><br></pre></td></tr></table></figure>

<h4 id="两个参数时：参数为要查找的项，和起始位置，如果找不到返回-1"><a href="#两个参数时：参数为要查找的项，和起始位置，如果找不到返回-1" class="headerlink" title="两个参数时：参数为要查找的项，和起始位置，如果找不到返回-1"></a>两个参数时：参数为要查找的项，和起始位置，如果找不到返回-1</h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fruits = [<span class="string">"banana"</span>,<span class="string">"apple"</span>,<span class="string">"mango"</span>,<span class="string">"orange"</span>,<span class="string">"apple"</span>];</span><br><span class="line">fruits.indexOf(<span class="string">"apple"</span>,<span class="number">3</span>) <span class="comment">//4</span></span><br></pre></td></tr></table></figure>

<h3 id="迭代方法"><a href="#迭代方法" class="headerlink" title="迭代方法"></a>迭代方法</h3><h4 id="map-方法：对数组中的每一项进行给定函数，返回每次函数调用结果组成数组"><a href="#map-方法：对数组中的每一项进行给定函数，返回每次函数调用结果组成数组" class="headerlink" title="map()方法：对数组中的每一项进行给定函数，返回每次函数调用结果组成数组"></a><code>map()</code>方法：对数组中的每一项进行给定函数，返回每次函数调用结果组成数组</h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">pow</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x*x;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>];</span><br><span class="line"><span class="keyword">var</span> result = arr.map(pow);</span><br><span class="line">alert(result); <span class="comment">//1,4,9,16,25,36</span></span><br><span class="line"><span class="comment">//将数组转为字符串</span></span><br><span class="line"><span class="keyword">var</span> result2 = arr.map(toString)</span><br><span class="line">alert(result2);<span class="comment">//"1","2","3","4","5","6"</span></span><br></pre></td></tr></table></figure>

<h4 id="every-和some-方法"><a href="#every-和some-方法" class="headerlink" title="every()和some()方法"></a><code>every()</code>和<code>some()</code>方法</h4><p>两个方法都是对数组中的每一项运行给定函数，但<code>every()</code>方法在所有项返回true时才会返回true，相反some只有一项返回true就返回true；</p>
<h4 id="filter-方法"><a href="#filter-方法" class="headerlink" title="filter()方法"></a><code>filter()</code>方法</h4><p>把传入的函数依次作用于每个元素，根据返回值是true还是false决定保留还是丢弃该元素</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> numbers = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">3</span>]</span><br><span class="line"><span class="keyword">var</span> results = numbers.filter(<span class="function"><span class="keyword">function</span>(<span class="params">items,index,array</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (items &gt; <span class="number">2</span>);</span><br><span class="line"> &#125;)</span><br><span class="line">alert(results)<span class="comment">//3,5,6,3</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//删除掉偶数</span></span><br><span class="line"><span class="keyword">var</span> numbers = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">3</span>];</span><br><span class="line"><span class="keyword">var</span> results = numbers.filter(<span class="function"><span class="keyword">function</span>(<span class="params">items,index,array</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (items % <span class="number">2</span>!== <span class="number">0</span>);</span><br><span class="line"> &#125;)</span><br><span class="line">alert(results)<span class="comment">//1,3,5,3</span></span><br></pre></td></tr></table></figure>


</li>
</ul>
<p>  <code>filter()</code>方法接收的回调函数可以有多个参数，通常我们使用第一个参数表示Array的某个元素，另两个参数表示元素的位置和数组本身</p>
  <figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//去掉重复的元素</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">5</span>];</span><br><span class="line"><span class="keyword">var</span> result = arr.filter(<span class="function"><span class="keyword">function</span>(<span class="params">element,index,self</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> arr.indexOf(element) === index;</span><br><span class="line"> &#125;)</span><br><span class="line">alert(result)<span class="comment">//1,2,3,4,5</span></span><br><span class="line"><span class="comment">//indexOf总返回第一个元素的位置，后续重复元素位置与indexOf返回不相等</span></span><br></pre></td></tr></table></figure>


<h4 id="forEach-方法：本质与for循环迭代数组一样"><a href="#forEach-方法：本质与for循环迭代数组一样" class="headerlink" title="forEach()方法：本质与for循环迭代数组一样"></a><code>forEach()</code>方法：本质与for循环迭代数组一样</h4><h3 id="归并方法"><a href="#归并方法" class="headerlink" title="归并方法"></a>归并方法</h3><p>  <code>reduce()</code>方法和<code>reduceRight()</code>方法，这两个方法都会迭代数组的所有项，构建一个最终返回的值，四个参数：前一个值、当前值、项的索引和数组对象</p>
  <figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用reduce求数组所有值的和</span></span><br><span class="line"><span class="keyword">var</span> value = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];</span><br><span class="line"><span class="keyword">var</span> sum = value.reduce(<span class="function"><span class="keyword">function</span>(<span class="params">prev,cur,index,array</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> prev + cur;</span><br><span class="line">&#125;)</span><br><span class="line">alert(sum); <span class="comment">//15</span></span><br></pre></td></tr></table></figure>


<p>  <strong>使用reduce还是reduceRight主要取决于要从哪头开始遍历数组，除此之外它们完全相同</strong></p>
]]></content>
      <categories>
        <category>前端</category>
        <category>js</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>Array</tag>
      </tags>
  </entry>
  <entry>
    <title>JS中面向对象程序设计</title>
    <url>/2019/10/29/2019-10-29-js%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="面向对象"><a href="#面向对象" class="headerlink" title="面向对象"></a>面向对象</h1><p>原型模式</p>
<p>我们所创建的每个函数都有一个<code>prototype</code>(原型)属性，这个属性是一个指针，指向一个对象，意思就是所有对象都可以共享它所包含的属性和方法</p>
<img src="/2019/10/29/2019-10-29-js面向对象/03.jpg">

<a id="more"></a>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Person.prototype.name = <span class="string">"mike"</span>;</span><br><span class="line">Person.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    alert(<span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> person1 = <span class="keyword">new</span> Person();</span><br><span class="line">preson1.sayName(); <span class="comment">//mike</span></span><br><span class="line"><span class="keyword">var</span> person2 = <span class="keyword">new</span> Person();</span><br><span class="line">person2.sayName(); <span class="comment">//mike</span></span><br><span class="line">alert(person1.sayName == person2.sayName) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h2 id="理解原型对象"><a href="#理解原型对象" class="headerlink" title="理解原型对象"></a>理解原型对象</h2><img src="/2019/10/29/2019-10-29-js面向对象/01.jpg">

<img src="/2019/10/29/2019-10-29-js面向对象/02.jpg">

<p><strong>事实上现在所展示的原型链还少一环，所有引用类型默认都继承了<code>Object</code>，而这个继承也是通过原型链实现的。因此默认原型内部还有一个指针，指向<code>Object.prototype</code>下面这个图为我们展示了完整的原型链</strong></p>
<img src="/2019/10/29/2019-10-29-js面向对象/04.png">

<p>虽然通过对象实例可以访问保存在原型中的值，但是却不能通过对象实例重写原型中的值。如果我们在实例中添加了一个属性与实例原型中的属性重名，那么会在实例中创建该属性以屏蔽原型中的那个属性</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Person.prototype.name = <span class="string">"mike"</span>;</span><br><span class="line"><span class="keyword">var</span> person1 = <span class="keyword">new</span> Person();</span><br><span class="line"><span class="keyword">var</span> person2 = <span class="keyword">new</span> Person();</span><br><span class="line">person1.name = <span class="string">"Greg"</span>;</span><br><span class="line">alert(person1.name)<span class="comment">//Greg已经修改</span></span><br><span class="line">alert(person2.name)<span class="comment">//mike 仍然访问原型数据</span></span><br><span class="line"><span class="keyword">delete</span> person1.name</span><br><span class="line">alert(person1.name)<span class="comment">//mike</span></span><br></pre></td></tr></table></figure>

<h2 id="实现b继承a的方法"><a href="#实现b继承a的方法" class="headerlink" title="实现b继承a的方法"></a>实现b继承a的方法</h2><h3 id="原型链继承"><a href="#原型链继承" class="headerlink" title="原型链继承"></a>原型链继承</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">   </span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//原型方法</span></span><br><span class="line">A.prototype.sayHi = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    alert(<span class="string">"Hello"</span>);</span><br><span class="line"> &#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"><span class="comment">//将父类的实例作为子类的原型</span></span><br><span class="line">B.prototype = <span class="keyword">new</span> A();</span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> B();</span><br><span class="line">alert(b.sayHi()); <span class="comment">//Hello</span></span><br></pre></td></tr></table></figure>

<p>缺点：不能给父类构造函数传递参数，父子构造函数的原型对象有共享数据问题</p>
<h3 id="借用构造函数继承"><a href="#借用构造函数继承" class="headerlink" title="借用构造函数继承"></a>借用构造函数继承</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">name</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">    <span class="keyword">this</span>.friends = [<span class="string">'jely'</span>,<span class="string">'bob'</span>]</span><br><span class="line">    <span class="keyword">this</span>.sayName() = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    alert(<span class="keyword">this</span>.name);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params">name</span>)</span>&#123;</span><br><span class="line">    <span class="comment">//使用call借用A的构造函数</span></span><br><span class="line">    A.call(<span class="keyword">this</span>,name);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> B(<span class="string">"Tom"</span>);</span><br><span class="line">b.sayName();<span class="comment">//Tom</span></span><br><span class="line">b.friends;<span class="comment">// jely,bob</span></span><br></pre></td></tr></table></figure>

<p>组合继承</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">           <span class="keyword">this</span>.name = name;</span><br><span class="line">           <span class="keyword">this</span>.colors = [<span class="string">"red"</span>, <span class="string">"blue"</span>, <span class="string">"green"</span>];</span><br><span class="line"></span><br><span class="line">       &#125;</span><br><span class="line">       A.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">           alert(<span class="keyword">this</span>.name);</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params">name,age</span>) </span>&#123;</span><br><span class="line">           <span class="comment">//继承属性</span></span><br><span class="line">           A.call(<span class="keyword">this</span>,name);</span><br><span class="line">           <span class="keyword">this</span>.age = age;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="comment">//继承方法</span></span><br><span class="line">       B.prototype = <span class="keyword">new</span> A();</span><br><span class="line">       <span class="comment">//修复构造函数指向</span></span><br><span class="line">       B.prototype.construnctor = B;</span><br><span class="line">       <span class="keyword">var</span> b = <span class="keyword">new</span> B(<span class="string">"Greg"</span>,<span class="number">27</span>);</span><br></pre></td></tr></table></figure>

<h3 id="原型式继承"><a href="#原型式继承" class="headerlink" title="原型式继承"></a>原型式继承</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> A = &#123;</span><br><span class="line">            name: <span class="string">"Nicholas"</span>,</span><br><span class="line">            friends: [<span class="string">"Shelby"</span>, <span class="string">"Court"</span>, <span class="string">"Van"</span>]</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">var</span> B = <span class="built_in">Object</span>.create(A);</span><br><span class="line">        B.name = <span class="string">"Greg"</span>;</span><br><span class="line">        B.friends.push(<span class="string">"Rob"</span>);</span><br><span class="line">        <span class="keyword">var</span> C = <span class="built_in">Object</span>.create(person);</span><br><span class="line">        C.name = <span class="string">"Linda"</span>;</span><br><span class="line">        C.friends.push(<span class="string">"Barbie"</span>);</span><br><span class="line"></span><br><span class="line">        alert(A.friends);      <span class="comment">//"Shelby,Court,Van,Rob,Barbie"</span></span><br></pre></td></tr></table></figure>

<h3 id="寄生式继承"><a href="#寄生式继承" class="headerlink" title="寄生式继承"></a>寄生式继承</h3><p>创建一个仅用于封装继承过程的函数，该 函数在内部以某种方式来增强对象，后再像真地是它做了所有工作一样返回对象</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//封装函数</span></span><br><span class="line">        <span class="comment">//original---&gt;原始的</span></span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">create</span>(<span class="params">original</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">var</span> clone = object(original); <span class="comment">//通过调用函数创建一个新对象</span></span><br><span class="line">            clone.sayHi = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">//以某种方式增强这个对象</span></span><br><span class="line">                alert(<span class="string">"hi"</span>)</span><br><span class="line">            &#125;;</span><br><span class="line">            <span class="keyword">return</span> clone;<span class="comment">//返回这个对象</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">var</span> A =&#123;</span><br><span class="line">            name: <span class="string">"mike"</span>,</span><br><span class="line">            friends:[<span class="string">'1'</span>,<span class="string">'2'</span>,<span class="string">'3'</span>]</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">var</span> b = create(A);</span><br><span class="line">        b.sayHi(); <span class="comment">//hi</span></span><br></pre></td></tr></table></figure>

<h3 id="寄生组合式继承"><a href="#寄生组合式继承" class="headerlink" title="寄生组合式继承"></a>寄生组合式继承</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">inherit</span>(<span class="params">b,a</span>)</span>&#123;</span><br><span class="line">   <span class="keyword">var</span> prototype = object(a.prototype)<span class="comment">//创建对象</span></span><br><span class="line">    prototype.constructor = b;</span><br><span class="line">    b.prototype = prototype;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">    <span class="keyword">this</span>.colors = [<span class="string">"red"</span>,<span class="string">"blue"</span>,<span class="string">"green"</span>];</span><br><span class="line">&#125;</span><br><span class="line">A.prototype.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    alert(<span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params">name,age</span>)</span>&#123;</span><br><span class="line">    A.call(<span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line">inherit(B,A);</span><br></pre></td></tr></table></figure>

<p>原型链的构建是通过将一个类型的实例赋值给另一个构造函 数的原型实现的。这样，子类型就能够访问超类型的所有属性和方法，这一点与基于类的继承很相似。 原型链的问题是对象实例共享所有继承的属性和方法，因此不适宜单独使用。解决这个问题的技术是借 用构造函数，即在子类型构造函数的内部调用超类型构造函数。这样就可以做到每个实例都具有自己的 属性，同时还能保证只使用构造函数模式来定义类型。使用多的继承模式是组合继承，这种模式使用 原型链继承共享的属性和方法，而通过借用构造函数继承实例属性</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>Object</tag>
      </tags>
  </entry>
  <entry>
    <title>Js函数表达式</title>
    <url>/2019/10/31/2019-10-31-%E5%87%BD%E6%95%B0%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<img src="/2019/10/31/2019-10-31-函数表达式/48.jpg">

<a id="more"></a>

<h1 id="函数表达式"><a href="#函数表达式" class="headerlink" title="函数表达式"></a>函数表达式</h1><h2 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h2><p>递归函数是在一个函数通过名字调用自身的情况下构成的</p>
<p>一个经典的递归阶乘函数</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">num</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(num &lt;= <span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> num * f(num<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>arguments.callee</code>是一个指向正在执行函数的指针，因此它可以实现对函数的递归调用</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">num</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(num &lt;= <span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> num * <span class="built_in">arguments</span>.callee(num<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>通过使用<code>arguments.calee</code>代替函数名，可以确保无论怎样调用函数都不会出问题，因此，在编写递归函数时，使用<code>agruments.callee</code>总比使用函数名更保险</strong></p>
<h2 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h2><h3 id="执行环境与作用域"><a href="#执行环境与作用域" class="headerlink" title="执行环境与作用域"></a>执行环境与作用域</h3><p>想要理解闭包就要理解执行环境与作用域:</p>
<p>每个函数都有自己的执行环境，每个执行环境都有一个与之关联的变量对象，当代码在一个环境中执行的时候，会创建变量对象的一个作用域链，如果这个环境是函数的话，则将其活动对象作为变量对象，活动对象最开始只包含一个变量，即arguments对象。</p>
<p>变量的作用域：全局变量和局部变量，js语言中，函数内部可以直接读取全局变量</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> n=<span class="number">100</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">alert(n);</span><br><span class="line">&#125;</span><br><span class="line">f1()<span class="comment">//100</span></span><br></pre></td></tr></table></figure>

<p>另一方面，在函数外部无法读取函数内的局部变量（注意这里内部声明变量要使用var，否则实际上声明 了一个全局变量）</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line"><span class="keyword">var</span> n = <span class="number">999</span>;</span><br><span class="line">&#125;</span><br><span class="line">alert(n); <span class="comment">//error</span></span><br></pre></td></tr></table></figure>

<h3 id="那么什么是闭包呢？"><a href="#那么什么是闭包呢？" class="headerlink" title="那么什么是闭包呢？"></a>那么什么是闭包呢？</h3><p>闭包是指有权访问另一个函数作用域中的变量的函数</p>
<p><strong>理解：闭包就是能够读取其它函数内部变量的函数，他是函数内部与外部连接起来的一座桥梁</strong></p>
<p>作用：</p>
<ol>
<li>从外部读取局部变量</li>
<li>让变量的始终保存在内存中</li>
</ol>
<p>那么如何从外部读取局部变量</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//那就是在函数内部在定义一个函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> n = <span class="number">100</span>;</span><br><span class="line">    <span class="comment">//局部变量n对外部不可见，对f2是可见的</span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">f2</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        alert(n); <span class="comment">//100</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//将f2作为返回值</span></span><br><span class="line">    <span class="keyword">return</span> f2;<span class="comment">//</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> result= f1();</span><br><span class="line">result();<span class="comment">//100</span></span><br></pre></td></tr></table></figure>

<p>上述代码中<code>f2</code>函数，就是闭包。</p>
<p>2.将变量的值保存在内存中</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　<span class="keyword">var</span> n=<span class="number">999</span>;</span><br><span class="line">    <span class="comment">//nAdd没有使用var声明，它是一个全局变量，值为一个匿名函数</span></span><br><span class="line">　　　　nAdd=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;n+=<span class="number">1</span>&#125;</span><br><span class="line">　　　　<span class="function"><span class="keyword">function</span> <span class="title">f2</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　　　alert(n);</span><br><span class="line">　　　　&#125;</span><br><span class="line">　　　　<span class="keyword">return</span> f2;</span><br><span class="line">　　&#125;</span><br><span class="line">　　<span class="keyword">var</span> result=f1();</span><br><span class="line">　　result(); <span class="comment">// 999</span></span><br><span class="line">　　nAdd();</span><br><span class="line">　　result(); <span class="comment">// 1000</span></span><br></pre></td></tr></table></figure>

<p>在这段代码中，result实际上就是闭包<code>f2</code>函数。它一共运行了两次，第一次的值是999，第二次的值是1000。这证明了，函数f1中的局部变量n一直保存在内存中，并没有在<code>f1</code>调用后被自动清除。</p>
<p>为什么会这样呢？原因就在于<code>f1</code>是<code>f2</code>的父函数，而<code>f2</code>被赋给了一个全局变量，这导致<code>f2</code>始终在内存中，而<code>f2</code>的存在依赖于<code>f1</code>，因此<code>f1</code>也始终在内存中，不会在调用结束后，被垃圾回收机制（garbage collection）回收。</p>
<p>这段代码中另一个值得注意的地方，就是”<code>nAdd=function(){n+=1}</code>“这一行，首先在<code>nAdd</code>前面没有使用var关键字，因此<code>nAdd</code>是一个全局变量，而不是局部变量。其次，<code>nAdd</code>的值是一个匿名函数（anonymous function），而这个匿名函数本身也是一个闭包，所以<code>nAdd</code>相当于是一个setter，可以在函数外部对函数内部的局部变量进行操作。</p>
<h3 id="关于this对象"><a href="#关于this对象" class="headerlink" title="关于this对象"></a>关于<code>this</code>对象</h3><p>我们需要理解两个例子</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">"The Window"</span>;</span><br><span class="line"></span><br><span class="line">　　<span class="keyword">var</span> object = &#123;</span><br><span class="line">　　　　name : <span class="string">"My Object"</span>,</span><br><span class="line"></span><br><span class="line">　　　　getNameFunc : <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　　　<span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　　　　　<span class="keyword">return</span> <span class="keyword">this</span>.name;</span><br><span class="line">　　　　　　&#125;;</span><br><span class="line"></span><br><span class="line">　　　　&#125;</span><br><span class="line"></span><br><span class="line">　　&#125;;</span><br><span class="line"></span><br><span class="line">　　alert(object.getNameFunc()());<span class="comment">//The window</span></span><br></pre></td></tr></table></figure>

<p>在全局函数中，this等于window,而匿名函数的执行环境具有全局性，因此this队形通常指向window</p>
<p>例2</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">"The Window"</span>;</span><br><span class="line"></span><br><span class="line">　　<span class="keyword">var</span> object = &#123;</span><br><span class="line">　　　　name : <span class="string">"My Object"</span>,</span><br><span class="line"></span><br><span class="line">　　　　getNameFunc : <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　　　<span class="keyword">var</span> that = <span class="keyword">this</span>;</span><br><span class="line">　　　　　　<span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　　　　　<span class="keyword">return</span> that.name;</span><br><span class="line">　　　　　　&#125;;</span><br><span class="line"></span><br><span class="line">　　　　&#125;</span><br><span class="line"></span><br><span class="line">　　&#125;;</span><br><span class="line"></span><br><span class="line">　　alert(object.getNameFunc()());</span><br></pre></td></tr></table></figure>

<p>在定义匿名函数之前就将this对象赋值给了that，而在定义了闭包之后，闭包可以访问这个变量，此时的that正是引用着object的this。</p>
<h3 id="闭包与变量"><a href="#闭包与变量" class="headerlink" title="闭包与变量"></a>闭包与变量</h3><p>在作用域链这种配置机制下引出的指的注意的副作用，即闭包只能取得包含函数中任何的变量的最后一个值</p>
<p>所以当返回函数引入了循环变量时</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">count</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> arr = [];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i=<span class="number">1</span>; i&lt;=<span class="number">3</span>; i++) &#123;</span><br><span class="line">        arr.push(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> i * i;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> results = count();</span><br><span class="line"><span class="keyword">var</span> f1 = results[<span class="number">0</span>];<span class="comment">//16</span></span><br><span class="line"><span class="keyword">var</span> f2 = results[<span class="number">1</span>];<span class="comment">//16</span></span><br><span class="line"><span class="keyword">var</span> f3 = results[<span class="number">2</span>];<span class="comment">//16</span></span><br></pre></td></tr></table></figure>

<p>全部都是<code>16</code>！原因就在于返回的函数引用了变量<code>i</code>，但它并非立刻执行。等到3个函数都返回时，它们所引用的变量<code>i</code>已经变成了<code>4</code>，因此最终结果为<code>16</code>。</p>
<p>解决：方法是再创建一个函数，用该函数的参数绑定循环变量当前的值，无论该循环变量后续如何更改，已绑定到函数参数的值不变：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">count</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> arr = [];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i=<span class="number">1</span>; i&lt;=<span class="number">3</span>; i++) &#123;</span><br><span class="line">        arr.push((<span class="function"><span class="keyword">function</span> (<span class="params">n</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> n * n;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;)(i));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> results = count();</span><br><span class="line"><span class="keyword">var</span> f1 = results[<span class="number">0</span>];</span><br><span class="line"><span class="keyword">var</span> f2 = results[<span class="number">1</span>];</span><br><span class="line"><span class="keyword">var</span> f3 = results[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">f1(); <span class="comment">// 1</span></span><br><span class="line">f2(); <span class="comment">// 4</span></span><br><span class="line">f3(); <span class="comment">// 9</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>闭包</tag>
        <tag>递归</tag>
      </tags>
  </entry>
  <entry>
    <title>2019-11-11-Vue-01</title>
    <url>/2019/11/11/2019-11-11-Vue-01/</url>
    <content><![CDATA[<h1 id="Vue-js是什么"><a href="#Vue-js是什么" class="headerlink" title="Vue.js是什么"></a>Vue.js是什么</h1><p><code>Vue</code>是一套用于构建用户界面的<strong>渐进式框架</strong>。与其它大型框架不同的是，<code>Vue</code> 被设计为可以自底向上逐层应用。<code>Vue</code> 的核心库只关注视图层，不仅易于上手，还便于与第三方库或既有项目整合。另一方面，当与<a href="https://cn.vuejs.org/v2/guide/single-file-components.html" target="_blank" rel="noopener">现代化的工具链</a>以及各种<a href="https://github.com/vuejs/awesome-vue#libraries--plugins" target="_blank" rel="noopener">支持类库</a>结合使用时，<code>Vue</code> 也完全能够为复杂的单页应用提供驱动。</p>
<a id="more"></a>

<h2 id="指令"><a href="#指令" class="headerlink" title="指令"></a>指令</h2><ol>
<li><p><code>v-once指令</code>:进行一次行插值，当数据改变时，插值处的内容不会更新</p>
</li>
<li><p><code>v-html指令</code>：输出<code>html</code></p>
</li>
<li><p><code>v-cloak指令</code>：可以隐藏未编译的标签直到实例准备完毕。解决插值表达式问题</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">[v-cloak]&#123;<span class="attr">display</span>: none&#125;</span><br><span class="line">&lt;div v-cloak&gt;</span><br><span class="line">    &#123;&#123; message &#125;&#125;</span><br><span class="line">&lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">/</span><span class="regexp">/不会显示直到编译结束</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>v-text指令</code>：没有需要用v-cloak解决的插值闪烁问题。并且它会覆盖元素中原本的内容，插值表达式只会替换自己的占位符</p>
</li>
<li><p><code>v-bind指令</code>:（缩写 ：）用来动态的绑定一个或多个特性（属性类似：title，class，style），或一个组件prop到表达式</p>
</li>
<li><p><code>v-on指令</code>: （简写：@）绑定事件监听器。事件类型由参数指定。表达式可以是一个方法的名字或一个内联语句，如果没有修饰符也可以省略。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;button v-on:click=<span class="string">"doThis"</span>&gt;&lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp">&lt;!-- 缩写 --&gt;</span></span><br><span class="line"><span class="regexp">&lt;button @click="doThis"&gt;&lt;/</span>button&gt;</span><br><span class="line">.stop - 调用 event.stopPropagation()。</span><br><span class="line">.prevent - 调用 event.preventDefault()。</span><br><span class="line">.capture - 添加事件侦听器时使用 capture 模式。</span><br><span class="line">.self - 只当事件是从侦听器绑定的元素本身触发时才触发回调。</span><br><span class="line">.&#123;keyCode | keyAlias&#125; - 只当事件是从特定键触发时才触发回调。</span><br><span class="line">.native - 监听组件根元素的原生事件。</span><br><span class="line">.once - 只触发一次回调。</span><br><span class="line">.left - (<span class="number">2.2</span><span class="number">.0</span>) 只当点击鼠标左键时触发。</span><br><span class="line">.right - (<span class="number">2.2</span><span class="number">.0</span>) 只当点击鼠标右键时触发。</span><br><span class="line">.middle - (<span class="number">2.2</span><span class="number">.0</span>) 只当点击鼠标中键时触发。</span><br><span class="line">.passive - (<span class="number">2.3</span><span class="number">.0</span>) 以 &#123; <span class="attr">passive</span>: <span class="literal">true</span> &#125; 模式添加侦听器</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>v-model指令</code>：数据双向绑定，仅限于表单数据。</p>
<p>输出的属性会影响元素变化</p>
</li>
<li><p><code>v-for指令</code>：对数据进行遍历</p>
</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>js作用域</title>
    <url>/2019/11/27/2019-11-27-js%E4%BD%9C%E7%94%A8%E5%9F%9F/</url>
    <content><![CDATA[<h1 id="关于函数作用域"><a href="#关于函数作用域" class="headerlink" title="关于函数作用域"></a>关于函数作用域</h1><p>几乎所有编程语言的基本功能之一就是<strong>能够存储变量当中的值</strong>，并且能在这之后进行访问和修改，那么变量存储在哪里？程序需要的时候如何找到他们。</p>
<p>事实上js也是一门编译语言，并且大部分情况下编译发生在代码执行前的几微秒（甚至更短）。</p>
<h2 id="关于编译原理"><a href="#关于编译原理" class="headerlink" title="关于编译原理"></a>关于编译原理</h2><a id="more"></a>

<p>传统编译语言的流程中，程序的一段源代码在执行之前会有三个步骤，统称为“编译”。</p>
<ol>
<li><p>分词/词法分析</p>
<p>这个过程会将由字符组成的字符串分解成（对编程语言来说）有意义的代码块，这些代 码块被称为词法单元（token）。例如，考虑程序 var a = 2;。这段程序通常会被分解成 为下面这些词法单元：var、a、=、2 、;。空格是否会被当作词法单元，取决于空格在 这门语言中是否具有意义。</p>
</li>
<li><p>解析/语法分析</p>
<p>这个过程是将词法单元流（数组）转换成一个由元素逐级嵌套所组成的代表了程序语法 结构的树。这个树被称为“抽象语法树”（Abstract Syntax Tree，AST）。</p>
</li>
<li><p>代码生成</p>
<p>将AST转换为可执行代码的过程被称为代码生成。</p>
</li>
</ol>
<h2 id="理解作用域"><a href="#理解作用域" class="headerlink" title="理解作用域"></a>理解作用域</h2><h3 id="进行工作的工作员"><a href="#进行工作的工作员" class="headerlink" title="进行工作的工作员"></a>进行工作的工作员</h3><ol>
<li><p>引擎</p>
<p>从头到尾负责整个JavaScript程序的编译及执行过程</p>
</li>
<li><p>编译器</p>
<p>负责语法分析及代码生成</p>
</li>
<li><p>作用域</p>
<p>负责收集并维护所有声明的变量组成的一系列查询，并实施非常严格的规则，确定当前执行的代码对这些标识符的访问权限</p>
</li>
</ol>
<h3 id="编译器"><a href="#编译器" class="headerlink" title="编译器"></a>编译器</h3><p>对于 var a = 2 ;</p>
<p>为变量分配内存，将其命名为a，然后将值2保存进这个变量，这句话并不完全正确</p>
<p>事实上编译器会进行如下处理：</p>
<ul>
<li>遇到 var a，编译器会询问作用域是否已经有一个该名称的变量存在于同一个作用域的 集合中。如果是，编译器会忽略该声明，继续进行编译；否则它会要求作用域在当前作 用域的集合中声明一个新的变量，并命名为 a。</li>
<li>接下来编译器会为引擎生成运行时所需的代码，这些代码被用来处理 a = 2 这个赋值 操作。引擎运行时会首先询问作用域，在当前的作用域集合中是否存在一个叫作 a 的 变量。如果是，引擎就会使用这个变量；如果否，引擎会继续查找该变量。</li>
</ul>
<p>如果引擎最终找到了 a 变量，就会将 2 赋值给它。否则引擎就会举手示意并抛出一个异 常！</p>
<h4 id="编译器的查询类型"><a href="#编译器的查询类型" class="headerlink" title="编译器的查询类型"></a>编译器的查询类型</h4><p>当变量出现在赋值操作的左侧时进行LHS查询，出现在右侧时进行RHS查询。讲得更准确一点，RHS 查询与简单地查找某个变量的值别无二致，而 LHS 查询则是试图 找到变量的容器本身，从而可以对其赋值。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(a)</span><br></pre></td></tr></table></figure>

<p>其中对a的引用是一个RHS引用，因为这里 a 并没有赋予任何值。相应地，需要查找并取 得 a 的值，这样才能将值传递给 console.log(..)。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">a = <span class="number">2</span></span><br></pre></td></tr></table></figure>

<p>这里对 a 的引用则是 LHS 引用，因为实际上我们并不关心当前的值是什么，只是想要为 = 2 这个赋值操作找到一个目标。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>TCP/IP协议</title>
    <url>/2020/02/27/2020-02-27/</url>
    <content><![CDATA[<p>TCP/IP协议 族各层的作用</p>
<ul>
<li><p>应用层：决定了用户提供应用服务时 通信的活动，比如FTP(文件传输协议)，DNS(域名系统)，HTTP</p>
</li>
<li><p>传输层：传输层对应上层应用层，提供处于网络连接中两台计算机之间的数据传输，传输层有两个性质不同的协议，TCP(传输控制协议)，UDP(用户数据报协议)</p>
<a id="more"></a>
</li>
<li><p>网络层：用来处理网络上流动的数据包，数据包是网络传输的最小数据单位。该层规定了通过怎样的路径到达对方计算机，并把数据传给对方。</p>
</li>
<li><p>链路层：用来 处理连接网络的硬件部分，包括操作系统，网卡，硬件的设备驱动等</p>
<img src="/2020/02/27/2020-02-27/1.png">

</li>
</ul>
<h3 id="确保可靠性的TCP协议"><a href="#确保可靠性的TCP协议" class="headerlink" title="确保可靠性的TCP协议"></a>确保可靠性的TCP协议</h3><p>TCP协议位于传输层，为了能够更容易的传输大数据将数据进行分割，并且要确保数据最终能否送达到对方，为列准确无误地将数据送达目标处，采用 了<strong>三次握手策略</strong></p>
<p>DNS服务是和HTTP协议一样位于应用层的协议，提供域名到ip地址之间的解析服务，由于用户通常适应字母配合数字进行记忆，但是计算机却更适合理解ip地址的一组纯数字，所以DNS服务应运而生，<strong>DNS协议通过 用户传入的域名进行查找对应的ip地址，或通过ip地址反向查找域名</strong></p>
<h3 id="URI与URL"><a href="#URI与URL" class="headerlink" title="URI与URL"></a>URI与URL</h3><p>URI(统一资源标识符)，标识某以互联网资源，而URL(统一资源定位符)，URI用字符串标识某一互联网资源，而URL表示资源的地址，可以说URL是URI的子集</p>
<p>URI：<a href="http://www.ietf.com/rfc/...txt" target="_blank" rel="noopener">http://www.ietf.com/rfc/...txt</a>  包括协议，登录信息，服务器地址，服务器端口号等</p>
<p>URL:<a href="http://ietf.com" target="_blank" rel="noopener">http://ietf.com</a></p>
]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>HTTP</tag>
        <tag>TCP/IP</tag>
      </tags>
  </entry>
  <entry>
    <title>写给自己看的flex布局</title>
    <url>/2020/02/20/2020-02-20-1/</url>
    <content><![CDATA[<h5 id="flex布局"><a href="#flex布局" class="headerlink" title="flex布局"></a>flex布局</h5><p>flex布局相关的属性分两组，一组作用在flex容器上，还有一组作用在flex子项上</p>
<table>
<thead>
<tr>
<th align="center">作用在flex容器上</th>
<th align="center">作用在flex子项上</th>
</tr>
</thead>
<tbody><tr>
<td align="center">flex-direction</td>
<td align="center">order</td>
</tr>
<tr>
<td align="center">flex-wrap</td>
<td align="center">flex-grow</td>
</tr>
<tr>
<td align="center">flex-flow</td>
<td align="center">flex-shrink</td>
</tr>
<tr>
<td align="center">justify-content</td>
<td align="center">flex-basis</td>
</tr>
<tr>
<td align="center">align-items</td>
<td align="center">flex</td>
</tr>
<tr>
<td align="center">align-content</td>
<td align="center">align-self</td>
</tr>
</tbody></table>
<p>flex有很多属性，和平时使用的css不同的是它控制的是一个容器和容器内的所有子项，通常不是针对某一个子元素，所以在最开始的时候总是习惯的想要对某一个元素进行时会纠结不清到底使用哪个属性。</p>
<p>最后认为搞清楚flex布局最重要的也就是明白控制元素的主轴</p>
<img src="/2020/02/20/2020-02-20-1/41.png">

<p>也就是使用<code>flex-direction</code>控制主轴方向，通常默认主轴水平方向，那么使用<code>justify-content</code>去控制子项在水平方向的对齐方式是便是控制子项在X轴上的对齐方式。当<code>flex-direction</code>:column;时，主轴由原来的X轴变为Y轴，那么<code>justify-content</code>控制的便是纵向的布局，而不是一味的认为控制当前页面的横向布局。</p>
<a id="more"></a>

<ol>
<li>flex-direction`：用来控制子项的整体布局方向,<strong>也就是决定了子项的主轴</strong>。</li>
</ol>
<p>   <code>flex-direction</code>: <code>row</code>(显示为行，从左向右)|<code>row-reverse</code>(从右向左)|<code>column</code>(显示为列)|<code>column-reverse</code>(与column相反)</p>
<ol start="2">
<li><p><code>flex-wrap</code>：控制子项整体单行显示或换行显示</p>
<p><code>flex-wrap</code>: <code>nowrap</code>(单行显示不换行)|<code>wrap</code>(宽度不足换行显示)|<code>wrap-reverse</code>(宽度不足换行，下面的子项去上面)</p>
</li>
<li><p><strong><code>align-items</code>flex的子项们在（侧轴）主轴垂直方向上的对齐方式</strong></p>
<p><code>stretch</code>默认值。子项拉伸</p>
<p><code>flex-start</code>:逻辑CSS属性值，与文档流相关，默认表现为容器顶部对齐</p>
<p><code>flex-end</code>:逻辑CSS属性值，与文档流方向相关。默认表现为容器底部对齐。</p>
<p><code>center</code>:表现为垂直居中对齐</p>
<p><code>baseline</code>:表现为所有flex子项都对于flex容器的基线对齐</p>
</li>
<li><p><strong><code>justify-content</code>:决定主轴水平方向子项的对齐和分布方式</strong></p>
<p><code>justify-content</code>: <code>flex-start</code>(与文档流方向相关，默认为左对齐)|<code>flex-end</code>(右对齐)|<code>center</code>(表现为居中对齐)|<code>space-between</code>(两端对齐)|<code>space-around</code>(环绕，展现为边缘的空白只有中间空白的一半)|<code>space-evenly</code>(均等，每个子项两侧空白完全相等)</p>
</li>
<li><p><code>algin-content</code>可以看成与<code>justify-content</code>是相似的但是且对立的属性，前者指明水平方向flex子项的对齐和分布方式，而<code>align-content</code>则指明垂直方向每一项flex元素的对齐和分布方式哦。<strong>如果所有flex子项只有一行，则<code>aligi-content</code>属性是没有任何效果的</strong>；</p>
<p><code>stretch</code>默认值。每一行flex子元素都等比例拉伸。例如，如果共两行flex子元素，则每一行拉伸高度是50%。</p>
<p><code>flex-start</code>逻辑CSS属性值，与文档流方向相关。默认表现为顶部堆砌。</p>
<p><code>flex-end</code>逻辑CSS属性值，与文档流方向相关。默认表现为底部堆放。</p>
<p><code>center</code>表现为整体垂直居中对齐。</p>
<p><code>space-between</code>表现为上下两行两端对齐。剩下每一行元素等分剩余空间。</p>
<p><code>space-around</code>每一行元素上下都享有独立不重叠的空白空间。</p>
<p><code>space-evenly</code>每一行元素都完全上下等分。</p>
</li>
<li><p><code>flex-flow</code>: 为上面两个的缩写，例：flex-flow: row nowrap;</p>
</li>
</ol>
<h6 id="子项上的属性"><a href="#子项上的属性" class="headerlink" title="子项上的属性"></a>子项上的属性</h6><ol>
<li><code>order</code>: order:<integer>//整数值，控制子项的位置例如要子项在最前面只需要给一个-1就可以了</integer></li>
<li><code>flex-grow:</code>//数值默认0，可以是小数最大为1，控制占据剩余空间的比例</li>
<li><code>flex-shrink</code>: 当容器空间不足时单个元素的收缩比例</li>
<li><code>flex-basis</code>定义了在分配剩余空间之前元素的默认大小，默认auto，或指定尺寸</li>
<li><strong><code>flex</code>flex-grow,flex-shrink,flex-basis的缩写定义子项占多少份</strong></li>
<li><code>align-self</code>: 控制单独某一个flex子项的垂直对齐方式，与align-item类似，不过指定单个元素</li>
</ol>
<p>将一个父盒子中的两个子盒子左右居中对齐</p>
<figure class="hljs highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#father</span>&#123;</span><br><span class="line">            <span class="attribute">width</span>: <span class="number">500px</span>;</span><br><span class="line">            <span class="attribute">height</span>: <span class="number">1000px</span>;</span><br><span class="line">            <span class="attribute">border</span>: <span class="number">1px</span> solid orange;</span><br><span class="line">            <span class="attribute">display</span>: flex;</span><br><span class="line">            <span class="attribute">justify-content</span>:space-between;</span><br><span class="line">            <span class="attribute">align-items</span>: center;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="selector-class">.son1</span>,<span class="selector-class">.son2</span>&#123;</span><br><span class="line">            <span class="attribute">width</span>: <span class="number">100px</span>;</span><br><span class="line">            <span class="attribute">height</span>: <span class="number">100px</span>;</span><br><span class="line">            <span class="attribute">background-color</span>: red;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Flex</tag>
      </tags>
  </entry>
  <entry>
    <title>passport验证token</title>
    <url>/2020/03/06/2020-03-06-passport/</url>
    <content><![CDATA[<p>1关于token：</p>
<p>为什么出现token：session需要存储空间，其次需要依赖cookie来进行传递，或者url重写的方式。但是token在服务器是不需要存储用户信息的，并且传递方式不限于cookie传递，并且也可以保存起来。</p>
<a id="more"></a>

<p>token与session之间都是为了 身份验证。session一般翻译为会话，而token更多时候翻译为令牌；</p>
<p>关于jsonwebtoken：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> jwt = <span class="built_in">require</span>(<span class="string">'jsonwebtoken'</span>);</span><br><span class="line"><span class="keyword">const</span> rule = &#123;</span><br><span class="line">    id:user.id;</span><br><span class="line">    name:user.name,</span><br><span class="line">    identity:user.identity</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//sgin('规则','加密名字'，'过期时间'，'箭头函数')</span></span><br><span class="line">jwt.sgin(rule,keys.secretOrKey,&#123; <span class="attr">exporesIn</span>:<span class="number">3600</span>&#125;,(err,token)=&gt;&#123;</span><br><span class="line">    <span class="keyword">if</span>(err)&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(err);</span><br><span class="line">        <span class="keyword">return</span> res.status(<span class="number">500</span>).json...</span><br><span class="line">    &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">        res.status(<span class="number">200</span>).json...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>前端接收，存储在localstroage中</p>
<p>passport:</p>
<p>引入passport并进行初始化</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">app.use(passport.initialize());</span><br></pre></td></tr></table></figure>

<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">const User = require(&quot;../dbModel/user&quot;);</span><br><span class="line">const Admin = require(&quot;../dbModel/admin&quot;);</span><br><span class="line">const Key = require(&quot;../secret/jwtkey&quot;).KEYORSECRET;</span><br><span class="line">const JwtStrategy = require(&apos;passport-jwt&apos;).Strategy,</span><br><span class="line">      ExtractJwt = require(&apos;passport-jwt&apos;).ExtractJwt;</span><br><span class="line">var opts = &#123;&#125;</span><br><span class="line">opts.jwtFromRequest = ExtractJwt.fromAuthHeaderAsBearerToken();//通过配置信息来生成jwt的请求，验证这个token</span><br><span class="line">opts.secretOrKey = Key;</span><br><span class="line">module.exports = passport =&gt; &#123;</span><br><span class="line">    passport.use(new JwtStrategy(opts, (jwt_payload, done) =&gt; &#123;//jwt_payload用户信息</span><br><span class="line">        User.findById(jwt_payload.id)</span><br><span class="line">                .then(user =&gt; &#123;</span><br><span class="line">                    if (user) &#123;</span><br><span class="line">                        return done(null, user);</span><br><span class="line">                    &#125; else &#123;</span><br><span class="line">                        Admin.findById(jwt_payload.id)</span><br><span class="line">                                .then(admin =&gt; &#123;</span><br><span class="line">                                    if(admin)&#123;</span><br><span class="line">                                        return done(null, admin);</span><br><span class="line">                                    &#125;else&#123;</span><br><span class="line">                                        return done(null, false);</span><br><span class="line">                                        // or you could create a new account</span><br><span class="line">                                    &#125;</span><br><span class="line">                                &#125;)</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;)</span><br><span class="line">    &#125;));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>passport-jwt: 是passport中的对于jsonwebtoken的插件</p>
<p>验证请求</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">app.post(<span class="string">'/profile'</span>, passport.authenticate(<span class="string">'jwt'</span>, &#123; <span class="attr">session</span>: <span class="literal">false</span> &#125;),<span class="function"><span class="keyword">function</span>(<span class="params">req, res</span>) </span>&#123;        res.send(req.user.profile);    </span><br><span class="line">     &#125;);</span><br></pre></td></tr></table></figure>

<p>new JwtStrategy(options, verify)</p>
]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>token</tag>
      </tags>
  </entry>
  <entry>
    <title>Github与码云同时上传</title>
    <url>/2020/03/03/2020-03-03/</url>
    <content><![CDATA[<p>今天学校通知将毕业设计的项目每天进程上传到码云，那么一直使用GitHub的我该怎么同时维护，上网找到的办法是这样的（不一定是最简单的方法）</p>
<a id="more"></a>

<p>首先我在将本地的仓库上传到码云，打开本地.git文件夹中的cofig</p>
<img src="/2020/03/03/2020-03-03/6.jpg">

<p>然后github中创建同样的仓库，在cofig中添加如下代码</p>
<img src="/2020/03/03/2020-03-03/2.jpg">

<p>之后push会将代码同时推送到GitHub与码云</p>
]]></content>
      <categories>
        <category>计算机</category>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>PPT模板</title>
    <url>/2019/03/29/3-29ppt%E6%A8%A1%E6%9D%BF%E5%88%86%E4%BA%AB/</url>
    <content><![CDATA[<h1 id="ppt模板"><a href="#ppt模板" class="headerlink" title="ppt模板"></a>ppt模板</h1><p><a href="https://pan.baidu.com/s/1tequzqrq0m7ok2BALQ2oow" target="_blank" rel="noopener">点这里哦！提取码twyn</a></p>
<img src="/2019/03/29/3-29ppt模板分享/base.jpg">

]]></content>
      <categories>
        <category>资源</category>
      </categories>
      <tags>
        <tag>resource</tag>
      </tags>
  </entry>
  <entry>
    <title>JS随记</title>
    <url>/2019/05/12/5-12/</url>
    <content><![CDATA[<h1 id="如何交换两个数"><a href="#如何交换两个数" class="headerlink" title="如何交换两个数"></a>如何交换两个数</h1><a id="more"></a>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">var  num1  =  10;</span><br><span class="line">var  num2  =  20;</span><br><span class="line">//定义变量temp</span><br><span class="line">var  temp;</span><br><span class="line">//将num1的值给temp，temp==10；</span><br><span class="line">temp  =  num1;</span><br><span class="line">//将num2的值给num1，num1==20；</span><br><span class="line">num1  =  num2;</span><br><span class="line">//将temp的值给num2，num2==10；</span><br><span class="line">num2  =  temp;</span><br><span class="line">console.log(num1, num2);</span><br></pre></td></tr></table></figure>

<h2 id="方式2，适用于数字交换"><a href="#方式2，适用于数字交换" class="headerlink" title="方式2，适用于数字交换"></a>方式2，适用于数字交换</h2><figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">var  num1=10;</span><br><span class="line">var  num2=20;</span><br><span class="line">num1=num1+num2;</span><br><span class="line">num2=num1-num2;</span><br><span class="line">num1=num1-num2;</span><br><span class="line">console.log(num1,num2);</span><br></pre></td></tr></table></figure>

<h1 id="js中的数据类型"><a href="#js中的数据类型" class="headerlink" title="js中的数据类型"></a>js中的数据类型</h1><p>原始数据类型：number,string,boolean,object,undefined,null</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">var num;</span><br><span class="line">console.log(num+10);//NaN---not an number---&gt;不是一个数字</span><br></pre></td></tr></table></figure>

<h2 id="如何获取变量的数据类型"><a href="#如何获取变量的数据类型" class="headerlink" title="如何获取变量的数据类型"></a>如何获取变量的数据类型</h2><p>typeof 变量名<br>typeof (变量名)<br>console.log(typeof num);//number</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>JS关于数组</title>
    <url>/2019/05/14/5-14/</url>
    <content><![CDATA[<p>案例</p>
<a id="more"></a>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">//判断一个年份是不是闰年</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span>  year=<span class="number">2016</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(year%<span class="number">4</span>==<span class="number">0</span>&amp;&amp;year%<span class="number">100</span>!=<span class="number">0</span>||year%<span class="number">400</span>==<span class="number">0</span>)&#123;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"闰年"</span>);</span><br><span class="line"></span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"平年"</span>);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<h1 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h1><ol>
<li><p>通过构造函数创建数组</p>
<p>语法：</p>
<p>var 数组名=new Array();</p>
<p>var arr=new Array(5);//一个数字”5”表示为长度！多个值代表数组中的数据 </p>
</li>
<li><p>通过字面量的方式创建数组</p>
<p>var 数组名=[];//空数组</p>
</li>
</ol>
<h2 id="数组拓展"><a href="#数组拓展" class="headerlink" title="数组拓展"></a>数组拓展</h2><ol>
<li><p>无论是构造函数的方式还是字面量的方式，定义的数组，如果有长度，那么默认是undefined。</p>
</li>
<li><p>数组可以通过对应的元素修改为新的值，因此、对数组的索引进行赋值会直接修改这个数组</p>
<p>例：</p>
</li>
</ol>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="string">'A'</span>,<span class="string">'B'</span>,<span class="string">'C'</span>];</span><br><span class="line">arr[<span class="number">1</span>] = <span class="number">99</span>;</span><br><span class="line">arr;<span class="comment">//arr现在变为['A','99','C'];</span></span><br></pre></td></tr></table></figure>

<h2 id="栈方法"><a href="#栈方法" class="headerlink" title="栈方法"></a>栈方法</h2><ol>
<li><p>.slice()截取Array的部分元素，然后返回一个新的Array<strong>如果不给slice()传递任何参数，它会从头到尾截取所有元素。利用这点可以很容易复制一个Array</strong></p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr= [<span class="string">'A'</span>, <span class="string">'B'</span>, <span class="string">'C'</span>, <span class="string">'D'</span>, <span class="string">'E'</span>, <span class="string">'F'</span>, <span class="string">'G'</span>],;</span><br><span class="line">arr.slice(<span class="number">0</span>, <span class="number">3</span>);</span><br><span class="line"><span class="comment">// 从索引0开始，到索引3结束，但不包括索引3: ['A', 'B', 'C']</span></span><br><span class="line">arr.slice(<span class="number">3</span>); <span class="comment">// 从索引3开始到结束: ['D', 'E', 'F', 'G']</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>push()向Array的末尾添加若干元素，pop()把Array的最后一个元素删除掉。</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line">arr.push(<span class="string">'A'</span>, <span class="string">'B'</span>); <span class="comment">// 返回Array新的长度: 4</span></span><br><span class="line">arr; <span class="comment">// [1, 2, 'A', 'B']</span></span><br><span class="line">arr.pop(); <span class="comment">// pop()返回'B'</span></span><br><span class="line">arr; <span class="comment">// [1, 2, 'A']</span></span><br><span class="line">arr.pop(); arr.pop(); arr.pop(); <span class="comment">// 连续pop 3次</span></span><br><span class="line">arr; <span class="comment">// []</span></span><br><span class="line">arr.pop(); <span class="comment">// 空数组继续pop不会报错，而是返回undefined</span></span><br><span class="line">arr; <span class="comment">// []</span></span><br></pre></td></tr></table></figure>

<h2 id="队列方法"><a href="#队列方法" class="headerlink" title="队列方法"></a>队列方法</h2><p>如果要往Array的头部添加若干元素，使用<code>unshift()</code>方法，shift()方法则把Array的第一个元素删掉。</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line">arr.unshift(<span class="string">'A'</span>, <span class="string">'B'</span>); <span class="comment">// 返回Array新的长度: 4</span></span><br><span class="line">arr; <span class="comment">// ['A', 'B', 1, 2]</span></span><br><span class="line">arr.shift(); <span class="comment">// 'A'</span></span><br><span class="line">arr; <span class="comment">// ['B', 1, 2]</span></span><br><span class="line">arr.shift(); arr.shift(); arr.shift(); <span class="comment">// 连续shift 3次</span></span><br><span class="line">arr; <span class="comment">// []</span></span><br><span class="line">arr.shift(); <span class="comment">// 空数组继续shift不会报错，而是返回undefined</span></span><br><span class="line">arr; <span class="comment">// []</span></span><br></pre></td></tr></table></figure>

<p>遍历数组</p>
<p>for循环、for…in:枚举数组或对象中所有‘索引号’或属性 for(var i in 数组名)、for…of:枚举数组中的‘元素值’ for(var i of 数组名)—–&gt;数组</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line"><span class="keyword">var</span> arr=[<span class="number">0</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>];</span><br><span class="line"><span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;arr.length;i++)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(arr[i]);</span><br><span class="line">&#125;</span><br><span class="line">    &lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line"><span class="keyword">var</span> arr=[<span class="number">0</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i <span class="keyword">in</span> arr)&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(arr[i]);</span><br><span class="line">&#125;</span><br><span class="line">    &lt;<span class="regexp">/script&gt;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">&lt;script&gt;</span></span><br><span class="line"><span class="regexp">var arr=[0,1,2,3,4,5,6];</span></span><br><span class="line"><span class="regexp">for(var i of arr)&#123;</span></span><br><span class="line"><span class="regexp">    console.log(arr[i]);</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp">    &lt;/</span>script&gt;</span><br></pre></td></tr></table></figure>

<h2 id="位置方法"><a href="#位置方法" class="headerlink" title="位置方法"></a>位置方法</h2><p><code>indexOf()</code> :搜索指定元素的位置</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">10</span>, <span class="number">20</span>, <span class="string">'30'</span>, <span class="string">'xyz'</span>];</span><br><span class="line">arr.indexOf(<span class="number">10</span>); <span class="comment">// 元素10的索引为0</span></span><br><span class="line">arr.indexOf(<span class="number">20</span>); <span class="comment">// 元素20的索引为1</span></span><br><span class="line">arr.indexOf(<span class="number">30</span>); <span class="comment">// 元素30没有找到，返回-1</span></span><br><span class="line">arr.indexOf(<span class="string">'30'</span>); <span class="comment">// 元素'30'的索引为2</span></span><br></pre></td></tr></table></figure>

<p><code>sort()</code>:由小到大，对数组进行排序，强制以字符串类型进行排序</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="string">'B'</span>, <span class="string">'C'</span>, <span class="string">'A'</span>];</span><br><span class="line">arr.sort();</span><br><span class="line">arr; <span class="comment">// ['A', 'B', 'C']</span></span><br></pre></td></tr></table></figure>

<p><code>reverse()</code>把整个<code>Array</code>的元素给掉个个，也就是反转</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="string">'one'</span>, <span class="string">'two'</span>, <span class="string">'three'</span>];</span><br><span class="line">arr.reverse(); </span><br><span class="line">arr; <span class="comment">// ['three', 'two', 'one']</span></span><br></pre></td></tr></table></figure>

<p><code>concat()</code>方法把当前的<code>Array</code>和另一个<code>Array</code>连接起来，并返回一个新的<code>Array</code>：</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="string">'A'</span>, <span class="string">'B'</span>, <span class="string">'C'</span>];</span><br><span class="line"><span class="keyword">var</span> added = arr.concat([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line">added; <span class="comment">// ['A', 'B', 'C', 1, 2, 3]</span></span><br><span class="line">arr; <span class="comment">// ['A', 'B', 'C']</span></span><br></pre></td></tr></table></figure>

<p>**note:<code>concat()</code>方法并没有修改当前<code>Array</code>，而是返回了一个新的”Array”。<code>concat()</code>方法可以接收任意个元素和<code>Array</code>，并且自动把<code>Array</code>拆开，然后全部添加到新的<code>Array</code>里</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="string">'A'</span>, <span class="string">'B'</span>, <span class="string">'C'</span>];</span><br><span class="line">arr.concat(<span class="number">1</span>, <span class="number">2</span>, [<span class="number">3</span>, <span class="number">4</span>]); <span class="comment">// ['A', 'B', 'C', 1, 2, 3, 4]</span></span><br></pre></td></tr></table></figure>

<p><code>join()</code>方法是一个非常实用的方法，它把当前<code>Array</code>的每个元素都用指定的字符串连接起来，然后返回连接后的字符串</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="string">'A'</span>, <span class="string">'B'</span>, <span class="string">'C'</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line">arr.join(<span class="string">'-'</span>); <span class="comment">// 'A-B-C-1-2-3'</span></span><br></pre></td></tr></table></figure>



</li>
</ol>
<h2 id="操作方法"><a href="#操作方法" class="headerlink" title="操作方法"></a>操作方法</h2><p><code>conncat()</code>方法可以基于当前数组中的所有项创建一个新数组。具体来水，这个放大会先创建一个当前数组的一个副本，然后将接收到的参数添加到这个副本的末尾。最后返回新构建的数组</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors =[<span class="string">"red"</span>,<span class="string">"green"</span>,<span class="string">"blue"</span>];</span><br><span class="line"><span class="keyword">var</span> colors2=colors.concat(<span class="string">"yellow"</span>,[<span class="string">"black"</span>,<span class="string">"blown"</span>]);</span><br><span class="line">alert(colors);    <span class="comment">//red,green,blue;</span></span><br><span class="line">alert(colors2);    <span class="comment">//red,green,blue,yellow,black,brown;</span></span><br></pre></td></tr></table></figure>

<p><code>splice()</code>方法</p>
<p>splice(参数1，参数2，参数3–n)</p>
<p>参数1：起始位</p>
<p>参数2：删除几个，不删除可写0；</p>
<p>参数3：添加一个或多个元素来替换掉被删除项(若没删除项则是向数组中添加元素)</p>
<p>例：a. 删除从 index 2 (“Thomas”) 开始的三个元素，并添加一个新元素 (“William”) 来替代被删除的元素</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">6</span>)</span><br><span class="line">arr[<span class="number">0</span>] = <span class="string">"George"</span></span><br><span class="line">arr[<span class="number">1</span>] = <span class="string">"John"</span></span><br><span class="line">arr[<span class="number">2</span>] = <span class="string">"Thomas"</span></span><br><span class="line">arr[<span class="number">3</span>] = <span class="string">"James"</span></span><br><span class="line">arr[<span class="number">4</span>] = <span class="string">"Adrew"</span></span><br><span class="line">arr[<span class="number">5</span>] = <span class="string">"Martin"</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.write(arr + <span class="string">"&lt;br /&gt;"</span>)</span><br><span class="line">arr.splice(<span class="number">2</span>,<span class="number">3</span>,<span class="string">"William"</span>)</span><br><span class="line"><span class="built_in">document</span>.write(arr)</span><br><span class="line"><span class="comment">//输出；</span></span><br><span class="line">George,John,Thomas,James,Adrew,Martin</span><br><span class="line">George,John,William,Martin</span><br></pre></td></tr></table></figure>

<p>创建一个新数组，并向其添加一个元素：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">6</span>)</span><br><span class="line">arr[<span class="number">0</span>] = <span class="string">"George"</span></span><br><span class="line">arr[<span class="number">1</span>] = <span class="string">"John"</span></span><br><span class="line">arr[<span class="number">2</span>] = <span class="string">"Thomas"</span></span><br><span class="line">arr[<span class="number">3</span>] = <span class="string">"James"</span></span><br><span class="line">arr[<span class="number">4</span>] = <span class="string">"Adrew"</span></span><br><span class="line">arr[<span class="number">5</span>] = <span class="string">"Martin"</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.write(arr + <span class="string">"&lt;br /&gt;"</span>)</span><br><span class="line">arr.splice(<span class="number">2</span>,<span class="number">0</span>,<span class="string">"William"</span>)</span><br><span class="line"><span class="built_in">document</span>.write(arr + <span class="string">"&lt;br /&gt;"</span>)</span><br><span class="line"><span class="comment">//输出</span></span><br></pre></td></tr></table></figure>

<p>删除位于 index 2 的元素，并添加一个新元素来替代被删除的元素:</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">6</span>)</span><br><span class="line">arr[<span class="number">0</span>] = <span class="string">"George"</span></span><br><span class="line">arr[<span class="number">1</span>] = <span class="string">"John"</span></span><br><span class="line">arr[<span class="number">2</span>] = <span class="string">"Thomas"</span></span><br><span class="line">arr[<span class="number">3</span>] = <span class="string">"James"</span></span><br><span class="line">arr[<span class="number">4</span>] = <span class="string">"Adrew"</span></span><br><span class="line">arr[<span class="number">5</span>] = <span class="string">"Martin"</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.write(arr + <span class="string">"&lt;br /&gt;"</span>)</span><br><span class="line">arr.splice(<span class="number">2</span>,<span class="number">1</span>,<span class="string">"William"</span>)</span><br><span class="line"><span class="built_in">document</span>.write(arr)</span><br><span class="line">George,John,Thomas,James,Adrew,Martin</span><br><span class="line">George,John,William,James,Adrew,Martin</span><br></pre></td></tr></table></figure>

<h2 id="迭代方法"><a href="#迭代方法" class="headerlink" title="迭代方法"></a>迭代方法</h2><p><code>ECMAScript5</code>为数组定义了五个迭代方法。每个方法都接收两个参数：要在每一项上运行的函数和运行该函数的作用域对象——影响this的值。传入这些方法专用的函数会接收三个参数：数组项的值(item)、该项在数组中的位置(index)和数组对象本身(array)。</p>
<ul>
<li><p><code>every()</code>：对数组中的每一项运行给定函数，如果该函数对每一项都返回true，则返回true</p>
</li>
<li><p><code>filter()</code>：对数组中的每一项运行给定函数，返回该汉族返回true的项组成的数组。</p>
</li>
<li><p><code>some()</code>：对数组中的每一项给定函数，如果该函数对任一一项返回true，则返回true。</p>
</li>
<li><p><code>forEach()</code>:对数组中的每一项运行给定函数，这个方法没有返回值。</p>
</li>
<li><p>``map()`:对数组中的每一项运行给定函数，返回每次函数调用的结果组成的数组。</p>
<p>以上方法都不会修改数组中的包含的值。</p>
<p>其中最为相似的是<code>every()</code>,和<code>some()</code>他们都用于查询数组中的项是否满足某个条件。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> numbers = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>];</span><br><span class="line"><span class="keyword">var</span> everyResult = numbers.every(<span class="function"><span class="keyword">function</span>(<span class="params">item,index,array</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (item &gt; <span class="number">2</span>);</span><br><span class="line">&#125;);</span><br><span class="line">alert(everyResult);   <span class="comment">//false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> someResult = number.some(<span class="function"><span class="keyword">function</span>(<span class="params">item,index,array</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span>(item &gt;<span class="number">2</span>);</span><br><span class="line">&#125;);</span><br><span class="line">alert(someResult);    <span class="comment">//true一项满足即为true</span></span><br></pre></td></tr></table></figure>

<p><code>filter()</code>函数，他利用指定函数是否在返回的数组中包含某一项。例如要返回一个所有数值大于2的数组，可以使用以下代码。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> number = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>];</span><br><span class="line"><span class="keyword">var</span> filterResult = number.filter(<span class="function"><span class="keyword">function</span>(<span class="params">item,index,array</span>)</span>&#123;</span><br><span class="line"><span class="keyword">return</span> (item &gt;<span class="number">2</span>);</span><br><span class="line">&#125;);</span><br><span class="line">alert(filterResult);    <span class="comment">//[3,4,5,4,3]</span></span><br></pre></td></tr></table></figure>

<p><code>map()</code>方法非常适合创建包含的项与另一个数组一一对应的数组</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> numbers = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>];</span><br><span class="line"><span class="keyword">var</span> mapResult = number.map(<span class="function"><span class="keyword">function</span>(<span class="params">item,index,array</span>)</span>&#123;</span><br><span class="line"><span class="keyword">return</span> item*<span class="number">2</span>;</span><br><span class="line">&#125;);</span><br><span class="line">alert(mapResult);  <span class="comment">//[2,4,6,8,10,8,6,4,2];</span></span><br></pre></td></tr></table></figure>

<p>最后一个方法是<code>forEach()</code>他只是对数组中的每一项运行传入的函数。这个方法没有返回值，本质上与使用for循环迭代数组一样。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> numbers = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>];</span><br><span class="line">number.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item,index,array</span>)</span>&#123;</span><br><span class="line"><span class="comment">//执行某些操作；</span></span><br><span class="line">&#125;)；</span><br></pre></td></tr></table></figure>

<p><code>IE9+</code>、Firefox 2+、Safari 3+、Opera 9.5+和 Chrome支持</p>
<h2 id="归并方法"><a href="#归并方法" class="headerlink" title="归并方法"></a>归并方法</h2><p><code>reduce()</code>、<code>reduceRight()</code>方法迭代数组的所有项，然后构建一个最终返回的值</p>
<p>两个方法接收四个参数：前一个值、当前值、项的索引、数组对象</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> values =[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];</span><br><span class="line"><span class="keyword">var</span> sum = values.reduce(<span class="function"><span class="keyword">function</span>(<span class="params">prev,cur,index,array</span>)</span>&#123;</span><br><span class="line"><span class="keyword">return</span> perv + cur;</span><br><span class="line">&#125;);</span><br><span class="line">alert(sum);  <span class="comment">//15</span></span><br></pre></td></tr></table></figure>

<p>第一次执行回调函数，<code>prev</code>是1，cur是2.第二次，<code>prev</code>是3(1+-2的结果)，cur是3(数组的第三项)。这个过程会持续到把数组中的每一项都访问一遍，最后返回结果。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javascript  Array</tag>
      </tags>
  </entry>
  <entry>
    <title>5-15数组案例</title>
    <url>/2019/05/15/5-15%E6%95%B0%E7%BB%84%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p>案例</p>
<a id="more"></a>

<ol>
<li><p>求数组中所有元素的和</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="number">10</span>,<span class="number">20</span>,<span class="number">30</span>,<span class="number">40</span>];</span><br><span class="line"><span class="keyword">var</span> sum=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span> <span class="keyword">in</span> arr)&#123;</span><br><span class="line">    sum+=arr[i];</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(sum);</span><br></pre></td></tr></table></figure>
</li>
<li><p>求数组中所有元素的平均值</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="number">10</span>,<span class="number">20</span>,<span class="number">30</span>];</span><br><span class="line"><span class="keyword">var</span> sum=<span class="number">0</span>,x=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i <span class="keyword">in</span> arr)&#123;</span><br><span class="line">    sum+=arr[i];</span><br><span class="line">    x=sum/arr.length;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(x);</span><br></pre></td></tr></table></figure>
</li>
<li><p>求数组中所有元素的最大值</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="number">10</span>,<span class="number">20</span>,<span class="number">30</span>];</span><br><span class="line"><span class="comment">//假设max是数组中最大的值</span></span><br><span class="line"><span class="keyword">var</span> max=arr[<span class="number">0</span>];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">1</span> <span class="keyword">in</span> arr)&#123;</span><br><span class="line"><span class="comment">//将max与数组中的值挨个比较，max较小就将对比的值赋给max</span></span><br><span class="line">    <span class="keyword">if</span>(max&lt;arr[i])&#123;</span><br><span class="line">        max=arr[i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(max);</span><br></pre></td></tr></table></figure>
</li>
<li><p>倒序输出数组</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=arr.length<span class="number">-1</span>;i&gt;=<span class="number">0</span>;i--)&#123;</span><br><span class="line"><span class="built_in">console</span>.log(arr[i]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>提示用户输入班级人数，求总成绩，平均值，最高分，最低分</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//提示用户输入人数，并转成数字类型</span></span><br><span class="line">      <span class="keyword">var</span> perCount = <span class="built_in">parseInt</span>(prompt(<span class="string">"请输入班级人数"</span>));</span><br><span class="line">      <span class="comment">//定义数组存储班级的每个人的成绩</span></span><br><span class="line">      <span class="keyword">var</span> perScores = [];</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; perCount; i++) &#123;</span><br><span class="line">          perScores[perScores.length] = <span class="built_in">parseInt</span>(prompt(<span class="string">"请输入第"</span> + (i + <span class="number">1</span>) + <span class="string">"个人"</span>));</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">//求总成绩</span></span><br><span class="line">      <span class="keyword">var</span> sum = <span class="number">0</span>,</span><br><span class="line">          avg = <span class="number">0</span>,</span><br><span class="line">          max = <span class="number">0</span>,</span><br><span class="line">          min = <span class="number">0</span>;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; perScores.length; i++) &#123;</span><br><span class="line">   </span><br><span class="line">          sum += perScores[i];</span><br><span class="line">          avg = sum / perScores.length;</span><br><span class="line">          <span class="comment">//最大值</span></span><br><span class="line">          max = perScores[<span class="number">0</span>];</span><br><span class="line">   </span><br><span class="line">          <span class="keyword">if</span> (max &lt; perScores[i]) &#123;</span><br><span class="line">              max = perScores[i];</span><br><span class="line">          &#125;</span><br><span class="line">          min=perScores[<span class="number">0</span>];</span><br><span class="line">          <span class="keyword">if</span>(min&gt;perCount[i])&#123;</span><br><span class="line">              min=perScores[i];</span><br><span class="line">          &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="built_in">console</span>.log(sum);</span><br><span class="line">      <span class="built_in">console</span>.log(avg);</span><br><span class="line">      <span class="built_in">console</span>.log(min);</span><br><span class="line">      <span class="built_in">console</span>.log(max);</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>冒泡排序</strong></p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="number">10</span>,<span class="number">2</span>,<span class="number">30</span>,<span class="number">20</span>,<span class="number">4</span>];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;arr.length<span class="number">-1</span>;i++)&#123;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> j=<span class="number">0</span>;j&lt;arr.length<span class="number">-1</span>-i;j++)&#123;</span><br><span class="line">    <span class="keyword">if</span>(arr[j]&gt;arr[j+<span class="number">1</span>])&#123;</span><br><span class="line">        <span class="keyword">var</span> temp=arr[j];</span><br><span class="line">        arr[j]=arr[j+<span class="number">1</span>];</span><br><span class="line">        arr[j+<span class="number">1</span>]=temp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(arr);</span><br></pre></td></tr></table></figure>

</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>5-16函数</title>
    <url>/2019/05/16/5-16%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>函数定义方式如下：</p>
<a id="more"></a>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">abs</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (x &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> -x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述abs()函数的定义如下：</p>
<ul>
<li>function指出这是一个函数定义；</li>
<li>abs是函数的名称；</li>
<li>(x)括号内列出函数的参数，多个参数以，分割；</li>
<li>{…}之间的代码是函数体，可以包含若干语句，甚至可以没有任何语句。</li>
</ul>
<p>函数体内部的语句在执行时，一旦执行到return时，函数就执行完毕，并将结果返回。所以，函数内部通过条件判断可以实现非常复杂的逻辑。如果没有return语句，函数执行完毕也会返回结果，只是结果为undefined。</p>
<p>例：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params">x,y</span>)</span>&#123;</span><br><span class="line"><span class="keyword">var</span> sum=x+y;<span class="comment">//没有返回值</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> result=f1(<span class="number">10</span>,<span class="number">20</span>);</span><br><span class="line"><span class="built_in">console</span>.log(result);<span class="comment">//undefined</span></span><br></pre></td></tr></table></figure>

<p>由于函数也是也是一个对象，上述定义的abs()函数实际上是一个函数对象，而函数名abs可以视为指向该函数的变量</p>
<p>第二种定义函数的方式：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> abs = <span class="function"><span class="keyword">function</span> (<span class="params">x</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (x &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> -x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这种方式下，function(x){}是一个匿名函数，它没有函数名。但是，这个函数赋值给了变量abs，所以，通过变量abs就可以调用该函数，这种方法需要在函数体末尾加一个；，表示赋值语句结束。</p>
<p>例：计算1-100之间所有数的和。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">everySum</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">1</span>;i&lt;=<span class="number">100</span>;i++)&#123;</span><br><span class="line">sum+=i;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">console</span>.log(sum);</span><br><span class="line">    &#125;</span><br><span class="line">    everySum();</span><br></pre></td></tr></table></figure>

<p>输入计算和。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">consoleSum</span>(<span class="params">s,y</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> sum = x+ y;<span class="comment">//计算和</span></span><br><span class="line">    <span class="built_in">console</span>.log(sum);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//函数调用</span></span><br><span class="line"><span class="keyword">var</span> num1=<span class="built_in">parseInt</span>(prompt(<span class="string">"请输入第一个数字"</span>));</span><br><span class="line"><span class="keyword">var</span> num2=<span class="built_in">parseInt</span>(prompt(<span class="string">"请输入第二个数字"</span>))；</span><br><span class="line">consoleSum(num1,num2);</span><br></pre></td></tr></table></figure>

<p>实参可以是值，也可以是变量。</p>
<p>计算1-n之间所有数字的和</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">egteverySum</span>(<span class="params">n</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> sum=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        sum+=i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(geteverySum(<span class="number">100</span>));</span><br></pre></td></tr></table></figure>

<p>求两个数中最大值：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getMax</span>(<span class="params">n,m</span>)</span>&#123;</span><br><span class="line"> <span class="keyword">return</span> n&gt;m?n:m;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(getMax(<span class="number">1</span>,<span class="number">3</span>));</span><br><span class="line"><span class="comment">//三个数中</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getMax</span>(<span class="params">n,m,x</span>)</span>&#123;</span><br><span class="line"> <span class="keyword">return</span> n&gt;m?(n&gt;x?n:x):(m&gt;x?m:x);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(getMax(<span class="number">1</span>,<span class="number">3</span>,<span class="number">10</span>));</span><br></pre></td></tr></table></figure>

<p>输出一组数中的最大值</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getMax</span>(<span class="params">array</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> max=array[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;=array.length;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(max&lt;array[i])&#123;</span><br><span class="line">            max=array[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(getMax([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">444</span>,<span class="number">5</span>]));</span><br></pre></td></tr></table></figure>

<p>数组反转</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">reverseArray</span>(<span class="params">arr</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> arr.reverse();</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(reverseArray([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>]));</span><br></pre></td></tr></table></figure>

<p>冒泡排序</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sortArray</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; arr.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">var</span> j = <span class="number">0</span>; j &lt; arr.length - <span class="number">1</span> - i; j++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (arr[j] &gt; arr[j + <span class="number">1</span>]) &#123;</span><br><span class="line">                        <span class="keyword">var</span> temp = arr[j + <span class="number">1</span>];</span><br><span class="line">                        arr[j + <span class="number">1</span>] = arr[j];</span><br><span class="line">                        arr[j] = temp;</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> arr;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">console</span>.log(sortArray([<span class="number">1</span>,<span class="number">2</span>,<span class="number">33</span>,<span class="number">44</span>,<span class="number">12</span>,<span class="number">32</span>,<span class="number">12</span>,<span class="number">21</span>]));</span><br></pre></td></tr></table></figure>

<p>求一个数字的阶乘</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">getJiecheng</span>(<span class="params">num</span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> result;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">1</span>;i&lt;=num;i++)&#123;</span><br><span class="line">    result*=i;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>斐波那契数列</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fb</span>(<span class="params">n</span>)</span>&#123;</span><br><span class="line">            <span class="keyword">var</span> first=<span class="number">1</span>,second=<span class="number">1</span>,result=<span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">3</span>;i&lt;=n;i++)&#123;</span><br><span class="line">                result=first+second;</span><br><span class="line">                first=second;</span><br><span class="line">                second=result;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125;</span><br><span class="line"><span class="number">2.</span></span><br><span class="line">&lt;script&gt;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">fb</span>(<span class="params">n</span>)</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(n=<span class="number">1</span>||n==<span class="number">2</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> fb(n<span class="number">-1</span>)+fb(n<span class="number">-2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    &lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<p>arguments对象伪数组，可以获得调用者传入的所有参数。也就是说，即使函数不定义任何参数，还是可以拿到参数的值。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">abs</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">arguments</span>.length === <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> x = <span class="built_in">arguments</span>[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">return</span> x &gt;= <span class="number">0</span> ? x : -x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">abs(); <span class="comment">// 0</span></span><br><span class="line">abs(<span class="number">10</span>); <span class="comment">// 10</span></span><br><span class="line">abs(<span class="number">-9</span>); <span class="comment">// 9</span></span><br><span class="line"><span class="comment">//求几个数的和可以这样</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    sum=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;=<span class="built_in">arguments</span>.length;i++)&#123;</span><br><span class="line">sum+=<span class="built_in">arguments</span>[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>6-19CSS相关问题</title>
    <url>/2020/06/19/6-19/</url>
    <content><![CDATA[<ul>
<li><p>text-indent:-999px;当做一个a链接的图片按钮时，可以用于在添加完图片时将原本的字隐藏，只显示图片作为链接按钮</p>
<img src="/2020/06/19/6-19/1.png">

<a id="more"></a>
</li>
<li><p>content用于在元素的::before和::after伪元素中插入内容。例如在自定义复选框样式时，</p>
<img src="/2020/06/19/6-19/2.png">

<p>使用label覆盖input，设置label的样式通过content设置在点击状态时显示背景图片</p>
</li>
<li><p>label标签表示用户界面中某个元素的说明。将一个 <code>&lt;label&gt;</code> 和一个 <a href="https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/input" target="_blank" rel="noopener">``</a> 元素相关联主要有这些优点：</p>
<ul>
<li>标签文本不仅与其相应的文本输入元素在视觉上相关联，程序中也是如此。 这意味着，当用户聚焦到这个表单输入元素时，屏幕阅读器可以读出标签，让使用辅助技术的用户更容易理解应输入什么数据。</li>
<li>你可以点击关联的标签来聚焦或者激活这个输入元素，就像直接点击输入元素一样。这扩大了元素的可点击区域，让包括使用触屏设备在内的用户更容易激活这个元素。</li>
</ul>
<p>将一个 <code>&lt;label&gt;</code> 和一个 <code>&lt;input&gt;</code> 元素匹配在一起，你需要给 <code>&lt;input&gt;</code> 一个 <code>id</code> 属性。而 <code>&lt;label&gt;</code> 需要一个 <code>for</code> 属性，其值和  <code>&lt;input&gt;</code> 的 <code>id</code> 一样。</p>
<p>另外，你也可以将 <code>&lt;input&gt;</code> 直接放在 <code>&lt;label&gt;</code> 里，此时则不需要 <code>for</code> 和 <code>id</code> 属性，因为关联已隐含存在</p>
</li>
<li><p>给父元素overflow：hidden；zoom：1；清除浮动。</p>
</li>
<li><p>移动端布局</p>
<ol>
<li>做移动端的两种方式，一个项目分为PC端适配和移动端布局，判断设备进行切换</li>
<li>一种PC移动端同时适配的响应式页面，适应pc页面和移动端页面，</li>
<li>@media screen and(max-width:300px)当页面宽度小于300像素时，使用此内容中的样式</li>
<li>宽度设为100%，盒子自动继承父盒子宽度</li>
</ol>
<p>轮播图</p>
<figure class="hljs highlight"><table><tr><td class="code"><pre><span class="line">&lt;div class="btnList"&gt;</span><br><span class="line">            &lt;div class="btn pre"&gt;&lt;&lt;/div&gt;</span><br><span class="line">            &lt;div class="btn next"&gt;&gt;&lt;/div&gt;</span><br><span class="line">        &lt;/div&gt;</span><br><span class="line"><span class="selector-class">.swiper</span> <span class="selector-class">.btn</span><span class="selector-class">.pre</span>&#123;<span class="attribute">width</span>: <span class="number">100px</span>;<span class="attribute">height</span>: <span class="number">60px</span>;<span class="attribute">line-height</span>: <span class="number">60px</span>;<span class="attribute">text-align</span>: center;<span class="attribute">background-color</span>: <span class="built_in">rgba</span>(<span class="number">0</span>,<span class="number">0</span>, <span class="number">0</span>, <span class="number">0.6</span>);<span class="attribute">color</span>: <span class="number">#ffffff</span>;<span class="attribute">position</span>: absolute;<span class="attribute">top</span>: <span class="built_in">calc</span>(<span class="number">50%</span> - <span class="number">30px</span>);<span class="attribute">font-size</span>: <span class="number">35px</span>;&#125;</span><br><span class="line"></span><br><span class="line">.swiper .btn.next&#123;width: 100px;height: 60px;line-height: 60px;text-align: center;background-color: rgba(0,0, 0, 0.6);color: #ffffff;position: absolute;right: 0;;top: calc(50% - 30px);font-size: 35px;&#125;</span><br></pre></td></tr></table></figure>

<figure class="hljs highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> preBtn = <span class="built_in">document</span>.querySelector(<span class="string">".swiper .pre"</span>);</span><br><span class="line">        <span class="keyword">var</span> nextBtn = <span class="built_in">document</span>.querySelector(<span class="string">".swiper .next"</span>);</span><br><span class="line">        <span class="keyword">var</span> imgListDivs = <span class="built_in">document</span>.querySelectorAll(<span class="string">".swiper .imgItems"</span>);</span><br><span class="line">        <span class="comment">// 图片数组的索引</span></span><br><span class="line">        <span class="keyword">var</span> currentImg = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 点击事件</span></span><br><span class="line">        nextBtn.onclick=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">            currentImg = currentImg + <span class="number">1</span>;</span><br><span class="line">            <span class="comment">// 点击到最后一张图片，回到起点</span></span><br><span class="line">            <span class="keyword">if</span>(currentImg&gt;=imgListDivs.length)&#123;</span><br><span class="line">                currentImg= <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 初始化，将所有的img列表的active去掉</span></span><br><span class="line">            imgListDivs.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item,i</span>)</span>&#123;</span><br><span class="line">                item.classList.remove(<span class="string">"active"</span>)</span><br><span class="line">            &#125;)</span><br><span class="line">            <span class="comment">// 点击后索引加1，active移动到下一个元素显示下一张图片</span></span><br><span class="line">            imgListDivs[currentImg].classList.add(<span class="string">"active"</span>)</span><br><span class="line">            </span><br><span class="line">        &#125;</span><br><span class="line">        preBtn.onclick=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">            currentImg = currentImg - <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">if</span>(currentImg&lt;<span class="number">0</span>)&#123;</span><br><span class="line">                currentImg= imgListDivs.length<span class="number">-1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 初始化，将所有的img列表的active去掉</span></span><br><span class="line">            imgListDivs.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item,i</span>)</span>&#123;</span><br><span class="line">                item.classList.remove(<span class="string">"active"</span>)</span><br><span class="line">            &#125;)</span><br><span class="line">            imgListDivs[currentImg].classList.add(<span class="string">"active"</span>)</span><br><span class="line">            </span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>CSS3</tag>
      </tags>
  </entry>
  <entry>
    <title>廖雪峰官方网站Git粗略总结</title>
    <url>/2019/06/04/6-4git/</url>
    <content><![CDATA[<h1 id="git学习总结"><a href="#git学习总结" class="headerlink" title="git学习总结"></a>git学习总结</h1><ol>
<li><p>初始化一个Git仓库，使用<code>git init</code>命令</p>
</li>
<li><p>添加文件到Git仓库，分两步：</p>
<p>使用命令<code>git add&lt;file&gt;</code>，可反复多次使用，添加多个文件；</p>
<p>使用命令<code>git commit -m &lt;message&gt;</code>，完成。</p>
<a id="more"></a>

</li>
</ol>
<h2 id="版本回退"><a href="#版本回退" class="headerlink" title="版本回退"></a>版本回退</h2><ol>
<li><p>使用<code>git log</code>命令查看最近到最远的修改记录，输出信息太多显得杂乱可以加上<code>--pretty=oneline</code> 参数</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">$ git log --pretty=oneline</span><br><span class="line">1094adb7b9b3807259d8cb349e7df1d4d6477073 (HEAD -&gt; master) append GPL</span><br><span class="line">e475afc93c209a690c39c13a46716e8fa000c366 add distributed</span><br><span class="line">eaadf4e385e865d25c48e7ca9c8395c3f7dfaef0 wrote a readme file</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用命令<code>git reset --hard HEAD^</code>回退到上一版本</p>
</li>
<li><p>回到未来在窗口没有被关掉时找到<code>commit id</code>，可以指定回到未来的某个版本</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">$ git reset --hard 1094a</span><br><span class="line">HEAD is now at 83b0afe append GPL</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果关闭了窗口使用<code>git reflog</code>名令查看每一次输入的命令找到曾经的<code>commit id</code></p>
</li>
</ol>
<h2 id="修改"><a href="#修改" class="headerlink" title="修改"></a>修改</h2><ol>
<li><code>git status</code>命令查看状态</li>
<li>再提交前，使用<code>git checkout --file</code>丢弃对工作区的修改<strong>–</strong>很重要</li>
<li>已经<code>git add</code>到暂存区，在<code>commit</code>之前<code>git reset HEAD &lt;file&gt;</code>可以把暂存区的修改撤销掉，重新放回工作区</li>
</ol>
<h2 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h2><p>命令<code>git rm file</code>删除文件，并且<code>commit</code></p>
<h2 id="查看"><a href="#查看" class="headerlink" title="查看"></a>查看</h2><p>查看已经存放：git ls-files</p>
<p>查看还没添加的文件：git status</p>
<h2 id="克隆"><a href="#克隆" class="headerlink" title="克隆"></a>克隆</h2><p>命令<code>git clone</code>在本地克隆一个仓库。</p>
<h2 id="分支管理"><a href="#分支管理" class="headerlink" title="分支管理"></a>分支管理</h2><h3 id="创建与合并分支"><a href="#创建与合并分支" class="headerlink" title="创建与合并分支"></a>创建与合并分支</h3><ol>
<li>查看分支：<code>git branch</code></li>
<li>创建分支：<code>git branch &lt;name&gt;</code></li>
<li>切换分支：<code>git checkout &lt;name&gt;</code></li>
<li>创建+切换分支：<code>git checkout -b &lt;name&gt;</code></li>
<li>合并某分支到当前分支：<code>git merge &lt;name&gt;</code></li>
<li>删除分支：<code>git branch -d &lt;name&gt;</code></li>
</ol>
<h3 id="解决冲突"><a href="#解决冲突" class="headerlink" title="解决冲突"></a>解决冲突</h3><p>在分支上进行修改，提交后切换到master，在master分支下又进行了修改提交，这时候进行分支合并则会产生冲突，必须手动解决冲突</p>
<ol>
<li><code>git status</code>会告诉我们冲突的文件</li>
<li><code>git log --graph</code>可以看到分支合并图</li>
</ol>
<h3 id="添加远程库"><a href="#添加远程库" class="headerlink" title="添加远程库"></a>添加远程库</h3><p>当已经在本地创建了一个Git仓库后，又想在<code>GitHub</code>上创建一个Git仓库，并且让这两个仓库惊醒远程同步.</p>
<ol>
<li><p>新建远程仓库在<code>repository name</code>填入<code>learngit</code>,其它保持默认设置，点击创建，就成功创建了一个新的Git仓库</p>
</li>
<li><p>现在在本地的<code>learngit</code>运行命令</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">$ git remote add origin git@github.com:你的github账户名/learngit.git</span><br></pre></td></tr></table></figure>
</li>
<li><p>下一步将本地库的所有内容推送到远程库上：</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">$ git push -u origin master</span><br><span class="line">Counting objects: 20, done.</span><br><span class="line">Delta compression using up to 4 threads.</span><br><span class="line">Compressing objects: 100% (15/15), done.</span><br><span class="line">Writing objects: 100% (20/20), 1.64 KiB | 560.00 KiB/s, done.</span><br><span class="line">Total 20 (delta 5), reused 0 (delta 0)</span><br><span class="line">remote: Resolving deltas: 100% (5/5), done.</span><br><span class="line">To github.com:michaelliao/learngit.git</span><br><span class="line"> * [new branch]      master -&gt; master</span><br><span class="line">Branch &apos;master&apos; set up to track remote branch &apos;master&apos; from &apos;origin&apos;</span><br></pre></td></tr></table></figure>

<p>把本地库的内容推送到远程，用<code>git push</code>命令，实际上是把当前分支master推送到远程。</p>
</li>
</ol>
<h3 id="从远程库克隆"><a href="#从远程库克隆" class="headerlink" title="从远程库克隆"></a>从远程库克隆</h3><p>找到<code>GitHub</code>中的仓库使用<code>git clone</code>克隆一个本地库</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">$ git clone git@github.com:michaelliao/gitskills.git</span><br><span class="line">Cloning into &apos;gitskills&apos;...</span><br><span class="line">remote: Counting objects: 3, done.</span><br><span class="line">remote: Total 3 (delta 0), reused 0 (delta 0), pack-reused 3</span><br><span class="line">Receiving objects: 100% (3/3), done.</span><br></pre></td></tr></table></figure>

<p>在本地要在自己想要的盘符进行克隆，如果想克隆在D盘，那么只需要在D盘下运行<code>Git bash</code></p>
]]></content>
      <categories>
        <category>计算机</category>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>linux</title>
    <url>/2019/06/05/6-5-linux-01/</url>
    <content><![CDATA[<h1 id="linux学习"><a href="#linux学习" class="headerlink" title="linux学习"></a>linux学习</h1><h2 id="学习linux的流程为："><a href="#学习linux的流程为：" class="headerlink" title="学习linux的流程为："></a>学习linux的流程为：</h2><ul>
<li>第一阶段：linux环境下的 基本操作命令，包括 文件操作命令(rm mkdir chmod chown) 编辑工具使用(vi vim) linux用户管理（useradd userdel）等</li>
<li>第二阶段：linux的各种配置（环境变量配置，网络配置，服务配置）</li>
<li>第三阶段：linux下如何搭建对应的语言开发环境(大数据，JavaEE,Python等)</li>
<li>第四阶段：能编写shell脚本，对linux服务器进行维护</li>
<li>第五阶段：能进行安全设置，防止攻击，保障服务器正常运行，能对系统调优</li>
<li>第六阶段：深入理解linux（对内核有研究），熟练掌握大型网站应用架构组成。并熟悉各个环节的部署和维护方法</li>
</ul>
<p><code>xrandr --size &lt;width&gt;x&lt;height&gt;</code>修改显示分辨率</p>
<h2 id="Linux目录结构"><a href="#Linux目录结构" class="headerlink" title="Linux目录结构"></a>Linux目录结构</h2><p><em>linux中一切皆文件</em>linux文件系统采用级层式的树状目录结构，在此结构中的最上层是根目录“/”。</p>
<ul>
<li>/bin    binary的缩写，存放的是最经常使用的命令</li>
<li>/sbin   存放系统管理员使用的系统管理程序</li>
<li>/home  存放普通用户的主目录</li>
<li>/root   系统管理员</li>
<li>/lib    系统开机所需要的最基本的动态链接共享库，其作用类似于windows例的dll文件</li>
<li><strong>/etc</strong>  所有的系统管理所需要的配置文件和子目录</li>
<li>*<em>/usr *</em>  非常重要的目录，用户的很多应用程序和文件都放在这个目录下</li>
<li>/boot  存放的是启动linux时使用的核心文件</li>
<li>/media  系统自动识别一些设备，例如光盘和U盘</li>
<li>/opt   给主机额外安装软件所摆放的目录</li>
</ul>
]]></content>
      <categories>
        <category>计算机</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOs</tag>
      </tags>
  </entry>
  <entry>
    <title>字符串总结</title>
    <url>/2019/07/22/7-22%E5%AD%97%E7%AC%A6%E4%B8%B2%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="string类型"><a href="#string类型" class="headerlink" title="string类型"></a>string类型</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>String类型是字符串的对象包装类型，可以像下面这样使用String构造函数来创建</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stringObject = <span class="keyword">new</span> string(<span class="string">"hello world"</span>);</span><br></pre></td></tr></table></figure>

<p>ECMAScript中字符串是不可改变的，也就是说一旦创建他们的值就是不可改变的，要改变某个变量保存的字符串，首先要先销毁原来的字符串，然后再用另一个包含新值的字符串填充该变量</p>
<a id="more"></a>

<h2 id="模版字符串"><a href="#模版字符串" class="headerlink" title="模版字符串"></a>模版字符串</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//可直接进行换行，解决了传统的拼接方式的繁琐</span></span><br><span class="line">$(<span class="string">'#result'</span>).append(<span class="string">`</span></span><br><span class="line"><span class="string">  There are &lt;b&gt;<span class="subst">$&#123;basket.count&#125;</span>&lt;/b&gt; items</span></span><br><span class="line"><span class="string">   in your basket, &lt;em&gt;<span class="subst">$&#123;basket.onSale&#125;</span>&lt;/em&gt;</span></span><br><span class="line"><span class="string">  are on sale!</span></span><br><span class="line"><span class="string">`</span>);</span><br></pre></td></tr></table></figure>

<h2 id="字符方法"><a href="#字符方法" class="headerlink" title="字符方法"></a>字符方法</h2><h3 id="转化方法"><a href="#转化方法" class="headerlink" title="转化方法"></a>转化方法</h3><ol>
<li>使用<code>toString()</code>基本每个值都有的方法，该方法不接受<strong>null和undefined</strong></li>
<li>String(),属于强制转换，null和undefined返回他们本身</li>
</ol>
<h3 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法"></a>常用方法</h3><h4 id="length-返回字符串长度"><a href="#length-返回字符串长度" class="headerlink" title=".length:返回字符串长度"></a><code>.length</code>:返回字符串长度</h4><h4 id="indexOf-searchString-position-返回字符串中指定文本的位置（也就是索引）"><a href="#indexOf-searchString-position-返回字符串中指定文本的位置（也就是索引）" class="headerlink" title="indexOf(searchString, ?position)返回字符串中指定文本的位置（也就是索引）"></a><code>indexOf(searchString, ?position)</code>返回字符串中指定文本的位置（也就是索引）</h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//可添加一个参数，搜索指定文本的位置，或通过第二个参数限定从第几个索引开始查找</span></span><br><span class="line"><span class="keyword">var</span> str = <span class="string">'The China full of name is China'</span></span><br><span class="line"><span class="built_in">console</span>.log(str.indexOf(<span class="string">'China'</span>)) <span class="comment">//没设置索引时显示的位于索引4的China</span></span><br><span class="line"><span class="built_in">console</span>.log(str.indexOf(<span class="string">'China'</span>, <span class="number">15</span>))<span class="comment">//索引开始的位置，第一个China被跳过</span></span><br><span class="line"><span class="comment">//当未查到文本时返回-1</span></span><br></pre></td></tr></table></figure>

<h4 id="lastIndexOf-同理，从字符串后方开始查询"><a href="#lastIndexOf-同理，从字符串后方开始查询" class="headerlink" title="lastIndexOf()同理，从字符串后方开始查询"></a><code>lastIndexOf()</code>同理，从字符串后方开始查询</h4><h4 id="search-方法检索字符串中的字符串"><a href="#search-方法检索字符串中的字符串" class="headerlink" title="search()方法检索字符串中的字符串"></a><code>search()</code>方法检索字符串中的字符串</h4><p>和<code>indexOf()</code>类似，查找指定字符串的位置，不同的是，<code>search()</code>,不接受第二个参数，而<code>indexOf()</code>不接受正则匹配模式</p>
<h4 id="提取部分字符串"><a href="#提取部分字符串" class="headerlink" title="提取部分字符串"></a>提取部分字符串</h4><p><code>slice(start, end)</code>,<code>substring(start, end)</code>,<code>substr(start, end)</code></p>
<h5 id="slice"><a href="#slice" class="headerlink" title="slice()"></a><code>slice()</code></h5><p>截取指定位置，指定个数的字符串</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">"Apple, Banana, Mango"</span>;</span><br><span class="line"><span class="keyword">var</span> res = str.slice(<span class="number">7</span>,<span class="number">13</span>);<span class="comment">//Banana,截取索引7到13部分的字符串</span></span><br><span class="line"><span class="keyword">var</span> res = str.slice(<span class="number">-13</span>,<span class="number">-7</span>);<span class="comment">//Banana,从倒数第13截取到倒数第7位</span></span><br><span class="line"><span class="comment">//当只选择一个参数时，截取从参数索引位置开始后的所有剩余部分</span></span><br><span class="line"><span class="keyword">var</span> res = str.slice(<span class="number">7</span>)；<span class="comment">//Banana, Mango</span></span><br><span class="line"><span class="comment">//参数为负时，选取倒着数索引后的所有字符串</span></span><br><span class="line"><span class="keyword">var</span> res = str.slice(<span class="number">-13</span>)；<span class="comment">//Banana, Mango</span></span><br></pre></td></tr></table></figure>

<h5 id="substring-与slice-相似，但是不接受负值的索引"><a href="#substring-与slice-相似，但是不接受负值的索引" class="headerlink" title="substring()与slice()相似，但是不接受负值的索引"></a><code>substring()</code>与<code>slice()</code>相似，但是不接受负值的索引</h5><h5 id="substr-也与slice-相似，不同之处为第二个参数不再是结束截取的索引，而是要截取的长度"><a href="#substr-也与slice-相似，不同之处为第二个参数不再是结束截取的索引，而是要截取的长度" class="headerlink" title="substr()也与slice()相似，不同之处为第二个参数不再是结束截取的索引，而是要截取的长度"></a><code>substr()</code>也与<code>slice()</code>相似，不同之处为第二个参数不再是结束截取的索引，而是要截取的长度</h5><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//	从第七位索引，截取6位字符串</span></span><br><span class="line"><span class="keyword">var</span> str = <span class="string">"Apple, Banana, Mango"</span>;</span><br><span class="line"><span class="keyword">var</span> res = str.substr(<span class="number">7</span>,<span class="number">6</span>);<span class="comment">//Banana</span></span><br></pre></td></tr></table></figure>

<h4 id="字符串的替换方法reaplace"><a href="#字符串的替换方法reaplace" class="headerlink" title="字符串的替换方法reaplace()"></a>字符串的替换方法<code>reaplace()</code></h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//replace()不会改变原有的字符串，而是返回新的字符串，替换的的文本为第一次匹配到的文本，对大小写敏感</span></span><br><span class="line">str = <span class="string">"Please visit Microsoft!"</span>;</span><br><span class="line"><span class="keyword">var</span> n = str.replace(<span class="string">"Microsoft"</span>, <span class="string">"W3School"</span>);</span><br><span class="line"><span class="built_in">console</span>.log(n);<span class="comment">//Please visit W3School</span></span><br><span class="line"><span class="comment">//如需执行大小写不敏感的替换，请使用正则表达式 /i（大小写不敏感）：</span></span><br><span class="line"><span class="keyword">var</span> n = str.replace(<span class="regexp">/MICROSOFT/i</span>, <span class="string">"W3School"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="字符串大小写转换方法"><a href="#字符串大小写转换方法" class="headerlink" title="字符串大小写转换方法"></a>字符串大小写转换方法</h4><p><code>ECMAScript</code>中涉及字符串大小写转换的方法有四个：<code>toLowerCase()</code>、<code>toLocaleLowerCase()</code>、<code>toUpperCase()</code>和<code>toLocaleUpperCase()</code>其中<code>toLowerCase()</code>和 <code>toUpperCase()</code>是两个经典的方法</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stringValue = <span class="string">"hello world"</span>;</span><br><span class="line">alert(stringValue.toLocaleUpperCase()); <span class="comment">// "HELLO WORLD";</span></span><br></pre></td></tr></table></figure>

<h4 id="concat-方法可用于代替加运算符"><a href="#concat-方法可用于代替加运算符" class="headerlink" title="concat() 方法可用于代替加运算符"></a><code>concat()</code> 方法可用于代替加运算符</h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> text = <span class="string">"Hello"</span> + <span class="string">" "</span> + <span class="string">"World!"</span>;</span><br><span class="line"><span class="keyword">var</span> text = <span class="string">"Hello"</span>.concat(<span class="string">" "</span>,<span class="string">"World!"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="trim-方法删除字符串两端的空白符"><a href="#trim-方法删除字符串两端的空白符" class="headerlink" title="trim()方法删除字符串两端的空白符"></a><code>trim()</code>方法删除字符串两端的空白符</h4><p>该方法最近常见的用法是在输入框，实现搜索匹配时，用于去除用户传值时产生的空格</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">"       Hello World!        "</span>;</span><br><span class="line">alert(str.trim());</span><br></pre></td></tr></table></figure>

<p><code>charAt()</code>返回指定位置的字符</p>
<p><code>charCodeAt()</code>返回指定位置字符的Unicode 编码</p>
<h2 id="fromCharCode-方法"><a href="#fromCharCode-方法" class="headerlink" title="fromCharCode()方法"></a><code>fromCharCode()</code>方法</h2><p>String构造函数本身还有一个静态方法，接收单一或多个字符编码，然后将他们转换成一个字符串。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">alert(<span class="built_in">String</span>.fromCharCode(<span class="number">104</span>,<span class="number">101</span>,<span class="number">108</span>,<span class="number">108</span>,<span class="number">111</span>));  <span class="comment">//"hello"</span></span><br><span class="line">这里传递的是字符串hello中每个字符的Unicode编码。</span><br></pre></td></tr></table></figure>

<h3 id="padStart-amp-padEnd"><a href="#padStart-amp-padEnd" class="headerlink" title="padStart()&amp;padEnd()"></a><code>padStart()</code>&amp;<code>padEnd()</code></h3><p>这两个方法是在ES6中新增的语法，用于为字符串提供补全方法，</p>
<p>例如：</p>
<p>格式化时间后的字符串<code>3:18</code>3分18秒</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">formateTime(seconds) &#123;</span><br><span class="line">      <span class="keyword">if</span> (seconds &lt; <span class="number">60</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> seconds.toString().padStart(<span class="number">2</span>, <span class="string">'0'</span>)</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (seconds &lt; <span class="number">3600</span>) &#123;</span><br><span class="line">        <span class="keyword">const</span> m = <span class="built_in">String</span>(<span class="built_in">parseInt</span>(seconds / <span class="number">60</span>)).padStart(<span class="number">2</span>, <span class="string">'0'</span>)</span><br><span class="line">        <span class="keyword">const</span> s = <span class="built_in">String</span>(<span class="built_in">parseInt</span>(seconds % <span class="number">60</span>)).padStart(<span class="number">2</span>, <span class="string">'0'</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;m&#125;</span>:<span class="subst">$&#123;s&#125;</span>`</span></span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">const</span> h = <span class="built_in">String</span>(<span class="built_in">parseInt</span>(seconds / <span class="number">3600</span>)).padStart(<span class="number">2</span>, <span class="string">'0'</span>)</span><br><span class="line">      <span class="keyword">const</span> m = <span class="built_in">String</span>(<span class="built_in">parseInt</span>((seconds % <span class="number">3600</span>) / <span class="number">60</span>)).padStart(<span class="number">2</span>, <span class="string">'0'</span>)</span><br><span class="line">      <span class="keyword">const</span> s = <span class="built_in">String</span>(<span class="built_in">parseInt</span>((seconds % <span class="number">3600</span>) % <span class="number">60</span>)).padStart(<span class="number">2</span>, <span class="string">'0'</span>)</span><br><span class="line">      <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;h&#125;</span>:<span class="subst">$&#123;m&#125;</span>:<span class="subst">$&#123;s&#125;</span>`</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p><code>padStart()</code>和<code>padEnd()</code>一共接受两个参数，第一个参数是字符串补全生效的最大长度，第二个参数是用来补全的字符串</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>String</tag>
      </tags>
  </entry>
  <entry>
    <title>jQuery总结一</title>
    <url>/2019/07/26/7-26jQuery%E6%80%BB%E7%BB%93%E4%B8%80/</url>
    <content><![CDATA[<h1 id="jQuery总结"><a href="#jQuery总结" class="headerlink" title="jQuery总结"></a>jQuery总结</h1><h2 id="引入jQuery的原因"><a href="#引入jQuery的原因" class="headerlink" title="引入jQuery的原因"></a>引入jQuery的原因</h2><p>在用js写代码会出现的问题</p>
<ul>
<li>window.onload事件有事件覆盖的问题，因此只能写一个事件</li>
<li><strong>浏览器兼容问题</strong></li>
<li>书写繁琐，代码量多</li>
<li>动画效果实现复杂</li>
</ul>
<p>jQuery可以解决上述问题</p>
<a id="more"></a>

<h2 id="jQuery怎么使用"><a href="#jQuery怎么使用" class="headerlink" title="jQuery怎么使用"></a>jQuery怎么使用</h2><p>jQuery就是js的一个类库，jQuery的方法原生js都可以实现，所有使用jQuery就是使用jQuery封装好的API。</p>
<p>这些API的共同特点几乎都是方法。所以在使用jQuery都是方法的调用。</p>
<h2 id="jquery的特点"><a href="#jquery的特点" class="headerlink" title="jquery的特点"></a>jquery的特点</h2><ol>
<li>隐式迭代：在调用方法的内部会为匹配到的所有元素进行循环遍历，执行相应的方法，我们不需要再进行循环，简化我们的操作，方便我们的调用</li>
<li>链式编程：<strong>原理 jQuery节点在调用api后会返回自身节点（return this）</strong></li>
</ol>
<h3 id="使用步骤"><a href="#使用步骤" class="headerlink" title="使用步骤"></a>使用步骤</h3><ol>
<li>引入js文件</li>
<li>页面加载事件</li>
<li>功能代码</li>
</ol>
<h2 id="jQuery页面加载事件"><a href="#jQuery页面加载事件" class="headerlink" title="jQuery页面加载事件"></a>jQuery页面加载事件</h2><p>DOM</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.onlad=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;;<span class="comment">//不仅要等文本加载完毕，而且要等图片也加载完毕才执行函数。</span></span><br></pre></td></tr></table></figure>

<p>jQuery的页面加载事件有以下几种</p>
<ol>
<li><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="built_in">window</span>).load(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;) /  $(<span class="built_in">window</span>).ready(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;)</span><br><span class="line"><span class="comment">//这两种方式也都需要页面所有元素都加载完毕才能触发</span></span><br></pre></td></tr></table></figure>
</li>
<li><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="built_in">document</span>).reday(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;)<span class="comment">//文档加载完毕，图片还没有加载的时候就可以触发</span></span><br></pre></td></tr></table></figure>
</li>
<li><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">上一种方法的简洁版</span><br><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;)</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="jQuery中的选择器"><a href="#jQuery中的选择器" class="headerlink" title="jQuery中的选择器"></a>jQuery中的选择器</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">id选择器   $(<span class="string">"#dev"</span>);</span><br><span class="line">标签选择器  $(<span class="string">"p"</span>);</span><br><span class="line">类选择器    $(<span class="string">".cls"</span>);</span><br><span class="line"><span class="comment">//层次选择器</span></span><br><span class="line">$(<span class="string">"#dv span"</span>);  <span class="comment">//div这个父级元素中所有的span标签</span></span><br><span class="line">$(<span class="string">"#dv&gt;span"</span>)   <span class="comment">//div这个父级元素的所有**直接子元素**span标签</span></span><br><span class="line">$(<span class="string">"#dv~span"</span>)   <span class="comment">//div这个父级元素后面的兄弟元素</span></span><br></pre></td></tr></table></figure>

<p>基本过滤选择器</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// :odd奇数选择</span></span><br><span class="line">$(<span class="string">"li:odd"</span>).css(<span class="string">"background"</span>, <span class="string">"red"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :even偶数选择</span></span><br><span class="line">$(<span class="string">"li:even"</span>).css(<span class="string">"background"</span>, <span class="string">"green"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :eq(index)索引选择器</span></span><br><span class="line">$(<span class="string">"ul li:eq(3)"</span>).css(<span class="string">"font-size"</span>, <span class="string">"30px"</span>);<span class="comment">//设置第四个li的字体</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// :lt(index)小于索引</span></span><br><span class="line">$(<span class="string">"li:lt(6)"</span>).css(<span class="string">"font-size"</span>, <span class="string">"30px"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :gt(index)大于该索引</span></span><br><span class="line">$(<span class="string">".ulList1 li:gt(7)"</span>).css(<span class="string">"font-size"</span>, <span class="string">"40px"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :first第一个</span></span><br><span class="line">$(<span class="string">".ulList li:first"</span>).css(<span class="string">"font-size"</span>, <span class="string">"40px"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :last最后一个</span></span><br><span class="line">$(<span class="string">"li:last"</span>).css(<span class="string">"font-size"</span>, <span class="string">"40px"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="筛选选择器"><a href="#筛选选择器" class="headerlink" title="筛选选择器"></a>筛选选择器</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">.prev();      该元素前一个兄弟元素</span><br><span class="line">.prevAll();   该元素前面所有兄弟元素</span><br><span class="line">.next();      该元素后一个兄弟元素</span><br><span class="line">.nextAll();   该元素后面所有兄弟元素</span><br><span class="line">.parnet();    该元素的父级元素</span><br><span class="line">.find();      后代寻找选择器（要指定参数）例：</span><br><span class="line">jqul.find(<span class="string">"li"</span>).css(<span class="string">"background"</span>, <span class="string">"pink"</span>);</span><br><span class="line">.sibling();   该元素所有兄弟元素</span><br></pre></td></tr></table></figure>

<h2 id="jQuery中的一些方法"><a href="#jQuery中的一些方法" class="headerlink" title="jQuery中的一些方法"></a>jQuery中的一些方法</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">jQuery对象.val();    <span class="comment">//表示获取元素的value属性值</span></span><br><span class="line">jQuery对象.val(<span class="string">"值"</span>);    <span class="comment">//表示设置元素的value属性值</span></span><br><span class="line"></span><br><span class="line">jQuery对象.css(<span class="string">"backgroundColor"</span>,<span class="string">"red"</span>)  设置css样式</span><br><span class="line"></span><br><span class="line">jQuery对象.text()         <span class="comment">//获取元素的文本内容</span></span><br><span class="line">jQuery对象.text(<span class="string">""</span>)       <span class="comment">//设置元素的文本内容</span></span><br><span class="line"></span><br><span class="line">jQuery对象.html()         <span class="comment">//获取元素的html内容</span></span><br><span class="line">jQuery对象.html(<span class="string">""</span>)       <span class="comment">//设置元素的html内容</span></span><br><span class="line"></span><br><span class="line">jQuery对象.mouseenter()   <span class="comment">//鼠标进入事件</span></span><br><span class="line">jQuery对象.mouseleave()   <span class="comment">//鼠标离开事件</span></span><br></pre></td></tr></table></figure>

<p>例子：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script src=<span class="string">"jquery-1.12.1.min.js"</span>&gt;&lt;<span class="regexp">/script&gt;</span></span><br><span class="line"><span class="regexp">&lt;script&gt;</span></span><br><span class="line"><span class="regexp">    /</span><span class="regexp">/页面加载事件</span></span><br><span class="line"><span class="regexp">      $(function () &#123; </span></span><br><span class="line"><span class="regexp">          $("ul&gt;li").mouseenter(function()&#123;</span></span><br><span class="line"><span class="regexp">           /</span><span class="regexp">/鼠标进入背景颜色变为红色，其他兄弟元素没有颜色</span></span><br><span class="line"><span class="regexp">              $(this).css("backgroundColor","red").siblings("li").css("backgroundColor","";</span></span><br><span class="line"><span class="regexp">          &#125;).mouseleave(function()&#123;</span></span><br><span class="line"><span class="regexp">$(this).parent().find("li").css("backgroundColor","");/</span><span class="regexp">/鼠标离开都没有颜色</span></span><br><span class="line"><span class="regexp">          &#125;).click(function()&#123;</span></span><br><span class="line"><span class="regexp">       /</span><span class="regexp">/添加点击事件鼠标点击上方变为黄色，下方变为蓝色</span></span><br><span class="line"><span class="regexp">              $(this).prevAll().css("backgroundColor","yellow").end().nextAll().css("backgroundColor","blue");</span></span><br><span class="line"><span class="regexp">          &#125;)</span></span><br><span class="line"><span class="regexp">          </span></span><br><span class="line"><span class="regexp">       &#125;)</span></span><br><span class="line"><span class="regexp">/</span><span class="regexp">/html</span></span><br><span class="line"><span class="regexp">&lt;ul&gt;</span></span><br><span class="line"><span class="regexp">        &lt;li&gt;青岛啤酒（TsingTao）&lt;/</span>li&gt;</span><br><span class="line">        &lt;li&gt;瓦伦丁（Wurenbacher）&lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">        &lt;li&gt;雪花（SNOW）&lt;/</span>li&gt;</span><br><span class="line">        &lt;li&gt;奥丁格教士（Franziskaner）&lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">        &lt;li&gt;科罗娜喜力柏龙（Paulaner）&lt;/</span>li&gt;</span><br><span class="line">        &lt;li&gt;嘉士伯Kaiserdom&lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">        &lt;li&gt;罗斯福（Rochefort）&lt;/</span>li&gt;</span><br><span class="line">        &lt;li&gt;粉象（Delirium）&lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">        &lt;li&gt;爱士堡（Eichbaum）&lt;/</span>li&gt;</span><br><span class="line">        &lt;li&gt;哈尔滨牌蓝带&lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">      &lt;/u</span>l&gt;</span><br></pre></td></tr></table></figure>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">.index();   <span class="comment">//当前元素的索引</span></span><br><span class="line">.hasClass()   <span class="comment">//判断是否应用类样式</span></span><br><span class="line">.addClass()    <span class="comment">//添加类样式</span></span><br><span class="line">.removeClass()  <span class="comment">//删除类样式</span></span><br><span class="line">.toggleClass()  <span class="comment">//切换类样式</span></span><br><span class="line"></span><br><span class="line">.show(<span class="string">"1000"</span>,<span class="string">"function"</span>)  <span class="comment">//可不加参数，意为如果隐藏则显示该元素，参数1：秒数  2：添加函数</span></span><br><span class="line">.hide(<span class="string">""</span>,<span class="string">""</span>)   <span class="comment">//同上，隐藏该元素</span></span><br><span class="line">例：</span><br><span class="line">&lt;script src=<span class="string">"jquery-1.12.1.min.js"</span>&gt;&lt;<span class="regexp">/script&gt;</span></span><br><span class="line"><span class="regexp">      &lt;script&gt;</span></span><br><span class="line"><span class="regexp">          $(function()&#123;</span></span><br><span class="line"><span class="regexp">    /</span><span class="regexp">/按钮1添加点击事件，从后开始隐藏图片，利用了回调函数</span></span><br><span class="line"><span class="regexp">            $("#btn1").click(function()&#123;</span></span><br><span class="line"><span class="regexp">          $("div&gt;img:last").hide(1000,function f1()&#123;</span></span><br><span class="line"><span class="regexp">              $(this).prev().hide(1000,f1)</span></span><br><span class="line"><span class="regexp">          &#125;);</span></span><br><span class="line"><span class="regexp">         </span></span><br><span class="line"><span class="regexp">      &#125;);</span></span><br><span class="line"><span class="regexp">    /</span><span class="regexp">/按钮2添加点击事件，从第一张图片开始出现</span></span><br><span class="line"><span class="regexp">      $("#btn2").click(function()&#123;</span></span><br><span class="line"><span class="regexp">        $("div&gt;img:first").show(1000,function f2()&#123;</span></span><br><span class="line"><span class="regexp">              $(this).next().show(1000,f2);</span></span><br><span class="line"><span class="regexp">      &#125;);</span></span><br><span class="line"><span class="regexp">    &#125;);</span></span><br><span class="line"><span class="regexp">          &#125;);</span></span><br><span class="line"><span class="regexp">&lt;body&gt;</span></span><br><span class="line"><span class="regexp">    &lt;input type="button" value="隐藏动画" id="btn1"/</span>&gt;</span><br><span class="line">&lt;input type=<span class="string">"button"</span> value=<span class="string">"显示动画"</span> id=<span class="string">"btn2"</span>/&gt;</span><br><span class="line">&lt;div&gt;</span><br><span class="line">  &lt;img src=<span class="string">"images/11.jpg"</span>/&gt;</span><br><span class="line">  &lt;img src=<span class="string">"images/22.jpg"</span>/&gt;</span><br><span class="line">  &lt;img src=<span class="string">"images/33.jpg"</span>/&gt;</span><br><span class="line">  &lt;img src=<span class="string">"images/44.jpg"</span>/&gt;</span><br><span class="line">&lt;<span class="regexp">/div&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">动画相关方法</span><br><span class="line">.slideUp();   <span class="comment">//滑入</span></span><br><span class="line">.slideDown();  <span class="comment">//滑出</span></span><br><span class="line">.slideToggle(); <span class="comment">//滑入滑出</span></span><br><span class="line"></span><br><span class="line">.fadeIn(); <span class="comment">//淡入</span></span><br><span class="line">.fadeOut(); <span class="comment">//淡出</span></span><br><span class="line">.fadeToggle();淡入淡出</span><br><span class="line"></span><br><span class="line">animate()自定义动画</span><br><span class="line">$(selector).animate(&#123;params&#125;,[speed],[easing],[callback]);</span><br><span class="line"><span class="comment">// &#123;params&#125;：要执行动画的CSS属性，带数字（必选）</span></span><br><span class="line"><span class="comment">// speed：执行动画时长（可选）</span></span><br><span class="line"><span class="comment">// easing:执行效果，默认为swing（缓动）  可以是linear（匀速）</span></span><br><span class="line"><span class="comment">// callback：动画执行完后立即执行的回调函数（可选）</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>JQuery</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>jQuery</tag>
      </tags>
  </entry>
  <entry>
    <title>jQuery总结二</title>
    <url>/2019/07/29/7-26jQuery%E6%80%BB%E7%BB%93%E4%BA%8C/</url>
    <content><![CDATA[<h2 id="jQuery节点操作"><a href="#jQuery节点操作" class="headerlink" title="jQuery节点操作"></a>jQuery节点操作</h2><h3 id="创建节点"><a href="#创建节点" class="headerlink" title="创建节点"></a>创建节点</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// $(htmlStr)</span></span><br><span class="line"><span class="comment">// htmlStr：html格式的字符串</span></span><br><span class="line">$(<span class="string">'&lt;span-这是一个span元素&lt;/span-'</span>);</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="添加节点"><a href="#添加节点" class="headerlink" title="添加节点"></a>添加节点</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">append  appendTo	在被选元素的结尾插入内容</span><br><span class="line">prepend prependTo	在被选元素的开头插入内容</span><br><span class="line">before				在被选元素之后插入内容</span><br><span class="line">after				在被选元素之前插入内容</span><br></pre></td></tr></table></figure>

<h3 id="清空节点与删除节点"><a href="#清空节点与删除节点" class="headerlink" title="清空节点与删除节点"></a>清空节点与删除节点</h3><ul>
<li>empty：清空指定节点的所有元素，自身保留(清理门户)</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="string">'div'</span>).empty(); <span class="comment">// 清空div的所有内容（推荐使用，会清除子元素上绑定的内容，源码）</span></span><br><span class="line">$(<span class="string">'div'</span>).html(<span class="string">''</span>);<span class="comment">// 使用html方法来清空元素，不推荐使用，会造成内存泄漏，绑定的事件不会被清除。</span></span><br></pre></td></tr></table></figure>

<ul>
<li>remove：相比于empty，自身也删除（自杀）</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="string">'div'</span>).remove();</span><br></pre></td></tr></table></figure>

<h3 id="克隆节点"><a href="#克隆节点" class="headerlink" title="克隆节点"></a>克隆节点</h3><ul>
<li>作用：复制匹配的元素</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 复制$(selector)所匹配到的元素（深度复制）</span></span><br><span class="line"><span class="comment">// cloneNode(true)</span></span><br><span class="line"><span class="comment">// 返回值为复制的新元素，和原来的元素没有任何关系了。即修改新元素，不会影响到原来的元素。</span></span><br><span class="line">$(selector).clone();</span><br></pre></td></tr></table></figure>

<h2 id="jQuery操作属性"><a href="#jQuery操作属性" class="headerlink" title="jQuery操作属性"></a>jQuery操作属性</h2><h3 id="attr操作"><a href="#attr操作" class="headerlink" title="attr操作"></a>attr操作</h3><ul>
<li>设置单个属性</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个参数：需要设置的属性名</span></span><br><span class="line"><span class="comment">// 第二个参数：对应的属性值</span></span><br><span class="line">$obj.attr(name, value);</span><br><span class="line"><span class="comment">// 用法举例</span></span><br><span class="line">$(<span class="string">'img'</span>).attr(<span class="string">'title'</span>,<span class="string">'哎哟，不错哦'</span>);</span><br><span class="line">$(<span class="string">'img'</span>).attr(<span class="string">'alt'</span>,<span class="string">'哎哟，不错哦'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li>设置多个属性</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 参数是一个对象，包含了需要设置的属性名和属性值</span></span><br><span class="line">$obj.attr(obj)</span><br><span class="line"><span class="comment">// 用法举例</span></span><br><span class="line">$(<span class="string">'img'</span>).attr(&#123;</span><br><span class="line">    title:<span class="string">'哎哟，不错哦'</span>,</span><br><span class="line">    alt:<span class="string">'哎哟，不错哦'</span>,</span><br><span class="line">    style:<span class="string">'opacity:.5'</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li>获取属性</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 传需要获取的属性名称，返回对应的属性值</span></span><br><span class="line">$obj.attr(name)</span><br><span class="line"><span class="comment">// 用法举例</span></span><br><span class="line"><span class="keyword">var</span> oTitle = $(<span class="string">'img'</span>).attr(<span class="string">'title'</span>);</span><br><span class="line">alert(oTitle);</span><br></pre></td></tr></table></figure>

<ul>
<li>移除属性</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 参数：需要移除的属性名，</span></span><br><span class="line">$obj.removeAttr(name);</span><br><span class="line"><span class="comment">// 用法举例</span></span><br><span class="line">$(<span class="string">'img'</span>).removeAttr(<span class="string">'title'</span>);</span><br></pre></td></tr></table></figure>

<h3 id="prop操作"><a href="#prop操作" class="headerlink" title="prop操作"></a>prop操作</h3><ul>
<li>在jQuery1.6之后，对于checked、selected、disabled这类boolean类型的属性来说，不能用attr方法，只能用prop方法。</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置属性</span></span><br><span class="line">$(<span class="string">':checked'</span>).prop(<span class="string">'checked'</span>,<span class="literal">true</span>);</span><br><span class="line"><span class="comment">// 获取属性</span></span><br><span class="line">$(<span class="string">':checked'</span>).prop(<span class="string">'checked'</span>);<span class="comment">// 返回true或者false</span></span><br></pre></td></tr></table></figure>

<h3 id="val-text-html"><a href="#val-text-html" class="headerlink" title="val()/text/()html()"></a>val()/text/()html()</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$obj.val()		获取或者设置表单元素的value属性的值</span><br><span class="line">$obj.html() 	对应innerHTML</span><br><span class="line">$obj.text()		对应innerText/textContent，处理了浏览器的兼容性</span><br></pre></td></tr></table></figure>

<h2 id="jQuery尺寸和位置操作"><a href="#jQuery尺寸和位置操作" class="headerlink" title="jQuery尺寸和位置操作"></a>jQuery尺寸和位置操作</h2><h3 id="width方法与height方法"><a href="#width方法与height方法" class="headerlink" title="width方法与height方法"></a>width方法与height方法</h3><ul>
<li>设置或者获取高度，不包括内边距、边框和外边距</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 带参数表示设置高度</span></span><br><span class="line">$(<span class="string">'img'</span>).height(<span class="number">200</span>);</span><br><span class="line"><span class="comment">// 不带参数获取高度</span></span><br><span class="line">$(<span class="string">'img'</span>).height();</span><br></pre></td></tr></table></figure>

<p>获取网页的可视区宽高</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取可视区宽度</span></span><br><span class="line">$(<span class="built_in">window</span>).width();</span><br><span class="line"><span class="comment">// 获取可视区高度</span></span><br><span class="line">$(<span class="built_in">window</span>).height();</span><br></pre></td></tr></table></figure>

<h3 id="innerWidth-innerHeight-outerWidth-outerHeight"><a href="#innerWidth-innerHeight-outerWidth-outerHeight" class="headerlink" title="innerWidth/innerHeight/outerWidth/outerHeight"></a><code>innerWidth</code>/<code>innerHeight</code>/<code>outerWidth</code>/<code>outerHeight</code></h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">innerWidth()/innerHeight()	方法返回元素的宽度/高度（包括内边距）。</span><br><span class="line">outerWidth()/outerHeight()  方法返回元素的宽度/高度（包括内边距和边框）。</span><br><span class="line">outerWidth(<span class="literal">true</span>)/outerHeight(<span class="literal">true</span>)  方法返回元素的宽度/高度（包括内边距、边框和外边距）。</span><br></pre></td></tr></table></figure>

<h3 id="scrollTop与scrollLeft"><a href="#scrollTop与scrollLeft" class="headerlink" title="scrollTop与scrollLeft"></a><code>scrollTop</code>与<code>scrollLeft</code></h3><ul>
<li>设置或者获取垂直滚动条的位置</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取页面被卷曲的高度</span></span><br><span class="line">$(<span class="built_in">window</span>).scrollTop();</span><br><span class="line"><span class="comment">// 获取页面被卷曲的宽度</span></span><br><span class="line">$(<span class="built_in">window</span>).scrollLeft();</span><br></pre></td></tr></table></figure>

<h3 id="offset方法与position方法"><a href="#offset方法与position方法" class="headerlink" title="offset方法与position方法"></a>offset方法与position方法</h3><ul>
<li>offset方法获取元素距离document的位置，position方法获取的是元素距离有定位的父元素(<code>offsetParent</code>)的位置。</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取元素距离document的位置,返回值为对象：&#123;left:100, top:100&#125;</span></span><br><span class="line">$(selector).offset();</span><br><span class="line"><span class="comment">// 获取相对于其最近的有定位的父元素的位置。</span></span><br><span class="line">$(selector).position();</span><br></pre></td></tr></table></figure>

<p>案例：固定导航栏   [17-固定导航栏<code>.html</code>]<br>案例：电梯导航     [18-电梯导航<code>.html</code>]</p>
<h2 id="jQuery事件机制"><a href="#jQuery事件机制" class="headerlink" title="jQuery事件机制"></a><code>jQuery</code>事件机制</h2><ul>
<li>JavaScript中已经学习过了事件，<code>jQuery</code>对JavaScript事件进行了封装，增加并扩展了事件处理机制。<code>jQuery</code>不仅提供了更加优雅的事件处理语法，而且极大的增强了事件的处理能力。</li>
</ul>
<h3 id="jQuery事件发展历程-了解"><a href="#jQuery事件发展历程-了解" class="headerlink" title="jQuery事件发展历程(了解)"></a><code>jQuery</code>事件发展历程(了解)</h3><p>简单事件绑定–bind事件绑定–delegate事件绑定–on事件绑定(推荐)</p>
<ul>
<li>简单事件注册</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">click(handler)			单击事件</span><br><span class="line">mouseenter(handler)		鼠标进入事件</span><br><span class="line">mouseleave(handler)		鼠标离开事件</span><br></pre></td></tr></table></figure>

<p>缺点：不能同时注册多个事件</p>
<ul>
<li>bind方式注册事件</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个参数：事件类型</span></span><br><span class="line"><span class="comment">// 第二个参数：事件处理程序</span></span><br><span class="line">$(<span class="string">'p'</span>).bind(<span class="string">'click mouseenter'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">// 事件响应方法</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>缺点：不支持动态事件绑定</p>
<ul>
<li>delegate注册委托事件</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个参数：selector，要绑定事件的元素</span></span><br><span class="line"><span class="comment">// 第二个参数：事件类型</span></span><br><span class="line"><span class="comment">// 第三个参数：事件处理函数</span></span><br><span class="line">$(<span class="string">'.parentBox'</span>).delegate(<span class="string">'p'</span>, <span class="string">'click'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">// 为 .parentBox下面的所有的p标签绑定事件</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>缺点：只能注册委托事件，因此注册时间需要记得方法太多了</p>
<ul>
<li>on注册事件</li>
</ul>
<h3 id="on注册事件-重点"><a href="#on注册事件-重点" class="headerlink" title="on注册事件(重点)"></a>on注册事件(重点)</h3><ul>
<li><code>jQuery1.7</code>之后，<code>jQuery</code>用on统一了所有事件的处理方法。</li>
<li>最现代的方式，兼容<code>zepto</code>(移动端类似<code>jQuery</code>的一个库)，强烈建议使用。</li>
</ul>
<p>on注册简单事件</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 表示给$(selector)绑定事件，并且由自己触发，不支持动态绑定。</span></span><br><span class="line">$(selector).on( <span class="string">'click'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;);</span><br></pre></td></tr></table></figure>

<p>on注册事件委托</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 表示给$(selector)绑定代理事件，当必须是它的内部元素span才能触发这个事件，支持动态绑定</span></span><br><span class="line">$(selector).on( <span class="string">'click'</span>,<span class="string">'span'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;);</span><br></pre></td></tr></table></figure>

<p>事件委托原理</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 事件委托的原理</span></span><br><span class="line"><span class="keyword">var</span> ul = <span class="built_in">document</span>.querySelector(<span class="string">'#ul'</span>);</span><br><span class="line">ul.onclick = <span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// console.log(e.target.tagName);</span></span><br><span class="line">  <span class="keyword">if</span> (e.target.tagName.toLowerCase() === <span class="string">'li'</span>) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(e.target);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>on注册事件的语法：</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个参数：events，绑定事件的名称可以是由空格分隔的多个事件（标准事件或者自定义事件）</span></span><br><span class="line"><span class="comment">// 第二个参数：selector, 执行事件的后代元素（可选），如果没有后代元素，那么事件将有自己执行。</span></span><br><span class="line"><span class="comment">// 第三个参数：data，传递给处理函数的数据，事件触发的时候通过event.data来使用（不常使用）</span></span><br><span class="line"><span class="comment">// 第四个参数：handler，事件处理函数</span></span><br><span class="line">$(selector).on(events[,selector][,data],handler);</span><br></pre></td></tr></table></figure>

<ul>
<li>通过源码查看 bind click delegate on 注册事件的区别</li>
</ul>
<h3 id="事件解绑"><a href="#事件解绑" class="headerlink" title="事件解绑"></a>事件解绑</h3><ul>
<li>unbind方式（不用）</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$(selector).unbind(); <span class="comment">// 解绑所有的事件</span></span><br><span class="line">$(selector).unbind(<span class="string">'click'</span>); <span class="comment">// 解绑指定的事件</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>undelegate</code>方式（不用）</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$( selector ).undelegate(); <span class="comment">// 解绑所有的delegate事件</span></span><br><span class="line">$( selector).undelegate( <span class="string">'click'</span> ); <span class="comment">// 解绑所有的click事件</span></span><br></pre></td></tr></table></figure>

<ul>
<li>off方式（推荐）</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 解绑匹配元素的所有事件</span></span><br><span class="line">$(selector).off();</span><br><span class="line"><span class="comment">// 解绑匹配元素的所有click事件</span></span><br><span class="line">$(selector).off(<span class="string">'click'</span>);</span><br></pre></td></tr></table></figure>

<h3 id="触发事件"><a href="#触发事件" class="headerlink" title="触发事件"></a>触发事件</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$(selector).click(); <span class="comment">// 触发 click事件</span></span><br><span class="line">$(selector).trigger(<span class="string">'click'</span>);</span><br></pre></td></tr></table></figure>

<h3 id="jQuery事件对象"><a href="#jQuery事件对象" class="headerlink" title="jQuery事件对象"></a><code>jQuery</code>事件对象</h3><p><code>jQuery</code>事件对象其实就是<code>js</code>事件对象的一个封装，处理了兼容性。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// screenX和screenY	对应屏幕最左上角的值</span></span><br><span class="line"><span class="comment">// clientX和clientY	距离页面左上角的位置（忽视滚动条）</span></span><br><span class="line"><span class="comment">// pageX和pageY	距离页面最顶部的左上角的位置（会计算滚动条的距离）</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// event.keyCode	按下的键盘代码</span></span><br><span class="line"><span class="comment">// event.data	存储绑定事件时传递的附加数据</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// event.stopPropagation()	阻止事件冒泡行为</span></span><br><span class="line"><span class="comment">// event.preventDefault()	阻止浏览器默认行为</span></span><br><span class="line"><span class="comment">// return false:既能阻止事件冒泡，又能阻止浏览器默认行为。</span></span><br></pre></td></tr></table></figure>

<h3 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h3><ul>
<li>按键变色 [19-按键变色<code>.html</code>]</li>
</ul>
<h2 id="jQuery补充知识点"><a href="#jQuery补充知识点" class="headerlink" title="jQuery补充知识点"></a><code>jQuery</code>补充知识点</h2><h3 id="链式编程"><a href="#链式编程" class="headerlink" title="链式编程"></a>链式编程</h3><ul>
<li>通常情况下，只有设置操作才能把链式编程延续下去。因为获取操作的时候，会返回获取到的相应的值，无法返回 <code>jQuery</code>对象。</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">end(); <span class="comment">// 筛选选择器会改变jQuery对象的DOM对象，想要回复到上一次的状态，并且返回匹配元素之前的状态。</span></span><br></pre></td></tr></table></figure>

<h3 id="each方法"><a href="#each方法" class="headerlink" title="each方法"></a>each方法</h3><ul>
<li><code>jQuery</code>的隐式迭代会对所有的DOM对象设置相同的值，但是如果我们需要给每一个对象设置不同的值的时候，就需要自己进行迭代了。</li>
</ul>
<p>作用：遍历<code>jQuery</code>对象集合，为每个匹配的元素执行一个函数</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 参数一表示当前元素在所有匹配元素中的索引号</span></span><br><span class="line"><span class="comment">// 参数二表示当前元素（DOM对象）</span></span><br><span class="line">$(selector).each(<span class="function"><span class="keyword">function</span>(<span class="params">index,element</span>)</span>&#123;&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="多库共存"><a href="#多库共存" class="headerlink" title="多库共存"></a>多库共存</h3><ul>
<li><code>jQuery</code>使用$作为标示符，但是如果与其他框架中的$冲突时，<code>jQuery</code>可以释放$符的控制权.</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> c = $.noConflict();<span class="comment">// 释放$的控制权,并且把$的能力给了c</span></span><br></pre></td></tr></table></figure>

<h2 id="插件"><a href="#插件" class="headerlink" title="插件"></a>插件</h2><h3 id="常用插件"><a href="#常用插件" class="headerlink" title="常用插件"></a>常用插件</h3><ul>
<li>弹出层插件 layer<ul>
<li><a href="https://github.com/sentsin/layer" target="_blank" rel="noopener">layer插件</a></li>
</ul>
</li>
<li>放大镜插件<ul>
<li><a href="http://www.jacklmoore.com/zoom/" target="_blank" rel="noopener">jQuery.zoom</a></li>
</ul>
</li>
<li>轮播图插件<ul>
<li><a href="http://sorgalla.com/jcarousel/" target="_blank" rel="noopener">http://sorgalla.com/jcarousel/</a></li>
<li><a href="https://github.com/OwlCarousel2/OwlCarousel2" target="_blank" rel="noopener">https://github.com/OwlCarousel2/OwlCarousel2</a></li>
</ul>
</li>
<li>图片懒加载插件<ul>
<li><a href="https://github.com/tuupola/jquery_lazyload" target="_blank" rel="noopener">jQuery.lazyload</a></li>
</ul>
</li>
</ul>
<h3 id="自己探索插件"><a href="#自己探索插件" class="headerlink" title="自己探索插件"></a>自己探索插件</h3><ul>
<li><a href="https://github.com/aui/artDialog" target="_blank" rel="noopener">artDialog</a></li>
<li><a href="https://github.com/fat/zoom.js" target="_blank" rel="noopener">图片放大</a></li>
<li><a href="http://www.github.com" target="_blank" rel="noopener">github上搜索</a></li>
</ul>
<h2 id="jQuery插件开发"><a href="#jQuery插件开发" class="headerlink" title="jQuery插件开发"></a><code>jQuery</code>插件开发</h2><ul>
<li>给<code>jQuery</code>增加方法的两种方式</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$.method = fn		静态方法</span><br><span class="line">$.fn.method = fn	实例方法</span><br></pre></td></tr></table></figure>

<ul>
<li>增加一个静态方法，实现两个数的和，插件</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">$</span>) </span>&#123;</span><br><span class="line">  $.add = <span class="function"><span class="keyword">function</span> (<span class="params">a, b</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;(jQuery))</span><br><span class="line"></span><br><span class="line">$.add(<span class="number">5</span>, <span class="number">6</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li>tab栏插件 [21-tab栏插件.html]</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">$</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// &#123;tabMenu: '#aa'&#125;</span></span><br><span class="line">  $.tab = <span class="function"><span class="keyword">function</span> (<span class="params">options</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 默认参数</span></span><br><span class="line">    <span class="keyword">var</span> defaults = &#123;</span><br><span class="line">      tabMenu: <span class="string">'#tab'</span>,</span><br><span class="line">      activeClass: <span class="string">'active'</span>,</span><br><span class="line">      tabMain: <span class="string">'#tab-main'</span>,</span><br><span class="line">      tabMainSub: <span class="string">'.main'</span>,</span><br><span class="line">      selectedClass: <span class="string">'selected'</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 把options中的属性，把对应属性的值赋给defaults对应的属性</span></span><br><span class="line">    <span class="comment">// defaults.tabMenu = options.tabMenu || defaults.tabMenu;</span></span><br><span class="line">    <span class="comment">// for(var key in options) &#123;</span></span><br><span class="line">    <span class="comment">//   defaults[key] = options[key];</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    $.extend(defaults, options);</span><br><span class="line"></span><br><span class="line">    $(defaults.tabMenu).on(<span class="string">'click'</span>, <span class="string">'li'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      $(<span class="keyword">this</span>)</span><br><span class="line">        .addClass(defaults.activeClass)</span><br><span class="line">        .siblings()</span><br><span class="line">        .removeClass(defaults.activeClass);</span><br><span class="line"></span><br><span class="line">      <span class="comment">//</span></span><br><span class="line">      <span class="keyword">var</span> index = $(<span class="keyword">this</span>).index();</span><br><span class="line">      <span class="comment">//</span></span><br><span class="line">      $(defaults.tabMain + <span class="string">' '</span> + defaults.tabMainSub)</span><br><span class="line">        .eq(index)</span><br><span class="line">        .addClass(defaults.selectedClass)</span><br><span class="line">        .siblings()</span><br><span class="line">        .removeClass(defaults.selectedClass);</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;(<span class="built_in">window</span>.jQuery))</span><br></pre></td></tr></table></figure>

<ul>
<li>表格插件  [22-表格插件<code>.html</code>]</li>
</ul>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">$</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 内部的变量，外部无法访问，防止变量名冲突</span></span><br><span class="line">  <span class="keyword">var</span> a = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// 给$增加了一个实例方法</span></span><br><span class="line">  $.fn.table = <span class="function"><span class="keyword">function</span> (<span class="params">header, data</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> array = [];</span><br><span class="line">    array.push(<span class="string">'&lt;table&gt;'</span>);</span><br><span class="line">    array.push(<span class="string">'&lt;tr&gt;'</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 生成表头</span></span><br><span class="line">    $.each(header, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      array.push(<span class="string">'&lt;th&gt;'</span> + <span class="keyword">this</span> + <span class="string">'&lt;/th&gt;'</span>);</span><br><span class="line">    &#125;)</span><br><span class="line">    array.push(<span class="string">'&lt;/tr&gt;'</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 生成数据行</span></span><br><span class="line">    $.each(data, <span class="function"><span class="keyword">function</span> (<span class="params">index</span>) </span>&#123;</span><br><span class="line">      <span class="comment">// this是当前遍历到的数组中的每一个对象</span></span><br><span class="line">      <span class="comment">// 拼数据行</span></span><br><span class="line">      array.push(<span class="string">'&lt;tr&gt;'</span>);</span><br><span class="line">      array.push(<span class="string">'&lt;td&gt;'</span> + (index + <span class="number">1</span>) + <span class="string">'&lt;/td&gt;'</span>);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 遍历对象，拼表格</span></span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">var</span> key <span class="keyword">in</span> <span class="keyword">this</span>) &#123;</span><br><span class="line">        array.push(<span class="string">'&lt;td&gt;'</span> + <span class="keyword">this</span>[key] + <span class="string">'&lt;/td&gt;'</span>);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      array.push(<span class="string">'&lt;/tr&gt;'</span>);</span><br><span class="line">    &#125;)</span><br><span class="line">    array.push(<span class="string">'&lt;/table&gt;'</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.append(array.join(<span class="string">''</span>));</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;(<span class="built_in">window</span>.jQuery))</span><br></pre></td></tr></table></figure>

<ul>
<li>插件开发的原理</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
        <category>JQuery</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>jQuery</tag>
      </tags>
  </entry>
  <entry>
    <title>7-2周记</title>
    <url>/2020/07/02/7-2%E5%91%A8%E8%AE%B0/</url>
    <content><![CDATA[<h3 id="层叠"><a href="#层叠" class="headerlink" title="层叠"></a>层叠</h3><p>对统一元素应用多个规则（样式）时，可能会包含冲突的声明，类似我们通过标签选择器，id选择器，类选择器分别为h1，指定不同字体，那么这个h1最终会采用哪种字体呢。</p>
<p>层叠就这一系列的规则，它决定了当不同选择器为元素设置样式时如何解决冲突。</p>
<a id="more"></a>

<p>层叠会依据三种条件来解决冲突，</p>
<ol>
<li>样式表的来源：样式是从哪里来的，包含我们自己写的样式和浏览器的样式等。</li>
<li>选择器的优先级：在多种选择器中，哪种选择器更重要。</li>
<li>源码的顺序：样式在样式表中出现的顺序。</li>
</ol>
<p>浏览器的默认样式被称作用户代理样式，也就是说让我们不设置样式时，浏览器会自动给元素添加一些样式，它的优先级很低，当我们设置样式时，会覆盖掉浏览器样式。</p>
<p>同时浏览器将优先级分为两部分：HTML的行内样式和选择器样式。如果使用HTML的style属性写样式，会覆盖掉任何来自样式表的样式</p>
<p>选择器的优先级</p>
<ul>
<li>id选择器的优先级要大于类选择器，那么当我们使用id选择器为h1设置颜色为红色，类选择器设置为黑色，浏览器就会将h1设置为红色，因为id选择器的优先级要高</li>
<li>如果选择器的id选择器更多，比如h1设置了三个id选择器，那么使用两个id选择器的样式会胜出</li>
<li>如果id选择器的数量一致，那么拥有更多类选择器的样式会胜出。</li>
</ul>
<p><strong>如果我们在CSS中写了一个声明，但是没有生效，一般就是被更高的优先级规则覆盖了，所以在大型项目中，通常让优先级尽可能低，这样当我们需要覆盖一些样式时才有更多的选择空间</strong></p>
<p>处理层叠的一些经验</p>
<ol>
<li>尽量不使用ID选择器，就算只使用一个ID也会大幅提高有心计，当需要覆盖这个优先级时，通常找不到另一个有意义的ID</li>
<li>尽量不适用！imporant，它比ID更难覆盖。</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>层叠</tag>
      </tags>
  </entry>
  <entry>
    <title>7-30记elementUi</title>
    <url>/2020/07/30/7-30%E8%AE%B0elementUi/</url>
    <content><![CDATA[<h1 id="项目中关于ElementUi的记录"><a href="#项目中关于ElementUi的记录" class="headerlink" title="项目中关于ElementUi的记录"></a>项目中关于ElementUi的记录</h1><h2 id="Pagination分页"><a href="#Pagination分页" class="headerlink" title="Pagination分页"></a>Pagination分页</h2><figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">el-pagination</span></span></span><br><span class="line"><span class="tag">  <span class="attr">:page-size</span>=<span class="string">"20"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">:pager-count</span>=<span class="string">"11"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">layout</span>=<span class="string">"prev, pager, next"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">:total</span>=<span class="string">"total"</span></span></span><br><span class="line"><span class="tag">   @<span class="attr">curtent-change</span>=<span class="string">"getpageUser"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">el-pagination</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>后端会获取数据的总条数和page页码</p>
<a id="more"></a>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">getPageUser(Page)&#123;</span><br><span class="line">    userlist(&#123;Page&#125;).then(<span class="function"><span class="params">data</span> =&gt;</span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.list = data.userlist</span><br><span class="line">        <span class="keyword">this</span>.total = data.total</span><br><span class="line">        <span class="keyword">this</span>.lsitloading = <span class="literal">false</span></span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>没有接口时可以在data中先写上假数据以便测试</p>
<h2 id="日期选择器"><a href="#日期选择器" class="headerlink" title="日期选择器"></a>日期选择器</h2><p>设置开始时间和结束时间的默认值为最近一周</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"block"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"demonstration"</span>&gt;</span>默认<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">el-date-picker</span></span></span><br><span class="line"><span class="tag">      <span class="attr">v-model</span>=<span class="string">"value1"</span></span></span><br><span class="line"><span class="tag">      <span class="attr">type</span>=<span class="string">"daterange"</span></span></span><br><span class="line"><span class="tag">      <span class="attr">range-separator</span>=<span class="string">"至"</span></span></span><br><span class="line"><span class="tag">      <span class="attr">start-placeholder</span>=<span class="string">"开始日期"</span></span></span><br><span class="line"><span class="tag">      <span class="attr">end-placeholder</span>=<span class="string">"结束日期"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">el-date-picker</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> now = <span class="keyword">new</span> <span class="built_in">Date</span>()</span><br><span class="line"><span class="keyword">const</span> endDate = parseTime(now, <span class="string">'&#123;y&#125;-&#123;m&#125;-&#123;d&#125;'</span>)</span><br><span class="line"><span class="keyword">const</span> lastWeek = parseTime((<span class="keyword">new</span> <span class="built_in">Date</span>() - <span class="number">3600</span> * <span class="number">1000</span> * <span class="number">24</span> * <span class="number">7</span>),<span class="string">'&#123;y&#125;-&#123;m&#125;-&#123;d&#125;'</span>)</span><br><span class="line"><span class="comment">//value1可以接收数组，当开始日期和结束日期都需要设置是传入个数组</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span>&#123;</span><br><span class="line">    data()&#123;</span><br><span class="line">        value:[lastWeek,endDate]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>使用过滤器格式化时间</strong></p>
<h2 id="form表单"><a href="#form表单" class="headerlink" title="form表单"></a>form表单</h2><figure class="hljs highlight html"><figcaption><span>+ javascrit</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">el-form-item</span> <span class="attr">prop</span>=<span class="string">"account"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"svg-container"</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">svg-icon</span> <span class="attr">icon-class</span>=<span class="string">"user"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">el-input</span> <span class="attr">ref</span>=<span class="string">"account"</span> <span class="attr">v-model</span>=<span class="string">"loginForm.account"</span> <span class="attr">:maxlength</span>=<span class="string">"12"</span><span class="attr">name</span>=<span class="string">"account"</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">placeholder</span>=<span class="string">"用户名"</span> <span class="attr">autocomplete</span>=<span class="string">"on"</span> /&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">el-form-item</span>&gt;</span></span><br><span class="line"></span><br><span class="line">      <span class="tag">&lt;<span class="name">el-form-item</span> <span class="attr">prop</span>=<span class="string">"password"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"svg-container"</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">svg-icon</span> <span class="attr">icon-class</span>=<span class="string">"password"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">el-input</span> <span class="attr">ref</span>=<span class="string">"password"</span> <span class="attr">:key</span>=<span class="string">"passwordType"</span> <span class="attr">v-model</span>=<span class="string">"loginForm.password"</span> <span class="attr">:maxlength</span>=<span class="string">"16"</span> <span class="attr">:type</span>=<span class="string">"passwordType"</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">placeholder</span>=<span class="string">"密码"</span> <span class="attr">autocomplete</span>=<span class="string">"on"</span> @<span class="attr">keyup.enter.native</span>=<span class="string">"handleLogin"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"show-pwd"</span> @<span class="attr">click</span>=<span class="string">"showPwd"</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">svg-icon</span> <span class="attr">:icon-class</span>=<span class="string">"passwordType=='password'?'eye':'eye-open'"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">el-form-item</span>&gt;</span></span><br><span class="line"></span><br><span class="line">      <span class="tag">&lt;<span class="name">el-button</span> <span class="attr">:loading</span>=<span class="string">"loading"</span> <span class="attr">type</span>=<span class="string">"primary"</span> <span class="attr">style</span>=<span class="string">"width:100%"</span> @<span class="attr">click.native.prevent</span>=<span class="string">"handleLogin"</span>&gt;</span>登录<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">el-form</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">  <span class="keyword">export</span> <span class="keyword">default</span> &#123;</span></span><br><span class="line">    data() &#123;</span><br><span class="line"><span class="javascript">        <span class="keyword">const</span> validateAccount = <span class="function">(<span class="params">rule, value, callback</span>) =&gt;</span> &#123;</span></span><br><span class="line">      if (!validAccount(value)) &#123;</span><br><span class="line"><span class="javascript">        callback(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'用户名应为3~16位字符'</span>))</span></span><br><span class="line"><span class="actionscript">      &#125; <span class="keyword">else</span> &#123;</span></span><br><span class="line">        callback()</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="javascript">    <span class="keyword">const</span> validatePassword = <span class="function">(<span class="params">rule, value, callback</span>) =&gt;</span> &#123;</span></span><br><span class="line">      if (!validPassword(value)) &#123;</span><br><span class="line"><span class="javascript">        callback(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'密码应为6~16位字符'</span>))</span></span><br><span class="line"><span class="actionscript">      &#125; <span class="keyword">else</span> &#123;</span></span><br><span class="line">        callback()</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="actionscript">      <span class="keyword">return</span> &#123;</span></span><br><span class="line"><span class="actionscript">        labelPosition: <span class="string">'right'</span>,</span></span><br><span class="line">         formLabelAlign: &#123;</span><br><span class="line"><span class="actionscript">          name: <span class="string">''</span>,</span></span><br><span class="line"><span class="actionscript">          region: <span class="string">''</span>,</span></span><br><span class="line"><span class="actionscript">          type: <span class="string">''</span></span></span><br><span class="line">        &#125;,</span><br><span class="line"><span class="actionscript">          <span class="comment">//trigger:'blur'当失去焦点时触发</span></span></span><br><span class="line">      loginRules: &#123;</span><br><span class="line"><span class="actionscript">        account: [&#123; required: <span class="literal">true</span>, trigger: <span class="string">'blur'</span>, validator: validateAccount &#125;],</span></span><br><span class="line"><span class="actionscript">        password: [&#123; required: <span class="literal">true</span>, trigger: <span class="string">'blur'</span>, validator: validatePassword &#125;]</span></span><br><span class="line">      &#125;,</span><br><span class="line">      &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ol>
<li>表单中 :model=”form”—&gt;表单数据对像</li>
<li>需要传入表单data和rule验证规则</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
        <category>Vue-elementUi</category>
      </categories>
      <tags>
        <tag>框架</tag>
        <tag>组件库</tag>
      </tags>
  </entry>
  <entry>
    <title>7-31记</title>
    <url>/2020/07/31/7-31%E8%AE%B0/</url>
    <content><![CDATA[<p>模板字符串</p>
<p>数据库中返回的是纯数字模式的时间，例如下午四点，数据库中存储的为16，下午六点，数据库中储存的为18，</p>
<p>要求是我们需要将16展示为16：00-16：59的时间段</p>
<p>在elementUi的表格中</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">el-table-column</span> <span class="attr">label</span>=<span class="string">"姓名"</span> <span class="attr">width</span>=<span class="string">"180"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">template</span> <span class="attr">slot-scope</span>=<span class="string">"&#123;row&#125;"</span>&gt;</span>&#123;&#123; `$(row.hour):00 - $(row.hour):59` &#125;&#125;<span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br><span class="line"> <span class="tag">&lt;/<span class="name">el-table-column</span>&gt;</span></span><br></pre></td></tr></table></figure>

<a id="more"></a>

<ol>
<li><p>解构赋值</p>
<p>对象的解构赋值</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123;foo, bar&#125; = &#123;<span class="attr">foo</span>: <span class="string">'aaa'</span>, <span class="attr">bar</span>: <span class="string">'bbb'</span>&#125;</span><br><span class="line">foo <span class="comment">// 'aaa'</span></span><br><span class="line">bar <span class="comment">// 'bbb'</span></span><br></pre></td></tr></table></figure>

<p>对像的属性没有次序，变量必须与属性同名，才能正确的值</p>
<ol start="3">
<li>JSON.stringify({x:5,y:6}) //{‘x’:’5’,”y”,’6’}</li>
<li>JSON.parse(json)//解析json转为js值或对象</li>
</ol>
</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>组件库</tag>
        <tag>JS</tag>
      </tags>
  </entry>
  <entry>
    <title>7-7记</title>
    <url>/2020/07/07/7-7%E8%AE%B0/</url>
    <content><![CDATA[<h3 id="rem布局"><a href="#rem布局" class="headerlink" title="rem布局"></a>rem布局</h3><p>新建rem.js文件创建自定义函数</p>
<figure class="hljs highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">resize</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> baseFontSize = <span class="number">100</span>  <span class="comment">//设计稿100px相当于1rem</span></span><br><span class="line">        <span class="keyword">var</span> designWidth = <span class="number">750</span>;<span class="comment">//设计稿宽度</span></span><br><span class="line">        <span class="keyword">var</span> width = <span class="built_in">window</span>.innerWidth;<span class="comment">//屏幕宽度</span></span><br><span class="line">        <span class="keyword">var</span> currentFontSize = (width/designWidth) * <span class="number">100</span>;</span><br><span class="line">        <span class="built_in">document</span>.querySelector(<span class="string">'html'</span>).style.fontSize = currentFontSize + <span class="string">'px'</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="built_in">window</span>.onresize = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        resize()</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">document</span>.addEventListener(<span class="string">'DOMContentLoaded'</span>,resize)</span><br><span class="line">&#125;())</span><br></pre></td></tr></table></figure>

<h3 id="关于mockjs的使用"><a href="#关于mockjs的使用" class="headerlink" title="关于mockjs的使用"></a>关于mockjs的使用</h3><p>当我们没有得到从后端传递来的数据时，可以在了解数据的类型之后，通过Mockjs在前端进行模拟数据的获取。</p>
<p>首先安装Mockjs</p>
<p>在api文件夹下新建数据文件夹（ServerData）在文件夹下创建数据文件position.js</p>
<p>在mockjs文件中首先导入Mockjs</p>
<p>导入postion文件</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">Mock.mock(&apos;/api/posi&apos;,position)</span><br></pre></td></tr></table></figure>

<p>此时我们获取数据的接口为</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">export const getPosiData = ()=&gt;&#123;</span><br><span class="line">return axios.request(&#123;</span><br><span class="line"> url:&apos;posi&apos;,</span><br><span class="line"> method:&apos;get&apos;</span><br><span class="line">&#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>rem</category>
      </categories>
      <tags>
        <tag>模拟数据</tag>
      </tags>
  </entry>
  <entry>
    <title>记this的指向</title>
    <url>/2020/08/01/8-1%E8%AE%B0this%E7%9A%84%E6%8C%87%E5%90%91/</url>
    <content><![CDATA[<h2 id="this的指向"><a href="#this的指向" class="headerlink" title="this的指向"></a>this的指向</h2><p>this的指向在函数的定义的时候是确定不了的，只有函数执行的时候才能确定this到底指向谁，所以this的最终指向的是调用它的对象（理论上）</p>
<p>第一个例子</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">app</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> user = <span class="string">"bpp"</span>; </span><br><span class="line">    <span class="built_in">console</span>.log(user);<span class="comment">//undefined</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>);<span class="comment">//window</span></span><br><span class="line">&#125;</span><br><span class="line">app()在全局作用域<span class="built_in">window</span>中调用app()</span><br></pre></td></tr></table></figure>

<p>此时this最终指向的是调用它的对象window，相当于<code>window.app()</code></p>
<a id="more"></a>

<p>第二个例子</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> temp = &#123;</span><br><span class="line">    user: <span class="string">'name'</span>,</span><br><span class="line">    fn:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.user);<span class="comment">//name</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">temp.fn()</span><br></pre></td></tr></table></figure>

<p>此时因为是对象temp调用了fn，所以this指向它的调用者temp</p>
<p>例子三证明了我们的this指向它的调用者是一个理论上的说法</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> temp = &#123;</span><br><span class="line">    user: <span class="string">'name'</span>,</span><br><span class="line">    fn:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.user);<span class="comment">//name</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">window</span>.temp.fn()</span><br></pre></td></tr></table></figure>

<p><strong>通过window调用发现this没有指向window</strong></p>
<p>例子四</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">var temp = &#123;</span><br><span class="line">user:&apos;yy&apos;</span><br><span class="line">temp2：&#123;</span><br><span class="line"> user: &apos;name&apos;,</span><br><span class="line">    fn:function()&#123;</span><br><span class="line">        console.log(this.user);//name</span><br><span class="line">  &#125;</span><br><span class="line"> &#125;  </span><br><span class="line">&#125;</span><br><span class="line">temp.temp2.fn()</span><br></pre></td></tr></table></figure>

<p>这里的temp2是通过temp点出来的，this同样没有指向它说明</p>
<ol>
<li>如果一个函数中有this没有通过上一级对象调用，那么它指向window</li>
<li>如果有上一级对象调用，那么this指向它的上一级对象</li>
<li>如果这个函数被多个对象嵌套，那么尽管这个函数被最外层的对象调用，this也指向它的上一级对象。</li>
</ol>
<p><strong>特殊情况</strong></p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> temp1 = &#123;</span><br><span class="line">    b:&#123;</span><br><span class="line">        a = <span class="number">10</span>,</span><br><span class="line">        fn:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.a);<span class="comment">//undefined</span></span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>);<span class="comment">//window</span></span><br><span class="line">  &#125;</span><br><span class="line">   &#125;</span><br><span class="line">  </span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> j = temp1.b.fn;</span><br><span class="line">j()</span><br></pre></td></tr></table></figure>

<p>此时的this没有指向最后调用它的b，因为在fn赋值给变量j的时候函数并没有运行，最终指向的是window</p>
<p>构造函数的this</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.user= <span class="string">"aaa"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> fn();</span><br><span class="line"><span class="built_in">console</span>.log(a.user);<span class="comment">//aaa</span></span><br></pre></td></tr></table></figure>

<p>这里之所以对象a可以点出函数Fn里面的user是因为new关键字可以改变this的指向，将这个this指向对象a，为什么我说a是对象，因为用了new关键字就是创建一个对象实例，理解这句话可以想想我们的例子3，我们这里用变量a创建了一个Fn的实例（相当于复制了一份Fn到对象a里面），此时仅仅只是创建，并没有执行，而调用这个函数Fn的是对象a，那么this指向的自然是对象a，那么为什么对象a中会有user，因为你已经复制了一份Fn函数到对象a中，用了new关键字就等同于复制了一份。</p>
<p>　　除了上面的这些以外，我们还可以自行改变this的指向，关于自行改变this的指向请看<a href="http://www.cnblogs.com/pssp/p/5215621.html" target="_blank" rel="noopener">JavaScript中call,apply,bind方法的总结</a>这篇文章，详细的说明了我们如何手动更改this的指向。</p>
<p>当this碰到return时</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.uer=<span class="string">"aaa"</span>;</span><br><span class="line">    <span class="keyword">return</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> fn();</span><br><span class="line"><span class="built_in">console</span>.log(a.user); <span class="comment">//undefined</span></span><br></pre></td></tr></table></figure>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>)  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">this</span>.user = <span class="string">'aaaa'</span>;  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> fn;  </span><br><span class="line"><span class="built_in">console</span>.log(a.user); <span class="comment">//aaaa</span></span><br></pre></td></tr></table></figure>

<p><strong>如果返回值是一个对象，那么this指向的就是那个返回的对象，如果返回值不是一个对象那么this还是指向函数的实例。</strong></p>
<h2 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h2><p>在严格模式中默认的this不再时window，而是undefined</p>
<p>new操作符会改变this的指向问题</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.user= <span class="string">"aaa"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> fn();</span><br><span class="line"><span class="built_in">console</span>.log(a.user);<span class="comment">//aaa</span></span><br></pre></td></tr></table></figure>

<p>为什么this会指向a？首先new关键字会创建一个空的对象，然后会自动调用一个函数（比如apply）方法，将this指向这个空对象，这样的化函数内部的this就会被这个空的对象替代</p>
<h2 id="Vue中的this"><a href="#Vue中的this" class="headerlink" title="Vue中的this"></a>Vue中的this</h2><p>我们可以看到在vue的组件中，我们调用了某个ui组件，使用的使用都会用到this，并且在获取数据的时候也会用到this</p>
<img src="/2020/08/01/8-1记this的指向/1.png">

<p>获取用户列表数据</p>
<img src="/2020/08/01/8-1记this的指向/2.png">

<p>使用elementui框架的弹框组件</p>
<p>Vue中的this指向当前组件的实例</p>
<p>使用箭头函数没有自己的this，所以指向组件当前的实例</p>
]]></content>
  </entry>
  <entry>
    <title>bootstrap</title>
    <url>/2019/08/27/8-27bootstrap/</url>
    <content><![CDATA[<h1 id="Bootstrap"><a href="#Bootstrap" class="headerlink" title="Bootstrap"></a>Bootstrap</h1><h2 id="Bootstrap-是最受欢迎的-HTML、CSS-和-JS-框架，用于开发响应式布局、移动设备优先的-WEB-项目。"><a href="#Bootstrap-是最受欢迎的-HTML、CSS-和-JS-框架，用于开发响应式布局、移动设备优先的-WEB-项目。" class="headerlink" title="Bootstrap 是最受欢迎的 HTML、CSS 和 JS 框架，用于开发响应式布局、移动设备优先的 WEB 项目。"></a>Bootstrap 是最受欢迎的 HTML、CSS 和 JS 框架，用于开发响应式布局、移动设备优先的 WEB 项目。</h2><a id="more"></a>

<p><strong>Bootstrap插件 全部依赖JQuery，因此JQuery必须在Bootstrap之前引入，这是一个基本模板</strong></p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"zh-CN"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"utf-8"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"X-UA-Compatible"</span> <span class="attr">content</span>=<span class="string">"IE=edge"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width, initial-scale=1"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Bootstrap 101 Template<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- Bootstrap --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">link</span> <span class="attr">href</span>=<span class="string">"https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css"</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- HTML5 shim 和 Respond.js 是为了让 IE8 支持 HTML5 元素和媒体查询（media queries）功能 --&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 警告：通过 file:// 协议（就是直接将 html 页面拖拽到浏览器中）访问页面时 Respond.js 不起作用 --&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--[if lt IE 9]&gt;</span></span><br><span class="line"><span class="comment">      &lt;script src="https://cdn.jsdelivr.net/npm/html5shiv@3.7.3/dist/html5shiv.min.js"&gt;&lt;/script&gt;</span></span><br><span class="line"><span class="comment">      &lt;script src="https://cdn.jsdelivr.net/npm/respond.js@1.4.2/dest/respond.min.js"&gt;&lt;/script&gt;</span></span><br><span class="line"><span class="comment">    &lt;![endif]--&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">h1</span>&gt;</span>你好，世界！<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- jQuery (Bootstrap 的所有 JavaScript 插件都依赖 jQuery，所以必须放在前边) --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://cdn.jsdelivr.net/npm/jquery@1.12.4/dist/jquery.min.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 加载 Bootstrap 的所有 JavaScript 插件。你也可以根据需要只加载单个插件。 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/js/bootstrap.min.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ol>
<li><p>Compatible</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"X-UA-Compatible"</span> <span class="attr">content</span>=<span class="string">"IE=edge"</span>&gt;</span></span><br><span class="line">//设置浏览器的兼容模式版本。表示如果在IE浏览器下则使用最新的标准渲染当前文档</span><br></pre></td></tr></table></figure>
</li>
<li><p>viewport视口：为了确保适当的绘制和触屏缩放</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width, initial-scale=1"</span>&gt;</span></span><br><span class="line">//声明当前网页在移动端浏览器中展示的相关设置。视口的作用：在移动浏览器中，当页面宽度超出设备，浏览器内部虚拟的一个页面容器，将页面容器缩放到设备这么大，然后展示</span><br></pre></td></tr></table></figure>

<p>属性解释：</p>
<ul>
<li>width:设置viewport的宽度。</li>
<li>initial-scale:初始化缩放比例。</li>
<li>minimum-scale：最小缩放比例。</li>
<li>maximum-scale：最大缩放比例。</li>
<li>user-scalable：是否允许用户手动缩放（yes/no,也可以写成1/0）</li>
</ul>
<p><strong>如果设置了不允许用户手动缩放，那么最小缩放和最大缩放就没有意义了。</strong></p>
<h3 id="使用Bootstrap搭建项目"><a href="#使用Bootstrap搭建项目" class="headerlink" title="使用Bootstrap搭建项目"></a>使用Bootstrap搭建项目</h3><ol>
<li>工程文件的目录结构</li>
</ol>
</li>
</ol>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">├─ Demo ·························· 项目所在目录</span><br><span class="line">└─┬─ /css/ ······················· 我们自己的CSS文件</span><br><span class="line">  ├─ /font/ ······················ 使用到的字体文件</span><br><span class="line">  ├─ /img/ ······················· 使用到的图片文件</span><br><span class="line">  ├─ /js/ ························ 自己写的JS脚步</span><br><span class="line">  ├─ /lib/ ······················· 从第三方下载回来的库【只用不改】</span><br><span class="line">  ├─ /favicon.ico ················ 站点图标</span><br><span class="line">  └─ /index.html ················· 入口文件</span><br></pre></td></tr></table></figure>

<p><a href="https://github.com/aFarkas/html5shiv" target="_blank" rel="noopener">html5shiv</a>让浏览器可以识别 HTML5 的新标签。如header、footer、section等。</p>
<p><a href="https://github.com/scottjehl/Respond" target="_blank" rel="noopener">respond.js</a>让低版本浏览器可以使用css3的媒体查询</p>
<p>JQuery：Bootstrap框架中的所有js组件都依赖于jquery实现。</p>
<p>我们可以把上面这三个库文件拷贝到lib文件夹中</p>
<h3 id="布局容器：container类"><a href="#布局容器：container类" class="headerlink" title="布局容器：container类"></a>布局容器：container类</h3><p>Bootstrap需要为页面内容和栅格系统包裹一个<code>.container</code>容器。由于<code>padding</code>等属性的原因，这两种容器不能相互嵌套。</p>
<p><code>container</code>类用于固定宽度并支持响应式布局的容器</p>
<p><code>container-fluid</code>类用于100%宽度，占据全部视口（viewport）的容器，又称流体容器</p>
<h3 id="媒体查询"><a href="#媒体查询" class="headerlink" title="媒体查询"></a>媒体查询</h3><p>在栅格系统中，我们在less文件中使用以下媒体查询来创建关键的分界点</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line">/* 超小屏幕（手机，小于 768px） */</span><br><span class="line">/* 没有任何媒体查询相关的代码，因为这在 Bootstrap 中是默认的（还记得 Bootstrap 是移动设备优先的吗？） */</span><br><span class="line"></span><br><span class="line">/* 小屏幕（平板，大于等于 768px） */</span><br><span class="line">@media (min-width: @screen-sm-min) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line">/* 中等屏幕（桌面显示器，大于等于 992px） */</span><br><span class="line">@media (min-width: @screen-md-min) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line">/* 大屏幕（大桌面显示器，大于等于 1200px） */</span><br><span class="line">@media (min-width: @screen-lg-min) &#123; ... &#125;</span><br></pre></td></tr></table></figure>

<img src="/2019/08/27/8-27bootstrap/6.png">

<h3 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h3><p>一个按钮称之为样式；两个按钮在一起，就可以称之为组件</p>
<p>js组件在官网有介绍</p>
<h3 id="bootstrap源码分析"><a href="#bootstrap源码分析" class="headerlink" title="bootstrap源码分析"></a>bootstrap源码分析</h3><p>固定容器</p>
<table>
<thead>
<tr>
<th>阈值</th>
<th align="center">width</th>
</tr>
</thead>
<tbody><tr>
<td>大于等于1200（lg   大屏pc）</td>
<td align="center">1170</td>
</tr>
<tr>
<td>大于等于992（md   中屏pc）小于1200</td>
<td align="center">970</td>
</tr>
<tr>
<td>大于等于768（sm  平板）</td>
<td align="center">750</td>
</tr>
<tr>
<td>小于768（xs   移动手机）</td>
<td align="center">auto</td>
</tr>
</tbody></table>
<h4 id="less根目录下的grid-less"><a href="#less根目录下的grid-less" class="headerlink" title="less根目录下的grid.less"></a>less根目录下的grid.less</h4><figure class="hljs highlight less"><table><tr><td class="code"><pre><span class="line"><span class="comment">//固定容器</span></span><br><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">  <span class="comment">//在mixin文件夹下grid.less的公用样式</span></span><br><span class="line">  <span class="selector-class">.container-fixed</span>();</span><br><span class="line"><span class="comment">//媒体查询</span></span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-sm-min</span>) &#123;</span><br><span class="line">    <span class="attribute">width</span>: <span class="variable">@container-sm</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-md-min</span>) &#123;</span><br><span class="line">    <span class="attribute">width</span>: <span class="variable">@container-md</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-lg-min</span>) &#123;</span><br><span class="line">    <span class="attribute">width</span>: <span class="variable">@container-lg</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Fluid container流体容器</span></span><br><span class="line"><span class="selector-class">.container-fluid</span> &#123;</span><br><span class="line">  <span class="selector-class">.container-fixed</span>();</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Row行的定义</span></span><br><span class="line"><span class="selector-class">.row</span> &#123;</span><br><span class="line">  <span class="selector-class">.make-row</span>();</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Columns列的定义</span></span><br><span class="line"><span class="selector-class">.make-grid-columns</span>();</span><br><span class="line"><span class="selector-class">.make-grid</span>(xs);</span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-sm-min</span>) &#123;</span><br><span class="line">  <span class="selector-class">.make-grid</span>(sm);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-md-min</span>) &#123;</span><br><span class="line">  <span class="selector-class">.make-grid</span>(md);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-lg-min</span>) &#123;</span><br><span class="line">  <span class="selector-class">.make-grid</span>(lg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="grid-framework-less"><a href="#grid-framework-less" class="headerlink" title="grid-framework.less"></a>grid-framework.less</h4><figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">行偏移与列偏移</span><br><span class="line">calc-grid-column(@index, @class, @type) when (@type = width) and (@index &gt; 0) &#123;</span><br><span class="line">  .col-@&#123;class&#125;-@&#123;index&#125; &#123;</span><br><span class="line">    width: percentage((@index / @grid-columns));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">.calc-grid-column(@index, @class, @type) when (@type = push) and (@index &gt; 0) &#123;</span><br><span class="line">  .col-@&#123;class&#125;-push-@&#123;index&#125; &#123;</span><br><span class="line">    left: percentage((@index / @grid-columns));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">.calc-grid-column(@index, @class, @type) when (@type = push) and (@index = 0) &#123;</span><br><span class="line">  .col-@&#123;class&#125;-push-0 &#123;</span><br><span class="line">    left: auto;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">.calc-grid-column(@index, @class, @type) when (@type = pull) and (@index &gt; 0) &#123;</span><br><span class="line">  .col-@&#123;class&#125;-pull-@&#123;index&#125; &#123;</span><br><span class="line">    right: percentage((@index / @grid-columns));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">.calc-grid-column(@index, @class, @type) when (@type = pull) and (@index = 0) &#123;</span><br><span class="line">  .col-@&#123;class&#125;-pull-0 &#123;</span><br><span class="line">    right: auto;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">.calc-grid-column(@index, @class, @type) when (@type = offset) &#123;</span><br><span class="line">  .col-@&#123;class&#125;-offset-@&#123;index&#125; &#123;</span><br><span class="line">    margin-left: percentage((@index / @grid-columns));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="mixin文件夹下的grid-less"><a href="#mixin文件夹下的grid-less" class="headerlink" title="mixin文件夹下的grid.less"></a>mixin文件夹下的grid.less</h4><figure class="hljs highlight less"><table><tr><td class="code"><pre><span class="line">/ Grid system固定容器和流体容器的公告样式</span><br><span class="line"><span class="comment">//@grid-gutter-width槽宽  30px</span></span><br><span class="line"><span class="selector-class">.container-fixed</span>(<span class="variable">@gutter</span>: <span class="variable">@grid-gutter-width</span>) &#123;</span><br><span class="line">  <span class="attribute">margin-right</span>: auto;</span><br><span class="line">  <span class="attribute">margin-left</span>: auto;</span><br><span class="line">  <span class="attribute">padding-left</span>:  floor((<span class="variable">@gutter</span> / <span class="number">2</span>));</span><br><span class="line">  <span class="attribute">padding-right</span>: ceil((<span class="variable">@gutter</span> / <span class="number">2</span>));</span><br><span class="line">  <span class="selector-tag">&amp;</span><span class="selector-pseudo">:extend(.clearfix</span> <span class="keyword">all</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Creates a wrapper for a series of columns</span></span><br><span class="line"><span class="selector-class">.make-row</span>(<span class="variable">@gutter</span>: <span class="variable">@grid-gutter-width</span>) &#123;</span><br><span class="line">  <span class="attribute">margin-left</span>:  ceil((<span class="variable">@gutter</span> / -<span class="number">2</span>));</span><br><span class="line">  <span class="attribute">margin-right</span>: floor((<span class="variable">@gutter</span> / -<span class="number">2</span>));</span><br><span class="line">  <span class="selector-tag">&amp;</span><span class="selector-pseudo">:extend(.clearfix</span> <span class="keyword">all</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Generate the extra small columns</span></span><br><span class="line"><span class="selector-class">.make-xs-column</span>(<span class="variable">@columns</span>; <span class="variable">@gutter</span>: <span class="variable">@grid-gutter-width</span>) &#123;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">float</span>: left;</span><br><span class="line">  <span class="attribute">width</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">1px</span>;</span><br><span class="line">  <span class="attribute">padding-left</span>:  (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line">  <span class="attribute">padding-right</span>: (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-xs-column-offset</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="attribute">margin-left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-xs-column-push</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="attribute">left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-xs-column-pull</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="attribute">right</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Generate the small columns</span></span><br><span class="line"><span class="selector-class">.make-sm-column</span>(<span class="variable">@columns</span>; <span class="variable">@gutter</span>: <span class="variable">@grid-gutter-width</span>) &#123;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">1px</span>;</span><br><span class="line">  <span class="attribute">padding-left</span>:  (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line">  <span class="attribute">padding-right</span>: (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-sm-min</span>) &#123;</span><br><span class="line">    <span class="attribute">float</span>: left;</span><br><span class="line">    <span class="attribute">width</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-sm-column-offset</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-sm-min</span>) &#123;</span><br><span class="line">    <span class="attribute">margin-left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-sm-column-push</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-sm-min</span>) &#123;</span><br><span class="line">    <span class="attribute">left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-sm-column-pull</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-sm-min</span>) &#123;</span><br><span class="line">    <span class="attribute">right</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Generate the medium columns</span></span><br><span class="line"><span class="selector-class">.make-md-column</span>(<span class="variable">@columns</span>; <span class="variable">@gutter</span>: <span class="variable">@grid-gutter-width</span>) &#123;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">1px</span>;</span><br><span class="line">  <span class="attribute">padding-left</span>:  (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line">  <span class="attribute">padding-right</span>: (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-md-min</span>) &#123;</span><br><span class="line">    <span class="attribute">float</span>: left;</span><br><span class="line">    <span class="attribute">width</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-md-column-offset</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-md-min</span>) &#123;</span><br><span class="line">    <span class="attribute">margin-left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-md-column-push</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-md-min</span>) &#123;</span><br><span class="line">    <span class="attribute">left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-md-column-pull</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-md-min</span>) &#123;</span><br><span class="line">    <span class="attribute">right</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Generate the large columns</span></span><br><span class="line"><span class="selector-class">.make-lg-column</span>(<span class="variable">@columns</span>; <span class="variable">@gutter</span>: <span class="variable">@grid-gutter-width</span>) &#123;</span><br><span class="line">  <span class="attribute">position</span>: relative;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">1px</span>;</span><br><span class="line">  <span class="attribute">padding-left</span>:  (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line">  <span class="attribute">padding-right</span>: (<span class="variable">@gutter</span> / <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-lg-min</span>) &#123;</span><br><span class="line">    <span class="attribute">float</span>: left;</span><br><span class="line">    <span class="attribute">width</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-lg-column-offset</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-lg-min</span>) &#123;</span><br><span class="line">    <span class="attribute">margin-left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-lg-column-push</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-lg-min</span>) &#123;</span><br><span class="line">    <span class="attribute">left</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.make-lg-column-pull</span>(<span class="variable">@columns</span>) &#123;</span><br><span class="line">  <span class="keyword">@media</span> (<span class="attribute">min-width</span>: <span class="variable">@screen-lg-min</span>) &#123;</span><br><span class="line">    <span class="attribute">right</span>: percentage((<span class="variable">@columns</span> / <span class="variable">@grid-columns</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>Bootstrap</category>
      </categories>
      <tags>
        <tag>UI 框架</tag>
      </tags>
  </entry>
  <entry>
    <title>Ajax</title>
    <url>/2019/08/02/Ajax/</url>
    <content><![CDATA[<h1 id="AJAX"><a href="#AJAX" class="headerlink" title="AJAX"></a>AJAX</h1><p>Ajax = Asynchronous JavaScript and XML（异步的JavaScript和XML）</p>
<p>在浏览器中，我们可以在不刷新页面的情况下，通过ajax的方式去获取一些新的内容</p>
<a id="more"></a>

<h2 id="发送Ajax请求的五个步骤"><a href="#发送Ajax请求的五个步骤" class="headerlink" title="发送Ajax请求的五个步骤"></a>发送Ajax请求的五个步骤</h2><ul>
<li>创建异步对象。即XMLHttpRequest对象。</li>
<li>使用open方法设置请求的参数。open(method,url,async)</li>
<li>发送请求</li>
<li>注册事件。注册onreadystatechange事件，状态改变时就会调用。</li>
<li>获取返回的数据</li>
</ul>
<h2 id="函数封装"><a href="#函数封装" class="headerlink" title="函数封装"></a>函数封装</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 方法：ajax get 五部曲</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ajax_get</span>(<span class="params">url,data</span>) </span>&#123;</span><br><span class="line">	<span class="comment">// 异步对象</span></span><br><span class="line">	<span class="keyword">var</span> ajax = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line"></span><br><span class="line">	<span class="comment">// url 方法</span></span><br><span class="line">	<span class="comment">// 如果是get发送数据 发送的格式为 xxx.php?name=jack&amp;age=18</span></span><br><span class="line">	<span class="comment">// 所以 这里 需要拼接 url</span></span><br><span class="line">	<span class="keyword">if</span> (data) &#123;</span><br><span class="line">		<span class="comment">// 如果有值 需要拼接字符串</span></span><br><span class="line">		<span class="comment">// 拼接为xxx.php?name=jack&amp;age=18</span></span><br><span class="line">		url+=<span class="string">'?'</span>;</span><br><span class="line">		url+=data;</span><br><span class="line">	&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	ajax.open(<span class="string">'get'</span>,url);</span><br><span class="line">	<span class="comment">// 发送</span></span><br><span class="line">	ajax.send();</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 注册事件</span></span><br><span class="line">	ajax.onreadystatechange = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">		<span class="comment">// 在事件中 获取数据 并修改界面显示</span></span><br><span class="line">		<span class="keyword">if</span> (ajax.readyState==<span class="number">4</span>&amp;&amp; ajax.status==<span class="number">200</span>) &#123;</span><br><span class="line">			<span class="built_in">console</span>.log(ajax.responseText);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 方法：ajax_post五部曲</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ajax_post</span>(<span class="params">url,data</span>) </span>&#123;</span><br><span class="line">	<span class="comment">// 异步对象</span></span><br><span class="line">	<span class="keyword">var</span> ajax = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line"></span><br><span class="line">	<span class="comment">// url 方法</span></span><br><span class="line">	ajax.open(<span class="string">'post'</span>,url);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 设置 请求报文</span></span><br><span class="line">	ajax.setRequestHeader(<span class="string">"Content-type"</span>,<span class="string">"application/x-www-form-urlencoded"</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 发送</span></span><br><span class="line">	<span class="keyword">if</span> (data) &#123;</span><br><span class="line">		<span class="comment">// 如果 有值 post请求 是在 send中 发送给服务器</span></span><br><span class="line">		ajax.send(data);</span><br><span class="line">	&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">		ajax.send();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="comment">// 注册事件</span></span><br><span class="line">	ajax.onreadystatechange = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">		<span class="comment">// 在事件中 获取数据 并修改界面显示</span></span><br><span class="line">		<span class="keyword">if</span> (ajax.readyState==<span class="number">4</span>&amp;&amp;ajax.status==<span class="number">200</span>) &#123;</span><br><span class="line">			<span class="built_in">console</span>.log(ajax.responseText);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 方法：将 get 跟post 封装到一起</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">	参数1:url</span></span><br><span class="line"><span class="comment">	参数2:数据</span></span><br><span class="line"><span class="comment">	参数3:请求的方法</span></span><br><span class="line"><span class="comment">	参数4:数据成功获取以后，调用的方法</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ajax_tool</span>(<span class="params">url,data,method,success</span>) </span>&#123;</span><br><span class="line">	<span class="comment">// 异步对象</span></span><br><span class="line">	<span class="keyword">var</span> ajax = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line"></span><br><span class="line">	<span class="comment">// get 跟post  需要分别写不同的代码</span></span><br><span class="line">	<span class="keyword">if</span> (method==<span class="string">'get'</span>) &#123;</span><br><span class="line">		<span class="comment">// get请求</span></span><br><span class="line">		<span class="keyword">if</span> (data) &#123;</span><br><span class="line">			<span class="comment">// 如果有值</span></span><br><span class="line">			url+=<span class="string">'?'</span>;</span><br><span class="line">			url+=data;</span><br><span class="line">		&#125;<span class="keyword">else</span>&#123;</span><br><span class="line"></span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">// 设置 方法 以及 url</span></span><br><span class="line">		ajax.open(method,url);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// send即可</span></span><br><span class="line">		ajax.send();</span><br><span class="line">	&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">		<span class="comment">// post请求</span></span><br><span class="line">		<span class="comment">// post请求 url 是不需要改变</span></span><br><span class="line">		ajax.open(method,url);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 需要设置请求报文</span></span><br><span class="line">		ajax.setRequestHeader(<span class="string">"Content-type"</span>,<span class="string">"application/x-www-form-urlencoded"</span>);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 判断data send发送数据</span></span><br><span class="line">		<span class="keyword">if</span> (data) &#123;</span><br><span class="line">			<span class="comment">// 如果有值 从send发送</span></span><br><span class="line">			ajax.send(data);</span><br><span class="line">		&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">			<span class="comment">// 木有值 直接发送即可</span></span><br><span class="line">			ajax.send();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// 注册事件</span></span><br><span class="line">	ajax.onreadystatechange = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">		<span class="comment">// 在事件中 获取数据 并修改界面显示</span></span><br><span class="line">		<span class="keyword">if</span> (ajax.readyState==<span class="number">4</span>&amp;&amp;ajax.status==<span class="number">200</span>) &#123;</span><br><span class="line">			<span class="comment">// console.log(ajax.responseText);</span></span><br><span class="line"></span><br><span class="line">			<span class="comment">// 将 数据 让 外面可以使用</span></span><br><span class="line">			<span class="comment">// return ajax.responseText;</span></span><br><span class="line"></span><br><span class="line">			<span class="comment">// 当 onreadystatechange 调用时 说明 数据回来了</span></span><br><span class="line">			<span class="comment">// ajax.responseText;</span></span><br><span class="line"></span><br><span class="line">			<span class="comment">// 数据成功获取以后，执行方法success()。</span></span><br><span class="line">			<span class="comment">//我们把获取的数据作为 success()的参数，意思是：</span></span><br><span class="line">			<span class="comment">//success方法是外面的，数据是里面给的。那数据就变相地传递到了外面去【重要】</span></span><br><span class="line">			success(ajax.responseText);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>Ajax</category>
      </categories>
      <tags>
        <tag>Ajax</tag>
      </tags>
  </entry>
  <entry>
    <title>Ajax2</title>
    <url>/2020/07/23/Ajax2/</url>
    <content><![CDATA[<h1 id="关于Ajax"><a href="#关于Ajax" class="headerlink" title="关于Ajax"></a>关于Ajax</h1><p>对于在学习Ajax过程中，再看了各种教程，或者视频讲解时，粗略了解到我们可以在不刷新页面的情况下，通过Ajax的方式区获取新的内容</p>
<a id="more"></a>

<h2 id="发送Ajax请求的五个步骤"><a href="#发送Ajax请求的五个步骤" class="headerlink" title="发送Ajax请求的五个步骤"></a>发送Ajax请求的五个步骤</h2><ul>
<li>创建异步对象。即XMLHttpRequest对象。</li>
<li>使用open方法设置请求的参数。open(method,url,async)</li>
<li>发送请求</li>
<li>注册事件。注册onreadystatechange事件，状态改变时就会调用。</li>
<li>获取返回的数据</li>
</ul>
<p>并且对Ajax的五个步骤进行封装，将url和将要执行的请求方法或者回调函数进行封装复用</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getAjax</span>(<span class="params">httpUrl,data,callbackFn</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">    <span class="comment">// 设置请求方法和路径</span></span><br><span class="line">    xhr.open(<span class="string">'GET'</span>,httpUrl+parseData(data));</span><br><span class="line">    <span class="comment">// 发送数据</span></span><br><span class="line">    xhr.send()</span><br><span class="line">    <span class="comment">// 监听后台是否返回数据</span></span><br><span class="line">    xhr.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(xhr.status==<span class="number">200</span>&amp;&amp;xhr.readyState ==<span class="number">4</span>)&#123;</span><br><span class="line">           callbackFn(xhr)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">parseData</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> str = <span class="string">"?"</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> key <span class="keyword">in</span> data)&#123;</span><br><span class="line">            str = str + key + <span class="string">"="</span> + data[key]+ <span class="string">"&amp;"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        str.substr(<span class="number">0</span>,str.length<span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> str</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="练习1"><a href="#练习1" class="headerlink" title="练习1"></a>练习1</h3><p>在大都数教程中会告诉我们，通过设置url就可以将数据进行返回</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> httpUrl = <span class="string">"https://api.apiopen.top/getJoke"</span></span><br><span class="line">    <span class="keyword">var</span> data = &#123;</span><br><span class="line">        page:<span class="number">1</span>,</span><br><span class="line">        count:<span class="number">10</span>,</span><br><span class="line">        type:<span class="string">"video"</span></span><br><span class="line">    &#125;</span><br><span class="line">    getAjax(httpUrl,data,<span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(xhr)</span><br><span class="line">        <span class="keyword">var</span> datiObj = <span class="built_in">JSON</span>.parse(xhr.response)</span><br><span class="line">        <span class="built_in">console</span>.log(datiObj)</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<img src="/2020/07/23/Ajax2/1.png">

<p>这样就请求到了数据，但是还是不清楚我们请求到的数据要做什么，在实际的项目中ajax是怎么操作的</p>
<p>一直保持懵懂的了解基本api 的程度</p>
<h2 id="练习2"><a href="#练习2" class="headerlink" title="练习2"></a>练习2</h2><p>通过练习一个段子项目渲染页面来熟悉ajax操作</p>
<img src="/2020/07/23/Ajax2/2.png">

<p>通过查看易点租的页面发现内部的商品结构全部是由js进行dom节点插入，我们实现一个类似今日头条的视频页面</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"videoList"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"videoItem"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"videoTop"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"img"</span> <span class="attr">style</span>=<span class="string">'background-image: url("http://wimg.spriteapp.cn/picture/2020/0713/5f0c04f33bf50_wpd.jpg")'</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">button</span> <span class="attr">type</span>=<span class="string">"button"</span>&gt;</span><span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">h3</span> <span class="attr">class</span>=<span class="string">"title"</span>&gt;</span>无知者无畏。。。看好自家小孩，以免酿成悲剧<span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">p</span>&gt;</span><span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"icon"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span> <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>10<span class="tag">&lt;/<span class="name">span</span>&gt;</span>次播放<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"videoBottom"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"http://thirdwx.qlogo.cn/mmopen/vi_32/rlIeDLHvqo1MpvibllRxg4K4pBJ8CxRkN8Vtv5TpJjicdeetpuJtqqibAHEHC1IKibAHqaQ5DQKDBg9XOPib76HlIicA/132"</span> <span class="attr">alt</span>=<span class="string">""</span> <span class="attr">class</span>=<span class="string">"headerImg"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"author"</span>&gt;</span>老朽姓何<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>根据页面结构设置好视频列表的布局，然后我们需要编辑该盒子CSS样式，这个时候的图片素材是我们直接在结构中写的已知的链接，</p>
<p>接下来编写ajax请求数据获取所有的图片等资源</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> httpUrl = <span class="string">"https://api.apiopen.top/getJoke?page=1&amp;count10&amp;type=video"</span>;</span><br><span class="line">    getAjax(httpUrl, <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">        <span class="comment">//var resultObj = "数据获取不到"</span></span><br><span class="line">        <span class="keyword">var</span> resultObj = <span class="built_in">JSON</span>.parse(xhr.response);</span><br><span class="line">        <span class="built_in">console</span>.log(resultObj);</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<p><strong>getAjax()函数是我们引入的已经封装好的Ajax文件</strong></p>
<h3 id="那么接下来是我们如何将页面动态的渲染"><a href="#那么接下来是我们如何将页面动态的渲染" class="headerlink" title="那么接下来是我们如何将页面动态的渲染"></a>那么接下来是我们如何将页面动态的渲染</h3><ol>
<li>首先加载好了数据</li>
<li>我们要通过获取的数据渲染出视频列表，那么就需要进行循环的创建</li>
</ol>
<p><strong>将上方写好的videoItem注释掉</strong></p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> httpUrl = <span class="string">"https://api.apiopen.top/getJoke?page=1&amp;count10&amp;type=video"</span>;</span><br><span class="line">  getAjax(httpUrl, <span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">var</span> resultObj = <span class="string">"数据获取不到"</span></span><br><span class="line">      <span class="keyword">var</span> resultObj = <span class="built_in">JSON</span>.parse(xhr.response);</span><br><span class="line">      <span class="built_in">console</span>.log(resultObj);</span><br><span class="line">      <span class="comment">//将请求的结果赋值给videolist</span></span><br><span class="line">      <span class="keyword">var</span> videoList = resultObj.result;</span><br><span class="line">      renderVideoList(videoList);</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="comment">//编写渲染函数</span></span><br><span class="line">   <span class="function"><span class="keyword">function</span> <span class="title">renderVideoList</span>(<span class="params">videoList</span>) </span>&#123;</span><br><span class="line">       <span class="comment">//循环创建内部节点</span></span><br><span class="line">      videoList.forEach(<span class="function"><span class="keyword">function</span> (<span class="params">item, index</span>) </span>&#123;</span><br><span class="line">          <span class="comment">//创建内部节点并添加类名</span></span><br><span class="line">          <span class="keyword">var</span> videoItem = <span class="built_in">document</span>.createElement(<span class="string">"div"</span>)</span><br><span class="line">          <span class="comment">//添加类名以便获取CSS样式</span></span><br><span class="line">          videoItem.className = <span class="string">"videoItem"</span></span><br><span class="line">          videoItem.innerHTML = <span class="string">`&lt;div class="videoTop"&gt;</span></span><br><span class="line"><span class="string">              &lt;div class="img" style='background-image: url(<span class="subst">$&#123;item.thumbnail&#125;</span>)'&gt;&lt;/div&gt;</span></span><br><span class="line"><span class="string">              &lt;button type="button"&gt;&lt;/button&gt;</span></span><br><span class="line"><span class="string">              &lt;h3 class="title"&gt;<span class="subst">$&#123;item.text&#125;</span>&lt;/h3&gt;</span></span><br><span class="line"><span class="string">              &lt;p&gt;&lt;span class="icon yanjing"&gt;&lt;/span&gt; &lt;span class="num"&gt;<span class="subst">$&#123;item.up&#125;</span>&lt;/span&gt;次播放&lt;/p&gt;</span></span><br><span class="line"><span class="string">          &lt;/div&gt;`</span></span><br><span class="line">          <span class="keyword">var</span> videoListDom = <span class="built_in">document</span>.querySelector(<span class="string">".videoList"</span>)</span><br><span class="line">          videoListDom.appendChild(videoItem)</span><br><span class="line">      &#125;)</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<img src="/2020/07/23/Ajax2/4.png">

<p>我们可以看到js渲染的页面全部获取到了ajax请求到的数据，包括每个栏目的图片、标题、观看量等。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>Ajax</category>
      </categories>
      <tags>
        <tag>Ajax</tag>
      </tags>
  </entry>
  <entry>
    <title>Ajax3-jquery</title>
    <url>/2020/07/24/Ajax3-jquery/</url>
    <content><![CDATA[<h1 id="原生Ajax"><a href="#原生Ajax" class="headerlink" title="原生Ajax"></a>原生Ajax</h1><h2 id="封装Ajax"><a href="#封装Ajax" class="headerlink" title="封装Ajax"></a>封装Ajax</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getAjax</span>(<span class="params">httpUrl,callbackFn</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">    <span class="comment">// 设置请求方法和路径</span></span><br><span class="line">    xhr.open(<span class="string">'GET'</span>,httpUrl);</span><br><span class="line">    <span class="comment">// 发送数据</span></span><br><span class="line">    xhr.send()</span><br><span class="line">    <span class="comment">// 监听后台是否返回数据</span></span><br><span class="line">    xhr.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(xhr.status==<span class="number">200</span>&amp;&amp;xhr.readyState ==<span class="number">4</span>)&#123;</span><br><span class="line">           callbackFn(xhr)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h2 id="调用Ajax"><a href="#调用Ajax" class="headerlink" title="调用Ajax"></a>调用Ajax</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">getAjax(httpUrl,<span class="function"><span class="keyword">function</span> (<span class="params">xhr</span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(xhr)</span><br><span class="line">        <span class="keyword">var</span> dataObj = <span class="built_in">JSON</span>.parse(xhr.response)</span><br><span class="line">        <span class="built_in">document</span>.querySelector(<span class="string">'body'</span>).append(<span class="string">`&lt;h1&gt;<span class="subst">$&#123;res.title&#125;</span>&lt;/h1&gt;`</span>)</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<p>加上url后，使用异步的回调函数将JSON数据进行解析</p>
<h2 id="使用jQueryAjax"><a href="#使用jQueryAjax" class="headerlink" title="使用jQueryAjax"></a>使用jQueryAjax</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$.<span class="keyword">get</span>(httpUrl,function (res) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(res)</span><br><span class="line">        $(<span class="string">'body'</span>).append(<span class="string">`&lt;h1&gt;<span class="subst">$&#123;res.title&#125;</span>&lt;/h1&gt;`</span>)</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<p>不需要再对JSON进行解析</p>
<h2 id="其它写法"><a href="#其它写法" class="headerlink" title="其它写法"></a>其它写法</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 请求段子地址</span></span><br><span class="line">    <span class="keyword">var</span> httpUrl1 = <span class="string">"https://api.apiopen.top/getJoke?page=1&amp;count10&amp;type=video"</span></span><br><span class="line">    $.<span class="keyword">get</span>(httpUrl1).then(function (res) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(res)</span><br><span class="line">    &#125;)</span><br><span class="line"><span class="comment">//使用.then()并且将参数作为对象传入</span></span><br><span class="line">    <span class="keyword">var</span> httpUrl2 = <span class="string">"https://api.apiopen.top/getJoke"</span></span><br><span class="line">    <span class="keyword">var</span> options = &#123;</span><br><span class="line">        page: <span class="number">1</span>,</span><br><span class="line">        count: <span class="number">10</span>,</span><br><span class="line">        type: <span class="string">"image"</span></span><br><span class="line">    &#125;</span><br><span class="line">    $.<span class="keyword">get</span>(httpUrl2, options).then(function (res) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(res)</span><br><span class="line">        res.result.forEach(<span class="function"><span class="keyword">function</span> (<span class="params">item, i</span>) </span>&#123;</span><br><span class="line">            $(<span class="string">'body'</span>).append(<span class="string">`&lt;img src="<span class="subst">$&#123;item.images&#125;</span>"&gt;`</span>)</span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<h2 id="ajax"><a href="#ajax" class="headerlink" title="$.ajax"></a>$.ajax</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$.ajax(&#123;</span><br><span class="line">        url: httpUrl2,</span><br><span class="line">        data: <span class="string">"options"</span>,<span class="comment">//地址后面的参数</span></span><br><span class="line">        method: <span class="string">"GET"</span>,</span><br><span class="line">        <span class="comment">//成功执行的函数</span></span><br><span class="line">        <span class="comment">// success:function () &#123;</span></span><br><span class="line">        <span class="comment">//</span></span><br><span class="line">        <span class="comment">// &#125;,</span></span><br><span class="line">        <span class="comment">// // 失败执行的函数</span></span><br><span class="line">        <span class="comment">// fail:function () &#123;</span></span><br><span class="line">        <span class="comment">//</span></span><br><span class="line">        <span class="comment">// &#125;,</span></span><br><span class="line">        <span class="comment">//不管成功还是失败都会执行的函数</span></span><br><span class="line">        complete: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span><br><span class="line">            <span class="built_in">console</span>.log(res)</span><br><span class="line">            <span class="keyword">var</span> res = res.responseJSON;</span><br><span class="line">            res.result.forEach(<span class="function"><span class="keyword">function</span> (<span class="params">item, i</span>) </span>&#123;</span><br><span class="line">                $(<span class="string">'body'</span>).append(<span class="string">`&lt;img src="<span class="subst">$&#123;item.images&#125;</span>"&gt;`</span>)</span><br><span class="line">            &#125;)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<p><strong>$.ajax()方法是jQuery最底层的Ajax实现</strong></p>
]]></content>
      <categories>
        <category>前端</category>
        <category>Ajax</category>
      </categories>
      <tags>
        <tag>jQuery</tag>
        <tag>Ajax</tag>
      </tags>
  </entry>
  <entry>
    <title>LESS</title>
    <url>/2019/03/11/LESS/</url>
    <content><![CDATA[<h1 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h1><a id="more"></a>

<p>特点：延迟执行</p>
<ol>
<li><p>值变量</p>
<p>定义：@变量名：属性值；</p>
<p>调用：@变量名</p>
</li>
<li><p>选择器变量</p>
<p>定义：@变量名：选择器名</p>
<p>调用：@{变量名}：{} </p>
</li>
<li><p>属性变量：</p>
<p>语法同上，不仅可存选择器属性名，也可存取其部分名称</p>
</li>
<li><p>url变量（）</p>
<p>定义：@变量名：‘../images’;</p>
<p>调用：@{url}</p>
</li>
<li><p>声明变量：在一个变量中声明一组样式：***声明：@变量名：{样式集合}；调用：@变量名（）；</p>
</li>
<li><p>变量运算：</p>
<p>数值：<em>减法操作时，减号前后要加空格</em>；</p>
<p>颜色：是否书写#，返回结果不同；</p>
<p>字符串：声明变量时建议用‘  ‘ 调用时间’{}‘ ；</p>
<p>用变量定义变量：</p>
<p>用第二个变量值定义第一个变量名，可实现传递变量值</p>
<h2 id="嵌套"><a href="#嵌套" class="headerlink" title="嵌套"></a>嵌套</h2><ul>
<li><p>外层元素{</p>
<p>​    子元素1{}</p>
<p>​    子元素2{</p>
<p>​          孙子元素{}</p>
<p>​       }</p>
<p>}</p>
<p>&amp;符的作用：代表上层元素的名字</p>
<p>1.用于连接伪类</p>
<p>2.用于交集选择器</p>
<p>3.用于动态统一命名&amp;前不可用其它符号</p>
<h2 id="3-样式混合（Minxin）"><a href="#3-样式混合（Minxin）" class="headerlink" title="3.样式混合（Minxin）"></a>3.样式混合（Minxin）</h2><p>与@变量名：{}；类似，该功能类似于函数，特点 ：一次定义可任意调用，声明混合样式时，可用（）代表默认不执行，调用同：.box1===.box();</p>
<h2 id="4-参数混合"><a href="#4-参数混合" class="headerlink" title="4.参数混合"></a>4.参数混合</h2><p>类名参数:默认值（）{样式}</p>
<p>例：<code>.b1(){</code></p>
<p><code>border:@w solid red;</code></p>
<p><code>}</code></p>
<h2 id="5-模式匹配"><a href="#5-模式匹配" class="headerlink" title="5.模式匹配"></a>5.模式匹配</h2><p>1.根据参数个数匹配方法（根据变量匹配）</p>
<p>2.根据常量名称进行方法的匹配</p>
<p>note：常量参数可以被变量参数接受</p>
<h2 id="6-变量作用域"><a href="#6-变量作用域" class="headerlink" title="6.变量作用域"></a>6.变量作用域</h2><p>note：就近原则，如同层次没定义，向上册寻找</p>
<h2 id="7-命名空间"><a href="#7-命名空间" class="headerlink" title="7.命名空间"></a>7.命名空间</h2><p>1.方法的集合，且方法各自独立</p>
<p>2.调用时只能通过子代关系进行调用</p>
<p>3.调用路径中，父层方法不能用括号</p>
<p>4.如需使用父层变量，该变量要在激活状态</p>
<p>5.参数的初始化不等于变量定义，变量作用域只是一种方法，方法内应用，命名空间是被第三方调用准备的</p>
</li>
</ul>
</li>
</ol>
<h2 id="8-补充案例：变量与方法结合使用"><a href="#8-补充案例：变量与方法结合使用" class="headerlink" title="8.补充案例：变量与方法结合使用"></a>8.补充案例：变量与方法结合使用</h2><p>   在自定义方法中，表达式的运算结果可直接赋值给变量；可在全局使用；</p>
   <figure class="hljs highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.avg</span>(<span class="variable">@a</span>, <span class="variable">@b</span>) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="variable">@avger:</span>(<span class="variable">@a</span>+<span class="variable">@b</span>)/<span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-id">#wrap</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="selector-class">.avg</span>(<span class="number">20px</span>, <span class="number">46px</span>);</span><br><span class="line"></span><br><span class="line">    <span class="attribute">width</span>: <span class="variable">@avger</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="9-条件表达式"><a href="#9-条件表达式" class="headerlink" title="9.条件表达式"></a>9.条件表达式</h2><p>   方法名(参数集合) when(条件表达式){</p>
<p>   …………}</p>
<p>   例：</p>
   <figure class="hljs highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.bd</span>(<span class="variable">@a</span>) <span class="keyword">when</span>(<span class="variable">@a</span>&gt;=<span class="number">50</span>)&#123;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">border</span>: <span class="variable">@a</span> solid red;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.box1</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="selector-class">.bd</span>(<span class="number">51px</span>);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>   注意：条件中的数值单位，以实参单位为准，进行模糊匹配；</p>
<p>   @a&gt;=50 50可以是50px，50%，50pt,50em</p>
<p>   只有true关键字，才可以做为真值；</p>
<p>   运算符:</p>
<p>   比较类 &gt;  &lt;  &gt;=  =&lt;  =</p>
<p>   逻辑类:</p>
<p>   与：1 and 2</p>
<p>   或：1 or 2             1,  2</p>
<p>   非： not  1</p>
   <figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">.bd(@a,@b) when(@a&gt;=50) and (@b=solid)&#123;</span><br><span class="line">    border: @a @b red;</span><br><span class="line">&#125;</span><br><span class="line">.box1&#123;</span><br><span class="line">    .bd(51px,solid);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="循环：less中没有任何循环关键字：如for-while"><a href="#循环：less中没有任何循环关键字：如for-while" class="headerlink" title="循环：less中没有任何循环关键字：如for while"></a>循环：less中没有任何循环关键字：如for while</h2><p>   可通过调用自身来完成（迭代关系）；</p>
<p>   方法名(参数集合) when(条件表达式){</p>
<p>   }</p>
<h2 id="10-属性（值）拼接"><a href="#10-属性（值）拼接" class="headerlink" title="10.属性（值）拼接"></a>10.属性（值）拼接</h2><p>1.属性名+：属性值：追加以逗号分隔的属性值；</p>
<p>2.属性名+_：属性值   追加以空格分割的属性值;</p>
<h2 id="11-权重（！important）"><a href="#11-权重（！important）" class="headerlink" title="11.权重（！important）"></a>11.权重（！important）</h2><p>note：less中，可将！imoprtant追回给方法，方法中所有属性都被声明</p>
<h2 id="12-外部加载less（-import）"><a href="#12-外部加载less（-import）" class="headerlink" title="12.外部加载less（@import）"></a>12.外部加载less（@import）</h2><p>@import：可实现html与css关联，也可实现css与css关联</p>
<p>语法：</p>
<p>@import    [{关键字}]   “less或css文件路径”</p>
<p>1.once：加载多个同名文件时，只编译一次；也为默认方式</p>
<p>2.multiple：同名文件，可多次编译</p>
<p>3.reference：只引用不编译；（重点）</p>
<p>note：如变量相同，延迟执行，可引用less在任意位置；</p>
<h2 id="13-继承（重点）：是less中的一个伪类"><a href="#13-继承（重点）：是less中的一个伪类" class="headerlink" title="13.继承（重点）：是less中的一个伪类"></a>13.继承（重点）：是less中的一个伪类</h2><p>：extend</p>
<p>语法：</p>
<p>1.选择器{</p>
<p>​         &amp;：extend（被继承元素 [all]）；  </p>
<p> }</p>
<p>2.选择器：extend（被继承元素 all）{}</p>
<p>all:继承指定选择器的属性及其子选择器名和属性</p>
<h2 id="14内置函数"><a href="#14内置函数" class="headerlink" title="14内置函数"></a>14内置函数</h2><p>isnumber():isnumber(#ff0);</p>
<p>​    iscolor();</p>
<p>​    isurl();</p>
<p>​    颜色函数</p>
<p>​    saturate(@color，10%)：增加一定数值的颜色饱和度</p>
<p>​    lighten(@color,10%):增加一定数值的颜色亮度</p>
<p>​    darken(@color,10%):降低一定数值的颜色亮度</p>
<p>​    fade(@color,10%):给颜色设定一定熟知的透明度</p>
<p>​    fadein(@color,10%):给颜色增加透明度</p>
<p>​    fadeout(@color,10%):给颜色降低透明度</p>
<p>​    mix(@color1,@color2):根据比例混合两种颜色</p>
<p>​    hue(@color):提取颜色色调值</p>
<p>​    saturation(@color):提取饱和度</p>
<p>​    lightness(@color)：提取亮度</p>
<p>​    数学函数</p>
<p>​    ceil:向上取整</p>
<p>   fllor:向下取整</p>
<p>​    percentage:将浮点数转化为百分比字符串</p>
<p>​    round：四舍五入</p>
<p>​    sqrt：计算一个数的平方根</p>
<p>​    abs：计算一个数的平方根</p>
<p>​    pow：计算一个数个乘方</p>
<p>note：arguments:接收全部参数</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>CSS LESS</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue02-组件</title>
    <url>/2020/05/14/Vue02/</url>
    <content><![CDATA[<h2 id="Vue组件"><a href="#Vue组件" class="headerlink" title="Vue组件"></a>Vue组件</h2><p>从概念上说，组件可以拓展HTML元素，封装复用性高的代码。组件是我们自定义的元素，Vue为其添加 了特殊的功能。在特殊情况下，组件也可以是原生的元素。</p>
<h3 id="为什么使用组件"><a href="#为什么使用组件" class="headerlink" title="为什么使用组件"></a>为什么使用组件</h3><p>input是一个简单常用的搜索框，很多项目会多次使用，通常情况下，复制粘贴会更加简单，这需要了粘一个那需要了，粘一个，但当需要为这些搜索框添加一个Enter事件或添加一个类时，工作量会非常大，若是使用组件，只需要修改一次。</p>
<p>组件的目的就是<strong>让代码复用更强</strong>，通过多种组件的组合形式完整的功能，同时功能的存在不需要额外增加多余的代码。</p>
<a id="more"></a>

<h3 id="组件通信"><a href="#组件通信" class="headerlink" title="组件通信"></a>组件通信</h3><p>父组件向子组件传值</p>
<figure class="hljs highlight js"><table><tr><td class="code"><pre><span class="line">&lt;div id=<span class="string">"app"</span>&gt;</span><br><span class="line">  &lt;son v-bind:parentsmsg=<span class="string">"msg"</span>&gt;&lt;<span class="regexp">/son&gt;</span></span><br><span class="line"><span class="regexp">&lt;/</span>div&gt;</span><br><span class="line"><span class="comment">//js</span></span><br><span class="line">data:&#123;</span><br><span class="line">    msg:<span class="string">"父亲的数据"</span></span><br><span class="line">&#125;</span><br><span class="line">components:&#123;</span><br><span class="line">    son:&#123;</span><br><span class="line">        template:<span class="string">'&lt;h1&gt;子组件数据++&#123;&#123; parentsmsg&#125;&#125;&lt;/h1&gt;'</span>,</span><br><span class="line">        props:[<span class="string">'parentsmsg'</span>]</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用props传来的值可以直接使用，props的数据传输是单向的，也就是说，父组件给子组件传值，子组件只能调用不能修改。</p>
<p>如果必须要修改，可以使用以下两个方法。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//1.在组件内部定义数据</span></span><br><span class="line">props:[<span class="string">'title'</span>],</span><br><span class="line">data()&#123;</span><br><span class="line"><span class="keyword">return</span>&#123;</span><br><span class="line">  local_title:<span class="keyword">this</span>.title</span><br><span class="line">     &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//2.使用计算属性</span></span><br><span class="line">props:[<span class="string">'title'</span>],</span><br><span class="line">computed:&#123;</span><br><span class="line">    local_title:<span class="function"><span class="params">()</span>=&gt;</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.title.trim(<span class="string">``</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="子组件向父组件传值使用自定义事件"><a href="#子组件向父组件传值使用自定义事件" class="headerlink" title="子组件向父组件传值使用自定义事件"></a>子组件向父组件传值使用自定义事件</h4><figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"app1"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">        &#123;&#123; new data&#125;&#125;</span><br><span class="line">        <span class="tag">&lt;<span class="name">son</span> @<span class="attr">event1</span>=<span class="string">"change($event)"</span>&gt;</span><span class="tag">&lt;/<span class="name">son</span>&gt;</span></span><br><span class="line">        //event为</span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>js</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> son = &#123;</span><br><span class="line">    template:<span class="string">"#temp1"</span>,</span><br><span class="line">    data()&#123;</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            data1:<span class="string">'子组件中的数据'</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    methods:&#123;</span><br><span class="line">        toparents:<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.$emit(<span class="string">'event1'</span>,<span class="keyword">this</span>.data1)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//event1为要触发的事件，this.data1是传的值</span></span><br><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">    el:<span class="string">"#app"</span>,</span><br><span class="line">    data()&#123;</span><br><span class="line">        <span class="keyword">return</span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.newdata = data;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    components:&#123;</span><br><span class="line">        son</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>首先在父组件调用子组件时将函数绑定，之后在子组件中使用$emit调用函数，并且可以传参，完成子组件向父组件的通信。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>框架</tag>
        <tag>组件</tag>
      </tags>
  </entry>
  <entry>
    <title>VueRouter1</title>
    <url>/2020/07/21/VueRouter/</url>
    <content><![CDATA[<h1 id="Vue路由"><a href="#Vue路由" class="headerlink" title="Vue路由"></a>Vue路由</h1><h2 id="History模式"><a href="#History模式" class="headerlink" title="History模式"></a>History模式</h2><p><code>vue-router</code>默认是hash模式，使用这种模式后端不需要额外的处理，前端路由主要通过URL中的hash(#号)来实现不同页面之间的切换，并不会向后端发送请求，并且HTTP请求中不会包含hash相关的内容，</p>
<p>但是使用默认模式会url会出现一个#号，比较丑，所以可以使用history模式，这种模式充分利用 <code>history.pushState</code> API 来完成 URL 跳转而无须重新加载页面。</p>
<a id="more"></a>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> router = <span class="keyword">new</span> VueRouter(&#123;</span><br><span class="line">  mode: <span class="string">'history'</span>,</span><br><span class="line">  routes: [...]</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>使用了history模式URL就会像正常的url，但是使用这种方式需要在后端进行配置，Vue官方有相对于Apache服务器，nginx，Nodejs等相关配置说明。甚至于使用Express框架时有相关的中间件配置。只需引用即可</p>
<h2 id="路由的使用"><a href="#路由的使用" class="headerlink" title="路由的使用"></a>路由的使用</h2><p>1、创建路由实例，参数为一个对象，这个配置对象中有一个属性：<code>routes</code>表示<strong>路由匹配规则</strong></p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> router = <span class="keyword">new</span> Router(&#123;</span><br><span class="line">    routes : [</span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/home'</span>, <span class="attr">component</span>: Home &#125;, <span class="comment">//当地址为/home时，显示Home组件</span></span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/goods'</span>, <span class="attr">component</span>: Goods &#125;,</span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/users'</span>, <span class="attr">component</span>: Users &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>routes属性是一个数组，数组中的每一项都是匹配规则。</p>
<p>每一个规则都是一个对象，这个对象中有两个必须的属性</p>
<p><strong>path</strong>:表示监听的路由地址</p>
<p><strong>component</strong>：表示如果路由匹配到path，则展示相关的组件；</p>
<p>2、将路由规则对象router注册到vue实例中</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  el: <span class="string">"#app"</span>,</span><br><span class="line">  <span class="comment">// 将路由对象注册到vue实例</span></span><br><span class="line">  router</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>3、一旦路由规则匹配成功，就会在vue-router提供的标签<strong><code>&lt;router-view&gt;</code></strong>上显示出来，这个标签就代表匹配成功后的component组件的显示位置</p>
<h2 id="router-link属性"><a href="#router-link属性" class="headerlink" title="router-link属性"></a>router-link属性</h2><p>我们之前的超链接使用a标签，在vue-router官方为我们提供了<code>router-link</code>的属性实现链接跳转</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;p&gt;</span><br><span class="line">   &lt;!-- 使用 router-link 组件来导航. --&gt;</span><br><span class="line">   &lt;!-- 通过传入 <span class="string">`to`</span> 属性指定链接. --&gt;</span><br><span class="line">   &lt;!-- &lt;router-link&gt; 默认会被渲染成一个 `&lt;a&gt;` 标签 --&gt;</span><br><span class="line">   &lt;router-link to="/foo"&gt;Go to Foo&lt;/router-link&gt;</span><br><span class="line">   &lt;router-link to="/bar"&gt;Go to Bar&lt;/router-link&gt;</span><br><span class="line"><span class="xml"> <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br></pre></td></tr></table></figure>

<p>并且我们也可以通过tag属性，tag=”span”，将默认的a标签转换成span标签。</p>
<h2 id="编程式导航"><a href="#编程式导航" class="headerlink" title="编程式导航"></a>编程式导航</h2><p>编程式导航就是通过js来实现页面跳转（之前都是通过标签router-link来跳转的），比如如果用户登录成功跳转道首页，这个判断的功能只能通过js来实现。</p>
<p>我在app.html添加一个按钮：</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> @<span class="attr">click</span>=<span class="string">'goHome'</span>&gt;</span>跳转到首页<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后在app.js里面实现点击事件：</p>
<p>通过<code>父组件.$router.push(&#39;路由地址&#39;)</code> 或者是<code>父组件.$router.push({name:&#39;路由别名&#39;})</code>的方式进行跳转。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">methods: &#123;</span><br><span class="line">    goHome() &#123;</span><br><span class="line">        <span class="keyword">this</span>.$router.push(<span class="string">'/home'</span>);</span><br><span class="line">        <span class="keyword">this</span>.$router.push(&#123; <span class="attr">name</span>: <span class="string">'h'</span> &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<p><code>this.$router.push</code>和<code>this.$router.replace</code>的区别:</p>
<p>push会保留跳转前的地址，而replace不会。通过名字也可以看出端倪。</p>
<p>还有<code>this.$router.go(1)</code> 执行浏览指针的前进或者后退次数。</p>
<h2 id="路由嵌套"><a href="#路由嵌套" class="headerlink" title="路由嵌套"></a>路由嵌套</h2><p>我们现在想在/home路径下再接子路由a和b，形成嵌套路由/home/a或者/home/b形式的路由。</p>
<p>在路由配置规则数组中使用 <code>children</code> 属性，实现子路由功能。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> router = <span class="keyword">new</span> Router(&#123;</span><br><span class="line">    routes: [</span><br><span class="line">        <span class="comment">// 路由重定向</span></span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/'</span>, <span class="attr">redirect</span>: <span class="string">'/home'</span> &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">            path: <span class="string">'/home'</span>,</span><br><span class="line">            component: Home,</span><br><span class="line">            <span class="comment">// 使用children实现子路由</span></span><br><span class="line">            children: [</span><br><span class="line">                &#123; <span class="attr">path</span>: <span class="string">'a'</span>, <span class="attr">component</span>: A &#125;,</span><br><span class="line">                &#123; <span class="attr">path</span>: <span class="string">'b'</span>, <span class="attr">component</span>: B &#125;</span><br><span class="line">            ]</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/goods'</span>, <span class="attr">component</span>: Goods &#125;,</span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/users'</span>, <span class="attr">component</span>: Users &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><strong>注意：子路由的匹配规则前面不要加/</strong></p>
<h3 id="当路由特别长的时候我们可以给路由起别名"><a href="#当路由特别长的时候我们可以给路由起别名" class="headerlink" title="当路由特别长的时候我们可以给路由起别名"></a>当路由特别长的时候我们可以给路由起别名</h3><p>起别名的方式就是往路由匹配规则加个<code>name</code>属性即可。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> router = <span class="keyword">new</span> Router(&#123;</span><br><span class="line">    routes: [</span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/'</span>, <span class="attr">redirect</span>: <span class="string">'/home'</span> &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">            path: <span class="string">'/home'</span>,</span><br><span class="line">            component: Home,</span><br><span class="line">            name: <span class="string">'h'</span>,</span><br><span class="line">            children: [</span><br><span class="line">                &#123; <span class="attr">path</span>: <span class="string">'a'</span>, <span class="attr">component</span>: A, <span class="attr">name</span>: <span class="string">'a'</span> &#125;,</span><br><span class="line">                &#123; <span class="attr">path</span>: <span class="string">'b'</span>, <span class="attr">component</span>: B, <span class="attr">name</span>: <span class="string">'b'</span> &#125;</span><br><span class="line">            ]</span><br><span class="line">        &#125;, <span class="comment">//当地址为/home时，显示Home组件</span></span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/goods'</span>, <span class="attr">component</span>: Goods, <span class="attr">name</span>: <span class="string">'g'</span> &#125;,</span><br><span class="line">        &#123; <span class="attr">path</span>: <span class="string">'/users'</span>, <span class="attr">component</span>: Users, <span class="attr">name</span>: <span class="string">'u'</span> &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>Router</tag>
      </tags>
  </entry>
  <entry>
    <title>apply与call、bind的区别</title>
    <url>/2019/07/04/apply%E4%B8%8Ecall%E3%80%81bind%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h1 id="apply和call"><a href="#apply和call" class="headerlink" title="apply和call()"></a>apply和call()</h1><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><ul>
<li>作用都可以改变this对象的指向</li>
<li>第一个参数都是this要指向的对象(thisArg//函数执行时this的指向,args//传入的实参)</li>
</ul>
<a id="more"></a>

<h2 id="call与apply的区别"><a href="#call与apply的区别" class="headerlink" title="call与apply的区别"></a>call与apply的区别</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params">x,y</span>)</span>&#123;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"结果："</span>+(x+y)+<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line">f1(<span class="number">10</span>,<span class="number">20</span>);</span><br><span class="line">obj=&#123;</span><br><span class="line">    name=<span class="string">"小米"</span>,</span><br><span class="line">    age=<span class="number">20</span></span><br><span class="line">&#125;</span><br><span class="line">f1.apply(obj,[<span class="number">10</span>,<span class="number">20</span>]);</span><br><span class="line">f1.call(obj,<span class="number">10</span>,<span class="number">20</span>);</span><br></pre></td></tr></table></figure>

<p> <code>apply()</code>与<code>call()</code>方法都可以将实参在对象之后传递，但是<code>apply()</code>方法需要将实参封装到一个数组中统一转递，即使只有一个实参。</p>
<p>简单理解</p>
<ol>
<li><code>apply()</code>把参数打包成<code>Array</code>再传入</li>
<li><code>call()</code>把参数按顺序传入</li>
</ol>
<h2 id="第一个参数的传递"><a href="#第一个参数的传递" class="headerlink" title="第一个参数的传递"></a>第一个参数的传递</h2><ol>
<li>thisObj不传或者为null，undefined时，函数中的this会指向window对象（非严格模式）</li>
<li>传递一个别的函数名时，函数中的this将指向这个函数的引用</li>
<li>传递的值为数字、布尔、字符串时，this会指向这些基本类型的包装对象Number、Boolean、String</li>
<li>传递一个对象时，函数中的this将指向传递的这个对象。</li>
</ol>
<h3 id="call-和apply-并不在实例对象中，那么对象是如何调用的"><a href="#call-和apply-并不在实例对象中，那么对象是如何调用的" class="headerlink" title="call()和apply()并不在实例对象中，那么对象是如何调用的"></a><code>call()</code>和<code>apply()</code>并不在实例对象中，那么对象是如何调用的</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>+<span class="string">":被调用了"</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(f1.__proto__==<span class="built_in">Function</span>.prototype);<span class="comment">//true</span></span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到<code>apply()</code>和<code>call()</code>不在实例对象中，而在对象的原型中</p>
<h2 id="bind"><a href="#bind" class="headerlink" title="bind()"></a>bind()</h2><ul>
<li>可以改变this的指向</li>
<li><code>call()</code>和<code>apply()</code>在调用函数时改变</li>
<li>bind是将函数返回，相当于复制了一份。</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>Cookie与session</title>
    <url>/2020/07/13/cookie%E4%B8%8Esession/</url>
    <content><![CDATA[<h2 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h2><p>HTTP Cookie（也叫Web Cookie或浏览器Cookie）是服务器发送到用户浏览器并保存在本地的一小块数据，它会在浏览器下次向同一服务器再发起请求时被携带并发送到服务器上。通常，它用于告知服务端两个请求是否来自同一浏览器，如保持用户的登录状态。Cookie使基于<a href="https://link.zhihu.com/?target=https%3A//developer.mozilla.org/en-US/docs/Web/HTTP/Overview%23HTTP_is_stateless_but_not_sessionless" target="_blank" rel="noopener">无状态</a>的HTTP协议记录稳定的状态信息成为了可能。</p>
<a id="more"></a>

<p>客户端浏览器会将Cookie存储起来，当再次请求该网站时，浏览器将请求地址和Cookie一同交给服务器，服务器检查Cookie来辨认用户的状态。</p>
<p>如果没有Cookie，那么在我们购物时会出现这样的场景。</p>
<p>输入用户名和密码，跳转到主页<br>C：点击添加购物车<br>B：不好意思，你是谁阿？这个添加购物车的按钮，需要获得登录权限之后才能使用<br>C：于是很憋屈地再次输入用户名和密码，打算再点击添加购物车的按钮<br>B：不好意思，你是谁阿？这个添加购物车的按钮，需要获得登录权限之后才能使用<br>C：…….</p>
<h2 id="session"><a href="#session" class="headerlink" title="session"></a>session</h2><p>session是另一种记录服务器和客户端会话状态的机制。</p>
<p>当第一次请求时服务器会创建Session返回SessionID给浏览器，当第二次请求时，浏览器会将SessionID传递给服务器进行验证。</p>
<p><strong>Cookie是客户端保存的用户信息的一种机制，用来记录 用户信息，可以说它是实现Session的一种方式。</strong></p>
<h2 id="session的实现方式"><a href="#session的实现方式" class="headerlink" title="session的实现方式"></a>session的实现方式</h2><p>1、借助Cookie的sessionId发送的方式</p>
<p>2、通过LocalStorage的方式</p>
<p>3、URL回写的方式，这是客户端禁止接收Cookie之后的方法。</p>
<p>4、隐藏表单字段（hidden）</p>
<h2 id="Cookie与session的区别"><a href="#Cookie与session的区别" class="headerlink" title="Cookie与session的区别"></a>Cookie与session的区别</h2><ul>
<li><strong>安全性：</strong> Session 比 Cookie 安全，Session 是存储在服务器端的，Cookie 是存储在客户端的。</li>
<li><strong>存取值的类型不同</strong>：Cookie 只支持存字符串数据，想要设置其他类型的数据，需要将其转换成字符串，Session 可以存任意数据类型。</li>
<li><strong>有效期不同：</strong> Cookie 可设置为长时间保持，比如我们经常使用的默认登录功能，Session 一般失效时间较短，客户端关闭（默认情况下）或者 Session 超时都会失效。</li>
<li><strong>存储大小不同：</strong> 单个 Cookie 保存的数据不能超过 4K，Session 可存储数据远高于 Cookie，但是当访问量过多，会占用过多的服务器资源。</li>
</ul>
]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>Cookie</tag>
        <tag>session</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS3布局模式</title>
    <url>/2019/04/16/css%E5%B8%83%E5%B1%80%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="CSS3新布局模式"><a href="#CSS3新布局模式" class="headerlink" title="CSS3新布局模式"></a>CSS3新布局模式</h1><h1 id="一、多列布局"><a href="#一、多列布局" class="headerlink" title="一、多列布局"></a>一、多列布局</h1><p>适用：</p>
<ol>
<li>针对文字排版</li>
<li>针对瀑布流</li>
</ol>
<p>补充：(横向布局方法)</p>
<ol>
<li>display:table/table-row/table-cell<br>以表格特征显示/以表格行特征显示/以单元格特征显示</li>
<li>min-width/min-height:最小宽度/最小高度</li>
<li>display:inline-block;</li>
<li>浮动方式:比较复杂，不便于文字排版维护<br>涉及属性：</li>
</ol>
<p><strong>1.column-width:每一列的列宽</strong><br><strong>2.column-count:强调列数，可单独使用，指定劣势，列宽自动分配。</strong></p>
<a id="more"></a>

<p>==note：当与其他属性配合使用时，其不再代表具体的值，而代表一个范围，如下：向下取整==<br>if((列宽<em>列数)&lt;(可用区域-间隔)){<br>按指定列数显示<br>按指定间隔显示<br>列宽自动分配<br>}else{<br>按指定列宽“可用”最大列数（列数小于指定列数）显示，列宽值自动分配，且&gt;=指定列宽<br>}<br>*</em>3.column-gap:列间隔**<br><strong>4.column-rule:间隔线，使用同：border</strong><br><strong>5.column-span:跨列显示，值为all，该属性火狐不支持</strong><br><strong>6.column-fill:auto/balance,但所有浏览器暂不支持</strong><br>http:// dummy images.com</p>
<h1 id="二、伸缩盒布局"><a href="#二、伸缩盒布局" class="headerlink" title="二、伸缩盒布局"></a>二、伸缩盒布局</h1><p>作用：解决子盒子的位置关系与规格计算，合理利用剩余空间</p>
<p>设置方法：display:box;</p>
<figure class="hljs highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#box</span>&#123;</span><br><span class="line"><span class="attribute">display</span>:-webkit-box;</span><br><span class="line"><span class="attribute">display</span>:-moz-box;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>note:</strong></p>
<ol>
<li>强调私有属性值写法:display:-webkit-box;</li>
<li>块元素默认横向排列，且宽度丢失，高度参照外层容器；</li>
<li>兼容性中display的兼容代码在冒号后面</li>
</ol>
<h2 id="1-显示方向控制"><a href="#1-显示方向控制" class="headerlink" title="1.显示方向控制:"></a>1.显示方向控制:</h2><p>==box-orient:horizeontal(横向) / vertical(纵向)要加前缀==</p>
<figure class="hljs highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#box</span>&#123;</span><br><span class="line"><span class="attribute">-webkit-box-orient</span>:vertical;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-宽度控制："><a href="#2-宽度控制：" class="headerlink" title="2.宽度控制："></a>2.宽度控制：</h2><p>如果不设宽，以最小内容宽或最大父层宽为参照;</p>
<h3 id="弹性布局："><a href="#弹性布局：" class="headerlink" title="弹性布局："></a>弹性布局：</h3><p>==为子盒子指定box-flex,值至少为1，==可具备自动伸缩功能，瓜分伸缩容器剩余空间；</p>
<figure class="hljs highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">-webkit-box-flex</span>: 1;</span><br><span class="line"><span class="selector-tag">-moz-box-flex</span><span class="selector-pseudo">:1</span>;</span><br></pre></td></tr></table></figure>

<p>如有内容，按内容多少决定；</p>
<h3 id="问题1如果忽略内容，强制按比例分配？"><a href="#问题1如果忽略内容，强制按比例分配？" class="headerlink" title="问题1如果忽略内容，强制按比例分配？"></a>问题1如果忽略内容，强制按比例分配？</h3><p>为子盒子指定width：0；</p>
<p>默认状态下：<br>子盒大小=上层大小*当前子盒的box-flex值/所有子盒box-flex之和</p>
<h3 id="问题2：弹性与固定混用"><a href="#问题2：弹性与固定混用" class="headerlink" title="问题2：弹性与固定混用"></a>问题2：弹性与固定混用</h3><p>子盒宽=width+（父层宽-width）/所有子盒box-flex之和*自身flex值</p>
<h2 id="3-高度控制"><a href="#3-高度控制" class="headerlink" title="3.高度控制"></a>3.高度控制</h2><ol>
<li>默认以内容高为准</li>
<li>box-flex可实现比例分配</li>
<li>固定与比例混用</li>
<li>如果使用侧轴对齐时，高度变为最小高度<h2 id="4-顺序控制："><a href="#4-顺序控制：" class="headerlink" title="4.顺序控制："></a>4.顺序控制：</h2>box-ordinal-group:后面跟的数值；数值越小越优先进行显示，不接受负值。<br>优点：不破坏html结构<br>box-direction:强调所有子元素正序或倒序显示。使用外层容器。</li>
</ol>
<figure class="hljs highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">-webkit-box-direction</span>:</span><br></pre></td></tr></table></figure>

<h2 id="5-对齐控制："><a href="#5-对齐控制：" class="headerlink" title="5.对齐控制："></a>5.对齐控制：</h2><p>（横）box-pack：start起点对齐；/end终点对齐/center/居中对齐/justify<br>（纵）box-align：start/end/center/baseline基线对齐/stretch纵向伸展对齐<br>baseline:基线对齐，是子元素第一行内容的底部；<br>stretch:尽可能与外层容器等高；</p>
<h1 id="三、弹性盒布局"><a href="#三、弹性盒布局" class="headerlink" title="三、弹性盒布局"></a>三、弹性盒布局</h1><p>特性：<br>是CSS3中BOX升级后的布局，display:box–flexbox–flex</p>
<ol>
<li>组成：一个伸缩容器与多个伸缩项目（子元素）</li>
<li>定义方法：display:flex; display:-webkit-flex；  display：flex；</li>
<li>android:支持box</li>
<li>safir：box flex</li>
<li>moz：flex<br>声明后特性：columns/floa/clear/vertical-aligh均无效果<br>项数量：任意；<br>影响：对容器外和项目内均无影响；<br>行个数：只能有一个；<br>显示顺序：左到右上到下；<h3 id="box、flex与column区别？"><a href="#box、flex与column区别？" class="headerlink" title="box、flex与column区别？"></a>box、flex与column区别？</h3>column：列宽统一，列间统一；没有对齐属性<br>box/flex:列宽自定义，间距可调，对齐属性全面<h2 id="3-定义伸缩方向"><a href="#3-定义伸缩方向" class="headerlink" title="3.定义伸缩方向"></a>3.定义伸缩方向</h2>作用：创建主轴<br>属性及值：flex-direction：row 横向；/row-reverse 横逆向/ column：纵向   /   column-reverse：纵逆向<br>与box对应关系<br>flex-direction==box-orient+box-direction<h2 id="4-定义行数（是否换行及换行方式）"><a href="#4-定义行数（是否换行及换行方式）" class="headerlink" title="4.定义行数（是否换行及换行方式）"></a>4.定义行数（是否换行及换行方式）</h2>（box下只有单行）<br>属性及值：flex-wrap<br>nowrap:默认值，强制不换行<br>wrap-reverse：反向换行<br>note：简写</li>
</ol>
<p><strong>flex-flow：方向     换行；例：flex-flow：row-reverse  wrap-reverse；</strong></p>
<h2 id="5-定义对齐方式"><a href="#5-定义对齐方式" class="headerlink" title="5.定义对齐方式"></a>5.定义对齐方式</h2><p>适用对象与作用：适用于伸缩容器，用于在多余空间上的位置分配。</p>
<ol>
<li>主轴对齐(水平对齐)：justifiy-content：flex-start/flex-end/center/space-between/space-around；<br>参数说明如下：<br>flex-start<br>弹性项目向行头紧挨着填充。这个是默认值。第一个弹性项的main-start外边距边线被放置在该行的main-start边线，而后续弹性项依次平齐摆放。<br>flex-end<br>弹性项目向行尾紧挨着填充。第一个弹性项的main-end外边距边线被放置在该行的main-end边线，而后续弹性项依次平齐摆放。<br>center<br>弹性项目居中紧挨着填充。（如果剩余的自由空间是负的，则弹性项目将在两个方向上同时溢出）。<br>space-between<br>弹性项目平均分布在该行上。如果剩余空间为负或者只有一个弹性项，则该值等同于flex-start。否则，第1个弹性项的外边距和行的main-start边线对齐，而最后1个弹性项的外边距和行的main-end边线对齐，然后剩余的弹性项分布在该行上，相邻项目的间隔相等。<br>space-around<br>弹性项目平均分布在该行上，两边留有一半的间隔空间。如果剩余空间为负或者只有一个弹性项，则该值等同于center。否则，弹性项目沿该行分布，且彼此间隔相等（比如是20px），同时首尾两边和弹性容器之间留有一半的间隔（1/2*20px=10px）</li>
<li>侧轴对齐（垂直对齐）：align-items：flex-end/flex-strat/center/baseline/stretch</li>
<li>伸缩行对齐（多行元素的垂直对齐关系）：</li>
<li>align-content:flex-start/flx-end/center/stretch/space-between/space-around<br>———以下属性为单独项目的控制属性<h2 id="6-定义伸缩项目"><a href="#6-定义伸缩项目" class="headerlink" title="6.定义伸缩项目"></a>6.定义伸缩项目</h2>适用对象：适用于伸缩项目（子元素）</li>
<li>显示位置：oreder：无单位属性值 =box-ordinal-group <strong>order：1//第一个显示</strong></li>
<li>伸缩空间（如何瓜分）：flex-grow：1无单位取值，强调份数，</li>
<li>收缩空间（如何压缩）：flex-shrink:1</li>
<li>伸缩基准值：flex-basis:100px(数值加单位)该属性与宽度冲突，会覆盖原有宽度值。所有缩放动作以该属性值为基础。与先后顺序无关。</li>
<li>对齐方式（垂直方向）：对象为单独子元素<br>align-self<h1 id="四、媒体查询"><a href="#四、媒体查询" class="headerlink" title="四、媒体查询"></a>四、媒体查询</h1>重点：</li>
<li>了解CSS3设备类型</li>
<li>如何使用媒体查询规则</li>
<li>设计并制作响应式页面<h2 id="一、了解媒体查询"><a href="#一、了解媒体查询" class="headerlink" title="一、了解媒体查询"></a>一、了解媒体查询</h2></li>
<li>CSS2时代：媒体类型(media type)<br>作用：针对指定媒体类型进行css开发<br>note：常用媒体类型<br>all 所有设备   screen  电脑屏幕  tv     电视    projection   投影设备     print  打印设备<br>handheld   手持设备        braille   触摸设备    speech   语音设备<h2 id="CSS3时代：针对不同媒体，精确匹配条件自动附加相关样式-medai-queries"><a href="#CSS3时代：针对不同媒体，精确匹配条件自动附加相关样式-medai-queries" class="headerlink" title="CSS3时代：针对不同媒体，精确匹配条件自动附加相关样式(medai queries)"></a>CSS3时代：针对不同媒体，精确匹配条件自动附加相关样式(medai queries)</h2>@media screen and (min-width:800px){}<h2 id="二、媒体查询规则"><a href="#二、媒体查询规则" class="headerlink" title="二、媒体查询规则"></a>二、媒体查询规则</h2>使用方法：<br>@media [not|only] 媒体类型 [and 查询规则…]{CSS代码}<h2 id="note："><a href="#note：" class="headerlink" title="note："></a>note：</h2></li>
<li>not and (only)</li>
<li>部分接受min 与max，代表大于等于或小于等于，避免使用&amp;lt；&amp;gt；</li>
<li>not 只能针对它所在的整个查询进行取反</li>
<li>media语句中包含一种媒体类型，如果媒体类型没有指定，则<h2 id="常用媒体规则："><a href="#常用媒体规则：" class="headerlink" title="常用媒体规则："></a>常用媒体规则：</h2>width：显示宽<br>height：显示高<br>device-width：设备宽度<br>deviece-height：设备高度<br>orientation：横屏|纵屏  （landspace| protrait）<br>color：彩色设备<br>aspect-ratio：显示宽高比<br>device-asepect-ratio：设备宽高比<br>resolution：分辨率</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>jQuery03选择器</title>
    <url>/2020/07/15/jQuery03%E9%80%89%E6%8B%A9%E5%99%A8/</url>
    <content><![CDATA[<h2 id="jQuery中的选择器"><a href="#jQuery中的选择器" class="headerlink" title="jQuery中的选择器"></a>jQuery中的选择器</h2><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">id选择器   $(<span class="string">"#dev"</span>);</span><br><span class="line">标签选择器  $(<span class="string">"p"</span>);</span><br><span class="line">类选择器    $(<span class="string">".cls"</span>);</span><br><span class="line"><span class="comment">//层次选择器</span></span><br><span class="line">$(<span class="string">"#dv span"</span>);  <span class="comment">//div这个父级元素中所有的span标签</span></span><br><span class="line">$(<span class="string">"#dv&gt;span"</span>)   <span class="comment">//div这个父级元素的所有**直接子元素**span标签</span></span><br><span class="line">$(<span class="string">"#dv~span"</span>)   <span class="comment">//div这个父级元素后面的兄弟元素</span></span><br></pre></td></tr></table></figure>

<p>原生方式封装$</p>
<a id="more"></a>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $ = <span class="function"><span class="keyword">function</span>(<span class="params">selector</span>)</span>&#123;</span><br><span class="line">    <span class="comment">//传入的选择器通过querySelector查找到赋值给domList</span></span><br><span class="line">    <span class="keyword">var</span> domList = <span class="built_in">document</span>.querySelectorAll(selector);</span><br><span class="line">    <span class="comment">//将选择器domList进行遍历</span></span><br><span class="line">    domList.__proto__.html = <span class="function"><span class="keyword">function</span>(<span class="params">value</span>)</span>&#123;</span><br><span class="line">        domList.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item,i</span>)</span>&#123;</span><br><span class="line">            item.innerHTML = value;</span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> domList;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="基本过滤选择器"><a href="#基本过滤选择器" class="headerlink" title="基本过滤选择器"></a>基本过滤选择器</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// :odd奇数选择</span></span><br><span class="line">$(<span class="string">"li:odd"</span>).css(<span class="string">"background"</span>, <span class="string">"red"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :even偶数选择</span></span><br><span class="line">$(<span class="string">"li:even"</span>).css(<span class="string">"background"</span>, <span class="string">"green"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :eq(index)索引选择器</span></span><br><span class="line">$(<span class="string">"ul li:eq(3)"</span>).css(<span class="string">"font-size"</span>, <span class="string">"30px"</span>);<span class="comment">//设置第四个li的字体</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// :lt(index)小于索引</span></span><br><span class="line">$(<span class="string">"li:lt(6)"</span>).css(<span class="string">"font-size"</span>, <span class="string">"30px"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :gt(index)大于该索引</span></span><br><span class="line">$(<span class="string">".ulList1 li:gt(7)"</span>).css(<span class="string">"font-size"</span>, <span class="string">"40px"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :first第一个</span></span><br><span class="line">$(<span class="string">".ulList li:first"</span>).css(<span class="string">"font-size"</span>, <span class="string">"40px"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// :last最后一个</span></span><br><span class="line">$(<span class="string">"li:last"</span>).css(<span class="string">"font-size"</span>, <span class="string">"40px"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="筛选选择器"><a href="#筛选选择器" class="headerlink" title="筛选选择器"></a>筛选选择器</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">.prev();      该元素前一个兄弟元素</span><br><span class="line">.prevAll();   该元素前面所有兄弟元素</span><br><span class="line">.next();      该元素后一个兄弟元素</span><br><span class="line">.nextAll();   该元素后面所有兄弟元素</span><br><span class="line">.parnet();    该元素的父级元素</span><br><span class="line">.find();      后代寻找选择器（要指定参数）例：</span><br><span class="line">jqul.find(<span class="string">"li"</span>).css(<span class="string">"background"</span>, <span class="string">"pink"</span>);</span><br><span class="line">.sibling();   该元素所有兄弟元素</span><br></pre></td></tr></table></figure>

<h3 id="通过jQuery选择器来实现一个tab切换组件"><a href="#通过jQuery选择器来实现一个tab切换组件" class="headerlink" title="通过jQuery选择器来实现一个tab切换组件"></a>通过jQuery选择器来实现一个tab切换组件</h3><img src="/2020/07/15/jQuery03选择器/1.png">

<p>点击不同按钮切换显示不同的内容：</p>
<p>设置三个内容隐藏，当点击对应按钮时为按钮和对应的内容添加active样式。</p>
<img src="/2020/07/15/jQuery03选择器/2.png">

<p>使用jquery仅需两行代码即可实现，选中元素样式的添加和兄弟元素的样式删除。</p>
<h2 id="jquery的样式操作"><a href="#jquery的样式操作" class="headerlink" title="jquery的样式操作"></a>jquery的样式操作</h2><figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">//设置在元素属性上，优先级高</span><br><span class="line">    $(&apos;h1&apos;).css(&apos;background&apos;,&apos;skyblue&apos;);</span><br><span class="line">    //一次性设置多个样式,传入一个对象。</span><br><span class="line">    $(&apos;h1&apos;).css(&#123;</span><br><span class="line">        color:&apos;red&apos;,</span><br><span class="line">        border:&apos;3px solid #ccc&apos;,</span><br><span class="line">        borderRadius:&apos;10px&apos;//使用驼峰命名方式，如果要使用border-radius方式需加引号</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<p>通过.css()方法设置元素的样式</p>
<figure class="hljs highlight js"><table><tr><td class="code"><pre><span class="line">  &lt;style&gt;</span><br><span class="line">        .d1&#123;</span><br><span class="line">            width: <span class="number">400</span>px;background-color: skyblue;height: <span class="number">400</span>px;opacity: <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        .d1.active&#123;<span class="attr">opacity</span>: <span class="number">1</span>&#125;</span><br><span class="line">    &lt;<span class="regexp">/style&gt;</span></span><br><span class="line"><span class="regexp">    &lt;script src="jquery-1.12.1.min.js"&gt;&lt;/</span>script&gt;</span><br><span class="line">&lt;<span class="regexp">/head&gt;</span></span><br><span class="line"><span class="regexp">&lt;body&gt;</span></span><br><span class="line"><span class="regexp">&lt;div class="d1"&gt;&lt;/</span>div&gt;</span><br><span class="line">&lt;button&gt;切换显示d1&lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp">&lt;script&gt;</span></span><br><span class="line"><span class="regexp">    $('button').click(function () &#123;</span></span><br><span class="line"><span class="regexp">        if($('.d1').hasClass('active'))&#123;</span></span><br><span class="line"><span class="regexp">            $('.d1').removeClass('active')</span></span><br><span class="line"><span class="regexp">        &#125;else&#123;</span></span><br><span class="line"><span class="regexp">            $('.d1').addClass('active')</span></span><br><span class="line"><span class="regexp">        &#125;</span></span><br><span class="line"><span class="regexp">        /</span><span class="regexp">/ $('.d1').toggleClass('active')</span></span><br><span class="line"><span class="regexp">    &#125;)</span></span><br></pre></td></tr></table></figure>

<p>.hasClass()判断元素是否含有此样式</p>
<p>.addClass()为元素增加类样式</p>
<p>.removeClass()为元素删除类样式</p>
<p>.toggleClass()当元素有此样式时删除，没有时增加</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JQuery</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>jQuery</tag>
        <tag>选择器</tag>
        <tag>样式</tag>
      </tags>
  </entry>
  <entry>
    <title>jquery04操作DOM</title>
    <url>/2020/07/18/jquery04%E6%93%8D%E4%BD%9CDOM/</url>
    <content><![CDATA[<h1 id="DOM操作的分三个方面"><a href="#DOM操作的分三个方面" class="headerlink" title="DOM操作的分三个方面"></a>DOM操作的分三个方面</h1><p>DOM Core（核心）、HTML-DOM和CSS-DOM</p>
<ol>
<li><p><strong>DOM Core</strong>JavaScript中的getElementById()、getElementsByTagName()、getAttribute()、setAttribute()等方法，都是DOM Core的组成部分。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用DOM Core获取表单：</span></span><br><span class="line"><span class="built_in">document</span>.getElementsByTagName(<span class="string">'form'</span>);</span><br><span class="line"><span class="comment">//使用DOM Core获取某元素的src属性方法：</span></span><br><span class="line">element.getAttribute(<span class="string">'src'</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>HTML-DOM</strong>提供一些简明的记号来描述各种HTML元素的属性</p>
<a id="more"></a>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用HTML-DOM来获取表单对象的方法：</span></span><br><span class="line"><span class="built_in">document</span>.forms;</span><br><span class="line"><span class="comment">//使用HTML-DOM来获取某元素的src属性的方法：</span></span><br><span class="line">element.src</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>CSS-DOM</strong>是针对CSS的操作，主要是获取和设置style对象的各种属性</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//设置某元素style对象字体颜色的方法；</span></span><br><span class="line">element.style.color = <span class="string">"red"</span></span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="jquery中的DOM操作"><a href="#jquery中的DOM操作" class="headerlink" title="jquery中的DOM操作"></a>jquery中的DOM操作</h2><h3 id="查找节点"><a href="#查找节点" class="headerlink" title="查找节点"></a>查找节点</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//查找元素节点</span></span><br><span class="line"><span class="keyword">var</span> $li = $(<span class="string">'ul li'</span>);<span class="comment">//查找li;</span></span><br><span class="line"><span class="comment">//查找属性节点,利用选择器找到元素后，通过attr()方法来获取他的各种属性的值。</span></span><br><span class="line"><span class="keyword">var</span> p_txt = $(<span class="string">'p'</span>).attr(<span class="string">'title'</span>);</span><br><span class="line"><span class="comment">//attr();接收两个参数，一个参数为获取属性，两个参数为设置属性</span></span><br></pre></td></tr></table></figure>

<h3 id="创建节点-amp-插入节点"><a href="#创建节点-amp-插入节点" class="headerlink" title="创建节点&amp;插入节点"></a>创建节点&amp;插入节点</h3><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//通过设置html来创建内容(推荐)</span></span><br><span class="line">    $(<span class="string">'div'</span>).html(<span class="string">'&lt;h2&gt;hello  world&lt;/h2&gt;'</span>);</span><br><span class="line">    <span class="comment">//append追加</span></span><br><span class="line">    $(<span class="string">'div'</span>).append(h1);</span><br><span class="line">    <span class="comment">//写法与append相反</span></span><br><span class="line">    h3.appendTo(<span class="string">'div'</span>);</span><br><span class="line">    $(<span class="string">'div'</span>).prepend(<span class="string">'&lt;h4&gt;前面追加&lt;/h4&gt;'</span>);</span><br><span class="line"><span class="comment">//在元素的后面插入节点</span></span><br><span class="line">    $(<span class="string">'div'</span>).after(<span class="string">'&lt;p&gt;123&lt;/p&gt;'</span>);</span><br></pre></td></tr></table></figure>

<h3 id="使用删除节点"><a href="#使用删除节点" class="headerlink" title="使用删除节点"></a>使用删除节点</h3><p>1、<strong>remove()</strong>当某个节点用remove()方法删除后，该节点所包含的所有后代节点将同时被删除。这个方法的返回值是一个指向已被删除的节点的引用，因此可以在以后再使用这些元素。</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//删除元素</span></span><br><span class="line">   <span class="keyword">var</span> $(<span class="string">'li'</span>) = $(<span class="string">'ul li:eq(1)'</span>).remove();将ul中的第二个li删除</span><br><span class="line">   $(<span class="string">'li'</span>).appendTo(<span class="string">"ul"</span>);<span class="comment">//再将删除的元素重新插入</span></span><br></pre></td></tr></table></figure>

<p>2、<strong>detach()</strong>方法和remove()方法类似，但是所有绑定的事件、附加的数据都会保留下来</p>
<p>3、<strong>empty()方法</strong>不是删除节点，而是清空节点</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">$(&apos;div&apos;).empty();//清空div内所有节点</span><br></pre></td></tr></table></figure>

<h3 id="替换节点"><a href="#替换节点" class="headerlink" title="替换节点"></a>替换节点</h3><p>使用replaceWith()和replaceAll()将匹配的元素替换成指定的元素</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="string">"p"</span>).replaceWith(<span class="string">"&lt;strong&gt;你喜欢的&lt;/strong&gt;"</span>);</span><br><span class="line">$(<span class="string">"&lt;strong&gt;你喜欢的&lt;/strong&gt;"</span>).replaceAll(<span class="string">"p"</span>)</span><br></pre></td></tr></table></figure>

<h3 id="获取属性和设置属性"><a href="#获取属性和设置属性" class="headerlink" title="获取属性和设置属性"></a>获取属性和设置属性</h3><p> 想要替换p的title属性</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">$(&quot;p&quot;).attr(&quot;title&quot;,&quot;change title&quot;)</span><br></pre></td></tr></table></figure>

<h3 id="删除属性"><a href="#删除属性" class="headerlink" title="删除属性"></a>删除属性</h3><p>removeAttr()删除元素的属性</p>
<h2 id="设置和获取HTML、文本和值"><a href="#设置和获取HTML、文本和值" class="headerlink" title="设置和获取HTML、文本和值"></a>设置和获取HTML、文本和值</h2><ol>
<li>.html()类似JavaScript中的innerHTML属性，可以用来读取或者设置某个元素中的HTML内容。</li>
<li>.text()类似innerText属性</li>
<li>val()方法</li>
</ol>
]]></content>
      <categories>
        <category>前端</category>
        <category>JQuery</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>jQuery</tag>
        <tag>DOM</tag>
      </tags>
  </entry>
  <entry>
    <title>js创建对象的几种方式</title>
    <url>/2019/03/13/js%E5%88%9B%E5%BB%BA%E5%AF%B9%E8%B1%A1%E7%9A%84%E5%87%A0%E7%A7%8D%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<h2 id="字面量方式"><a href="#字面量方式" class="headerlink" title="字面量方式"></a>字面量方式</h2><a id="more"></a>

<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">var obj=&#123;&#125;;</span><br><span class="line"></span><br><span class="line">obj.name=&quot;小黑&quot;;</span><br><span class="line"></span><br><span class="line">obj.age=10;</span><br><span class="line"></span><br><span class="line">//优化：</span><br><span class="line"></span><br><span class="line">var obj2=&#123;</span><br><span class="line"></span><br><span class="line">    name:&quot;小小&quot;,</span><br><span class="line"></span><br><span class="line">    age:10,</span><br><span class="line"></span><br><span class="line">    sayHi:function()&#123;</span><br><span class="line"></span><br><span class="line">        console.log(&quot;hello&quot;);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line">//缺陷：一次性对象，更改属性需要修改源代码</span><br></pre></td></tr></table></figure>

<h2 id="构造函数方式创建"><a href="#构造函数方式创建" class="headerlink" title="构造函数方式创建"></a>构造函数方式创建</h2><figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">var smallDog = new Object();</span><br><span class="line"></span><br><span class="line">            smallDog.eat = &quot;吃骨头&quot;;</span><br><span class="line"></span><br><span class="line">            smallDog.walk = function () &#123;</span><br><span class="line"></span><br><span class="line">                console.log(this.name+&quot;不想走路&quot;);</span><br><span class="line"></span><br><span class="line">            &#125;</span><br></pre></td></tr></table></figure>

<h2 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h2><figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">function createObject(name,age) &#123;</span><br><span class="line"></span><br><span class="line">            var o = new Object();</span><br><span class="line"></span><br><span class="line">            o.name = name;</span><br><span class="line"></span><br><span class="line">            o.age = age;</span><br><span class="line"></span><br><span class="line">            o.sayName = function () &#123;</span><br><span class="line"></span><br><span class="line">                alert(&quot;this.name&quot;);</span><br><span class="line"></span><br><span class="line">            &#125;;</span><br><span class="line"></span><br><span class="line">            return o;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"></span><br><span class="line">            var person1 = createObject(&quot;小刘&quot;,12);</span><br></pre></td></tr></table></figure>

<p>函数createPerson()能够根据接受的参数来构建一个包含所有必要信息的Person对象，可以无数次的调用这个函数，而它每次都会返回一个包含两个属性一个方法的对象，虽然解决了创建多个相似对象的问题，但仍没有解决对象的识别问题。</p>
<h2 id="自定义构造函数模式"><a href="#自定义构造函数模式" class="headerlink" title="自定义构造函数模式"></a>自定义构造函数模式</h2><figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">function Person(name,age) &#123;</span><br><span class="line"></span><br><span class="line">            this.name = name;</span><br><span class="line"></span><br><span class="line">            this.age = age;</span><br><span class="line">            this.sayName = sayName;</span><br><span class="line">            </span><br><span class="line">        &#125;</span><br><span class="line">function sayName()&#123;</span><br><span class="line">            alert(this.name);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        var obj=new Person(&quot;小刘&quot;,20);</span><br></pre></td></tr></table></figure>

<p>构造函数首字母要大写，这样其他程序员一看就会明白这是构造函数是创建对象的。</p>
<p><strong>同时我将sayName函数放到了构造函数外部,而在内部将属性设置成等于全局变量的sayName函数，这样解决了两个函数做同一件事的问题，但新的问题是如果对象需要定义很多方法，那么就要定义很多个全局函数，这样这个自定义函数的引用类型就丝毫没有封装性可言。这些问题可以通过原型模式来解决。</strong></p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JS</category>
      </categories>
      <tags>
        <tag>javascript</tag>
      </tags>
  </entry>
  <entry>
    <title>vue后台管理系统</title>
    <url>/2020/07/16/vue%E5%90%8E%E5%8F%B0%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="使用VueElementUI-thinkJS搭建后台管理系统"><a href="#使用VueElementUI-thinkJS搭建后台管理系统" class="headerlink" title="使用VueElementUI+thinkJS搭建后台管理系统"></a>使用VueElementUI+thinkJS搭建后台管理系统</h2><p>后台的核心就是提供数据，封装操作数据库的接口提供给前端调用。使用了Vue-element-admin的模板加thinkJS+Mysql快速的搭建起项目,</p>
<p>主要练习前端登录验证，数据列表，获取用户信息和elementUi组件。</p>
<a id="more"></a>

<h3 id="前端的登录过程"><a href="#前端的登录过程" class="headerlink" title="前端的登录过程"></a>前端的登录过程</h3><h4 id="首先在后台创建接口来处理前端的登录的相关请求。"><a href="#首先在后台创建接口来处理前端的登录的相关请求。" class="headerlink" title="首先在后台创建接口来处理前端的登录的相关请求。"></a>首先在后台创建接口来处理前端的登录的相关请求。</h4><p>1、创建登录接口</p>
<img src="/2020/07/16/vue后台管理系统/1.png">

<p>2、使用JWT保持用户的登录状态，可以实现跨域的身份验证</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//在config中配置jwt的加密和cookie</span></span><br><span class="line">jwt:&#123;</span><br><span class="line">    secret:<span class="string">"passwordss"</span>,</span><br><span class="line">    cookie:<span class="string">'jwt-token'</span>,<span class="comment">//cookie中字段的设置</span></span><br><span class="line">    expire:<span class="number">3000</span><span class="comment">//秒，口令的有效时间</span></span><br><span class="line">  &#125;,</span><br></pre></td></tr></table></figure>

<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">checkAuth()&#123;</span><br><span class="line">    <span class="keyword">let</span> token = <span class="keyword">this</span>.ctx.headers[<span class="string">'x-token'</span>];</span><br><span class="line">    <span class="comment">//sercet加密，exipre时间，this.config('jwt')获取config中的jwt配置</span></span><br><span class="line">    <span class="keyword">const</span> &#123;secret,cookie,exipre&#125; = <span class="keyword">this</span>.config(<span class="string">'jwt'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">var</span> tokenObj = token ? jsonwebtoken.verify(token,secret):&#123;&#125;;</span><br><span class="line">      <span class="keyword">this</span>.ctx.state.username = tokenObj.name;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">this</span>.authFail()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!tokenObj.name)&#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">this</span>.authFail()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.updateAuth(token.name)</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">//更新token</span></span><br><span class="line">  updateAuth(userName)&#123;</span><br><span class="line">    <span class="keyword">const</span> userInfo = &#123;</span><br><span class="line">      name:userName</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">// 获取jwt的配置信息</span></span><br><span class="line">    <span class="keyword">const</span> &#123;secret,cookie,expire&#125; = <span class="keyword">this</span>.config(<span class="string">'jwt'</span>)</span><br><span class="line">    <span class="keyword">const</span> token = jsonwebtoken.sign(userInfo,secret,&#123;<span class="attr">expiresIn</span>:expire&#125;);</span><br><span class="line">    <span class="keyword">this</span>.cookie(cookie,token);</span><br><span class="line"> <span class="comment">//客户端收到服务器返回的 JWT，可以储存在 Cookie 里面，也可以储存在 localStorage。</span></span><br><span class="line"><span class="comment">//此后，客户端每次与服务器通信，都要带上这个 JWT。你可以把它放在 Cookie 里面自动发送，但是这样不能跨域，所以更好的做法是放在 HTTP 请求的头信息Authorization字段里面。</span></span><br><span class="line">    <span class="keyword">this</span>.header(<span class="string">'authoriztion'</span>,token);</span><br><span class="line">    <span class="keyword">return</span> token;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 失败处理</span></span><br><span class="line">  authFail()&#123;</span><br><span class="line">    <span class="keyword">this</span>.json(&#123;<span class="attr">error</span>:<span class="string">"JWT校验失败"</span>&#125;);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>随后在中间件中引入jwt</p>
<h5 id="JWT原理"><a href="#JWT原理" class="headerlink" title="JWT原理"></a>JWT原理</h5><p>JWT的原理是，服务器认证以后生成一个JSON对象，发回给用户，</p>
<figure class="hljs highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"姓名"</span>: <span class="string">"小刘"</span>,</span><br><span class="line">    <span class="attr">"角色"</span>: <span class="string">"管理员"</span>,</span><br><span class="line">    <span class="attr">"到期时间"</span>: <span class="string">"2020年8月1日0点0分"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>以后，用户与服务端通信的时候，都要发回这个 JSON 对象。服务器完全只靠这个对象认定用户身份。为了防止用户篡改数据，服务器在生成这个对象的时候，会加上签名；</p>
<p>JWT的三个部分依次如下。</p>
<p>Header(头部)、Payload(负载)、Signature(签名)</p>
<p>Header.payload.Signature</p>
<h5 id="JWT的使用方式"><a href="#JWT的使用方式" class="headerlink" title="JWT的使用方式"></a>JWT的使用方式</h5><p>客户端收到服务器返回的JWT，可以存储在Cookie里面，也可以存储在localStorage。</p>
<p>此后，客户端每次与服务器通信，都要带上这个 JWT。你可以把它放在 Cookie 里面自动发送，但是这样不能跨域，所以更好的做法是放在 HTTP 请求的头信息<code>Authorization</code>字段里面。</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">Authorization:Bearer&lt;token&gt;</span><br></pre></td></tr></table></figure>

<p>另一种方式是，在跨域的时候，JWT就放在POST请求的数据体里面</p>
<h4 id="进入前端的登录逻辑"><a href="#进入前端的登录逻辑" class="headerlink" title="进入前端的登录逻辑"></a>进入前端的登录逻辑</h4><p>1、在login.vue中修改路由并在router中修改路由，在根目录时跳转到login页</p>
<p>2、前端中封装axios请求，同时设置baseurl,并设置跨域</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">baseURL: process.env.VUE_APP_BASE_API, <span class="comment">// url = base url + request url</span></span><br><span class="line"><span class="comment">//在.env.development中将base设置</span></span><br><span class="line">VUE_APP_BASE_API = <span class="string">'/api'</span></span><br><span class="line"><span class="comment">//在vue.config.js中设置代理服务器</span></span><br><span class="line">proxy:&#123;</span><br><span class="line">      <span class="string">'/api'</span>:&#123;</span><br><span class="line">        target:<span class="string">'http://127.0.0.1:8360'</span>,</span><br><span class="line">        pathRewrite:&#123;</span><br><span class="line">          <span class="string">'^/api'</span>:<span class="string">""</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>3、编写登录请求</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">login</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> request(&#123;</span><br><span class="line">    url: <span class="string">'/user/login/login'</span>,</span><br><span class="line">    method: <span class="string">'post'</span>,</span><br><span class="line">    data</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">getInfo</span>(<span class="params">token</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> request(&#123;</span><br><span class="line">    url: <span class="string">'/admin/index/userInfo'</span>,</span><br><span class="line">    method: <span class="string">'get'</span></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>4、Vuex编写获取用户信息函数在数据库中获取用户名头像等</p>
<p>5、登出仅需删除token</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// user logout</span></span><br><span class="line">logout(&#123; commit, state &#125;) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line"></span><br><span class="line">      removeToken() <span class="comment">// must remove  token  first</span></span><br><span class="line">      resetRouter()</span><br><span class="line">      commit(<span class="string">'RESET_STATE'</span>)</span><br><span class="line">      resolve()</span><br><span class="line"></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<h3 id="用户管理获取用户列表删除修改增加等"><a href="#用户管理获取用户列表删除修改增加等" class="headerlink" title="用户管理获取用户列表删除修改增加等"></a>用户管理获取用户列表删除修改增加等</h3><h4 id="后台创建用户管理接口"><a href="#后台创建用户管理接口" class="headerlink" title="后台创建用户管理接口"></a>后台创建用户管理接口</h4><img src="/2020/07/16/vue后台管理系统/2.png">

<p>编写从数据库获取用户列表和删除列表等接口</p>
<h4 id="前端用户列表增删改查"><a href="#前端用户列表增删改查" class="headerlink" title="前端用户列表增删改查"></a>前端用户列表增删改查</h4><p>用户列表使用table组件，模板中匹配对应数据，在点击增加用户时设置跳转路由，跳转到对应增加信息的页面。</p>
<h4 id="列表分页操作"><a href="#列表分页操作" class="headerlink" title="列表分页操作"></a>列表分页操作</h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;el-pagination</span><br><span class="line">      background</span><br><span class="line">      layout=<span class="string">"prev, pager, next"</span></span><br><span class="line">      :total=<span class="string">"total"</span> @current-change=<span class="string">"getPageUsers"</span>&gt;</span><br><span class="line">    &lt;<span class="regexp">/el-pagination&gt;</span></span><br><span class="line"><span class="regexp">/</span><span class="regexp">/total总条数从后端获取</span></span><br><span class="line"><span class="regexp">/</span><span class="regexp">/下一页</span></span><br><span class="line"><span class="regexp">getPageUsers(page) &#123;</span></span><br><span class="line"><span class="regexp">        console.log(page);</span></span><br><span class="line"><span class="regexp">        userlist(&#123;page&#125;).then(response =&gt; &#123;</span></span><br><span class="line"><span class="regexp">          this.list = response.userlist;</span></span><br><span class="line"><span class="regexp">          this.total = response.total;</span></span><br><span class="line"><span class="regexp">          console.log(this.list)</span></span><br><span class="line"><span class="regexp">          this.listLoading = false</span></span><br><span class="line"><span class="regexp">        &#125;)</span></span><br><span class="line"><span class="regexp">      &#125;,</span></span><br></pre></td></tr></table></figure>

<h4 id="在删除时通过弹框提示"><a href="#在删除时通过弹框提示" class="headerlink" title="在删除时通过弹框提示"></a>在删除时通过弹框提示</h4><figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> del(id) &#123;</span><br><span class="line">        <span class="comment">//弹框</span></span><br><span class="line">        <span class="keyword">this</span>.$confirm(<span class="string">'是否需要删除$&#123;id&#125;的用户?, 确认删除'</span>, <span class="string">'提示'</span>, &#123;</span><br><span class="line">          confirmButtonText: <span class="string">'确定'</span>,</span><br><span class="line">          cancelButtonText: <span class="string">'取消'</span>,</span><br><span class="line">          type: <span class="string">'warning'</span></span><br><span class="line">        &#125;).then(<span class="keyword">async</span> () =&gt; &#123;</span><br><span class="line">          <span class="keyword">this</span>.listLoading = <span class="literal">true</span>;</span><br><span class="line">          <span class="keyword">let</span> res = <span class="keyword">await</span> deluser(&#123;id&#125;);</span><br><span class="line">          <span class="keyword">this</span>.listLoading = <span class="literal">false</span>;</span><br><span class="line">          <span class="keyword">this</span>.fetchData()</span><br><span class="line">          &#125;);</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<h4 id="添加权限操作"><a href="#添加权限操作" class="headerlink" title="添加权限操作"></a>添加权限操作</h4><p>后台编写添加权限接口</p>
<img src="/2020/07/16/vue后台管理系统/3.png">

<p>前台导出调用后台接口的请求给页面调用</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">addrule</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> request(&#123;</span><br><span class="line">    url:<span class="string">'/admin/rule/addrule'</span>,</span><br><span class="line">    method:<span class="string">'post'</span>,</span><br><span class="line">    data:data</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>addrule页面中调用方法，当提交表单时</p>
<figure class="hljs highlight javascript"><table><tr><td class="code"><pre><span class="line">methods: &#123;</span><br><span class="line">      onSubmit() &#123;</span><br><span class="line">        <span class="keyword">this</span>.$message(<span class="string">'submit!'</span>)</span><br><span class="line">        addrule(<span class="keyword">this</span>.form)</span><br><span class="line">        <span class="keyword">this</span>.$router.push(<span class="string">'/user/rulelist'</span>)</span><br><span class="line">      &#125;,</span><br><span class="line">      onCancel() &#123;</span><br><span class="line">        <span class="keyword">this</span>.$message(&#123;</span><br><span class="line">          message: <span class="string">'cancel!'</span>,</span><br><span class="line">          type: <span class="string">'warning'</span></span><br><span class="line">        &#125;)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前端</category>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>框架</tag>
        <tag>thinkJS</tag>
      </tags>
  </entry>
  <entry>
    <title>微信小程序</title>
    <url>/2020/05/20/%E5%BE%AE%E4%BF%A1%E5%B0%8F%E7%A8%8B%E5%BA%8F/</url>
    <content><![CDATA[<ol>
<li><p>x小程序没有DOM对象，一切基于组件化</p>
</li>
<li><p>小程序的4个重要文件</p>
<p>wxml—&gt;view结构—&gt; 类似HTML</p>
<p>wxss—&gt;view样式—-&gt; 类似CSS</p>
<p>js</p>
<p>json—&gt;数据—–&gt;json文件</p>
<a id="more"></a>
</li>
<li><p>需要储备的知识</p>
<p>理解事件机制</p>
<p>组件化</p>
<p>数据绑定</p>
<p>Flex布局</p>
<p>移动端适配</p>
</li>
</ol>
<p>根目录下的App()表示注册一个小程序 必须在app.js中调用且只能调用一次</p>
<p>根目录下的app.json文件用来对微信小程序进行全局配置，包含文件的路径，底部tab栏，默认窗口表现等。</p>
<p>根目录下的app.wxss中可以看到采用了flex布局。</p>
<h2 id="微信中的事件"><a href="#微信中的事件" class="headerlink" title="微信中的事件"></a>微信中的事件</h2><p>事件分为冒泡事件和非冒泡事件：</p>
<ol>
<li>冒泡事件：使用<code>bind</code>当一个组件上的事件被触发后，该事件会向父节点传递。</li>
<li>非冒泡事件：使用<code>catch</code>当一个组件上的事件被触发后，该事件不会向父节点传递。</li>
</ol>
<h2 id="列表渲染"><a href="#列表渲染" class="headerlink" title="列表渲染"></a>列表渲染</h2><figure class="hljs highlight js"><table><tr><td class="code"><pre><span class="line">&lt;block wx:<span class="keyword">for</span>=<span class="string">"&#123;&#123;content&#125;&#125;"</span> wx:key=<span class="string">"index"</span>&gt;</span><br><span class="line">	&lt;view <span class="class"><span class="keyword">class</span></span>=<span class="string">"content"</span>&gt;</span><br><span class="line">		&lt;view <span class="class"><span class="keyword">class</span></span>=<span class="string">"name"</span>&gt;姓名：&#123;&#123;item.name&#125;&#125;&lt;<span class="regexp">/view&gt;</span></span><br><span class="line"><span class="regexp">		&lt;view class="phone"&gt;手机号：&#123;&#123;item.phone&#125;&#125;&lt;/</span>view&gt;</span><br><span class="line">	&lt;<span class="regexp">/view&gt;</span></span><br><span class="line"><span class="regexp">&lt;/</span>block&gt;</span><br><span class="line"><span class="comment">//设置通信列表数据，content为列表名，name和phone为json数据的一个key</span></span><br><span class="line">data: &#123;</span><br><span class="line">    <span class="comment">//设置通信列表数据</span></span><br><span class="line">    content:[</span><br><span class="line">      &#123;<span class="attr">name</span>:<span class="string">"qi"</span>, <span class="attr">phone</span>:<span class="string">"12344444"</span>&#125;,</span><br><span class="line">      &#123;<span class="attr">name</span>:<span class="string">"qi1"</span>, <span class="attr">phone</span>:<span class="string">"12344444"</span>&#125;,</span><br><span class="line">      &#123;<span class="attr">name</span>:<span class="string">"qi2"</span>, <span class="attr">phone</span>:<span class="string">"12344444"</span>&#125;,</span><br><span class="line">      &#123;<span class="attr">name</span>:<span class="string">"qi3"</span>, <span class="attr">phone</span>:<span class="string">"12344444"</span>&#125;,</span><br><span class="line">      &#123;<span class="attr">name</span>:<span class="string">"qi4"</span>, <span class="attr">phone</span>:<span class="string">"12344444"</span>&#125;,</span><br><span class="line">    ]</span><br><span class="line">  &#125;,</span><br></pre></td></tr></table></figure>

<ul>
<li><p>页面跳转：</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">wx.navigateTo(&#123;</span><br><span class="line">  url:&apos;test/&apos;,</span><br><span class="line">  events:&#123;&#125;,</span><br><span class="line">  success: function(data)&#123;</span><br><span class="line">    </span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>swiper组件</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">view</span> <span class="attr">class</span>=<span class="string">"container"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">swiper</span> <span class="attr">indicator-dots</span>=<span class="string">"&#123;&#123;indicatorDots&#125;&#125;"</span><span class="attr">autoplay</span>=<span class="string">"&#123;&#123;autoplay&#125;&#125;"</span> <span class="attr">interval</span>=<span class="string">"&#123;&#123;interval&#125;&#125;"</span> <span class="attr">duration</span>=<span class="string">"&#123;&#123;duration&#125;&#125;"</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">swiper-item</span> <span class="attr">wx:for</span>=<span class="string">"&#123;&#123;background&#125;&#125;"</span> <span class="attr">wx:key</span>=<span class="string">"index"</span>&gt;</span></span><br><span class="line">				<span class="tag">&lt;<span class="name">image</span> <span class="attr">src</span>=<span class="string">"&#123;&#123;item&#125;&#125;"</span> <span class="attr">class</span>=<span class="string">"swiper-item"</span>&gt;</span><span class="tag">&lt;/<span class="name">image</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;/<span class="name">swiper-item</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">swiper</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">view</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="hljs highlight css"><table><tr><td class="code"><pre><span class="line">、<span class="selector-class">.container</span>&#123;</span><br><span class="line">  <span class="attribute">justify-content</span>: start;</span><br><span class="line">    //发现swiper一直在container中居中显示，是container有一个padding200rpx的样式</span><br><span class="line">  <span class="selector-tag">padding</span>: 0;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">swiper</span>&#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">400</span>rpx;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.swiper-item</span>&#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>列表循环</p>
<figure class="hljs highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">view</span> <span class="attr">wx:for</span>=<span class="string">"&#123;&#123;array&#125;&#125;"</span>&gt;</span></span><br><span class="line">  &#123;&#123;index&#125;&#125;: &#123;&#123;item.message&#125;&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">view</span>&gt;</span></span><br><span class="line">//使用wx:for-item 可以指定数组当前元素的变量名</span><br><span class="line">//使用 wx:for-index 可以指定数组当前下标的变量名</span><br></pre></td></tr></table></figure>

</li>
</ul>
]]></content>
      <categories>
        <category>微信小程序</category>
      </categories>
      <tags>
        <tag>小程序</tag>
      </tags>
  </entry>
  <entry>
    <title>vue项目问题</title>
    <url>/2020/06/01/vue%E9%A1%B9%E7%9B%AE%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h2 id="vue踩坑"><a href="#vue踩坑" class="headerlink" title="vue踩坑"></a>vue踩坑</h2><p>今天在执行项目时碰到这样的问题首先是项目</p>
<img src="/2020/06/01/vue项目问题/7.png">

<a id="more"></a>

<p>这是一个点歌系统，后端node+express那么这个表单在上传文件时也就是歌曲时。</p>
<img src="/2020/06/01/vue项目问题/3.png">

<p>报了这样一个错，检查跨域并没有问题，项目本身也不会有问题，毕竟早就完美运行过了</p>
<p>更换浏览器发现同样错误</p>
<img src="/2020/06/01/vue项目问题/8.png">

<p>当发送请求时</p>
<img src="/2020/06/01/vue项目问题/88.png">

<p>这个fs引起了注意，最终发现谷歌浏览器最近的MP3压缩格式后缀改成了mpeg。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>Vue</category>
      </categories>
      <tags>
        <tag>javaScript</tag>
        <tag>框架</tag>
        <tag>bug</tag>
      </tags>
  </entry>
  <entry>
    <title>更改github用户名导致hexo博客无法更新</title>
    <url>/2020/02/20/%E6%9B%B4%E6%94%B9github%E7%94%A8%E6%88%B7%E5%90%8D/</url>
    <content><![CDATA[<p>1 关于Github更改用户名字的问题，一直以来感觉自己的github名字起的比较幼稚，所以今天看着弹出来的一堆警告狠下心来改掉了用户名。</p>
<a id="more"></a>

<p>那么现在更改名字过后本地的库还存在但是需要手动更改所有项目的远程地址，我们可以通过以下途径看到他们：</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">git remote -v</span><br></pre></td></tr></table></figure>

<p>之后删除掉旧的远程地址：</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">git remote rm git@github.com:old_account/res.git</span><br></pre></td></tr></table></figure>

<p>最后，添加新的远程地址</p>
<figure class="hljs highlight plain"><table><tr><td class="code"><pre><span class="line">git remote add origin git@github.com:new_account/res.git</span><br></pre></td></tr></table></figure>

<h2 id="但是"><a href="#但是" class="headerlink" title="但是"></a>但是</h2><p>更改过后发现通过hexo搭建的博客无法更新的问题，找来找去发现了问题</p>




<p>需要将hexo中的配置地址更改，并且远程库的名字也要更改为新的名字</p>
]]></content>
      <categories>
        <category>计算机</category>
        <category>Git</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
</search>
